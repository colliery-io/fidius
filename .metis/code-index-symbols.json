{"files":{"crates/fidius-build/src/lib.rs":[{"name":"emit_wit","kind":"function","file_path":"crates/fidius-build/src/lib.rs","start_line":41,"end_line":48,"signature":"()","qualified_name":null,"doc_comment":"Regenerate `wit/` and the conversions from `src/lib.rs`. Call from `build.rs`.\nPanics with a clear message on any error (the build-script convention).","visibility":"public"},{"name":"run","kind":"function","file_path":"crates/fidius-build/src/lib.rs","start_line":52,"end_line":76,"signature":"(manifest_dir: &Path, out_dir: &Path) -> Result<(), String>","qualified_name":null,"doc_comment":"Core of [`emit_wit`], parameterized on the crate dir + output dir so it is\ntestable without a cargo build-script environment.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-build/src/lib.rs","start_line":79,"end_line":165,"signature":null,"qualified_name":null,"doc_comment":"trait and the `#[derive(WitType)]` types to live in `src/lib.rs`.\nRe-runs whenever `src/lib.rs` changes. v1 expects the `#[plugin_interface]`\n\nadapter `include!`s).\n- `$OUT_DIR/fidius_wit_conversions.rs` (the generated↔author `From` impls the\n`wit_bindgen::generate!{ path: \"wit\" }`), and\n- `wit/<interface>.wit` (consumed by the `#[plugin_impl]` adapter's\nIt parses the crate's `src/lib.rs`, and writes:\n\n```\n}\nfidius_build::emit_wit();\nfn main() {\n// build.rs\n```ignore\n\nstep. Call this from a one-line `build.rs`:\n`#[derive(WitType)]` user types must be generated from the source by a build\nA proc-macro can't see a crate's `struct`/`enum` definitions, so the WIT for\n\n`build.rs` helper for Fidius WASM plugins (FIDIUS-I-0023).","visibility":"private"},{"name":"writes_wit_and_conversions_for_a_user_typed_interface","kind":"function","file_path":"crates/fidius-build/src/lib.rs","start_line":83,"end_line":108,"signature":"()","qualified_name":null,"doc_comment":"trait and the `#[derive(WitType)]` types to live in `src/lib.rs`.\nRe-runs whenever `src/lib.rs` changes. v1 expects the `#[plugin_interface]`\n\nadapter `include!`s).\n- `$OUT_DIR/fidius_wit_conversions.rs` (the generated↔author `From` impls the\n`wit_bindgen::generate!{ path: \"wit\" }`), and\n- `wit/<interface>.wit` (consumed by the `#[plugin_impl]` adapter's\nIt parses the crate's `src/lib.rs`, and writes:\n\n```\n}\nfidius_build::emit_wit();\nfn main() {\n// build.rs\n```ignore\n\nstep. Call this from a one-line `build.rs`:\n`#[derive(WitType)]` user types must be generated from the source by a build\nA proc-macro can't see a crate's `struct`/`enum` definitions, so the WIT for\n\n`build.rs` helper for Fidius WASM plugins (FIDIUS-I-0023).","visibility":"private"},{"name":"follows_external_modules","kind":"function","file_path":"crates/fidius-build/src/lib.rs","start_line":111,"end_line":142,"signature":"()","qualified_name":null,"doc_comment":"trait and the `#[derive(WitType)]` types to live in `src/lib.rs`.\nRe-runs whenever `src/lib.rs` changes. v1 expects the `#[plugin_interface]`\n\nadapter `include!`s).\n- `$OUT_DIR/fidius_wit_conversions.rs` (the generated↔author `From` impls the\n`wit_bindgen::generate!{ path: \"wit\" }`), and\n- `wit/<interface>.wit` (consumed by the `#[plugin_impl]` adapter's\nIt parses the crate's `src/lib.rs`, and writes:\n\n```\n}\nfidius_build::emit_wit();\nfn main() {\n// build.rs\n```ignore\n\nstep. Call this from a one-line `build.rs`:\n`#[derive(WitType)]` user types must be generated from the source by a build\nA proc-macro can't see a crate's `struct`/`enum` definitions, so the WIT for\n\n`build.rs` helper for Fidius WASM plugins (FIDIUS-I-0023).","visibility":"private"},{"name":"primitives_only_writes_empty_conversions","kind":"function","file_path":"crates/fidius-build/src/lib.rs","start_line":145,"end_line":164,"signature":"()","qualified_name":null,"doc_comment":"trait and the `#[derive(WitType)]` types to live in `src/lib.rs`.\nRe-runs whenever `src/lib.rs` changes. v1 expects the `#[plugin_interface]`\n\nadapter `include!`s).\n- `$OUT_DIR/fidius_wit_conversions.rs` (the generated↔author `From` impls the\n`wit_bindgen::generate!{ path: \"wit\" }`), and\n- `wit/<interface>.wit` (consumed by the `#[plugin_impl]` adapter's\nIt parses the crate's `src/lib.rs`, and writes:\n\n```\n}\nfidius_build::emit_wit();\nfn main() {\n// build.rs\n```ignore\n\nstep. Call this from a one-line `build.rs`:\n`#[derive(WitType)]` user types must be generated from the source by a build\nA proc-macro can't see a crate's `struct`/`enum` definitions, so the WIT for\n\n`build.rs` helper for Fidius WASM plugins (FIDIUS-I-0023).","visibility":"private"}],"crates/fidius-cli/src/commands.rs":[{"name":"Result","kind":"type","file_path":"crates/fidius-cli/src/commands.rs","start_line":19,"end_line":19,"signature":"= std::result::Result<T, Box<dyn std::error::Error>>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"resolve_dep","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":30,"end_line":56,"signature":"(value: &str, version_override: Option<&str>) -> String","qualified_name":null,"doc_comment":"Resolve a dependency string to a Cargo.toml dependency value.\n\nLogic:\n1. If `value` is a path that exists on disk → `{ path = \"...\" }`\n2. If `version_override` is set → `\"<version>\"`\n3. Check crates.io for `value` → if found, use latest version\n4. Warn and fall back to `{ path = \"<value>\" }`","visibility":"private"},{"name":"check_crates_io","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":59,"end_line":74,"signature":"(name: &str) -> Option<String>","qualified_name":null,"doc_comment":"Check crates.io for a crate and return its latest version, if found.","visibility":"private"},{"name":"init_interface","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":78,"end_line":138,"signature":"(\n    name: &str,\n    trait_name: &str,\n    path: Option<&Path>,\n    version: Option<&str>,\n    extension: Option<&str>,\n) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"init_plugin","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":142,"end_line":280,"signature":"(\n    name: &str,\n    interface: &str,\n    trait_name: &str,\n    path: Option<&Path>,\n    version: Option<&str>,\n) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"init_host","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":284,"end_line":392,"signature":"(\n    name: &str,\n    interface: &str,\n    trait_name: &str,\n    path: Option<&Path>,\n    version: Option<&str>,\n) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"keygen","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":396,"end_line":417,"signature":"(out: &str) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"sign","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":421,"end_line":441,"signature":"(key_path: &Path, dylib_path: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"verify","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":445,"end_line":477,"signature":"(key_path: &Path, dylib_path: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"inspect","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":481,"end_line":527,"signature":"(dylib_path: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":531,"end_line":612,"signature":"(dir: &Path, release: bool) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_validate","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":616,"end_line":631,"signature":"(dir: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_build","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":635,"end_line":666,"signature":"(dir: &Path, release: bool) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_inspect","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":670,"end_line":707,"signature":"(dir: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_sign","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":711,"end_line":732,"signature":"(key_path: &Path, dir: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_verify","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":736,"end_line":764,"signature":"(key_path: &Path, dir: &Path) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package_pack","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":768,"end_line":807,"signature":"(dir: &Path, output: Option<&Path>, precompile: bool) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"prepare_wasm_pack","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":812,"end_line":836,"signature":"(dir: &Path, component: &str, precompile: bool) -> Result","qualified_name":null,"doc_comment":"Validate (and optionally precompile) a wasm component at pack time. Requires\nthe CLI built with `--features wasm` (pulls wasmtime via `fidius-host/wasm`).","visibility":"private"},{"name":"prepare_wasm_pack","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":839,"end_line":848,"signature":"(_dir: &Path, component: &str, precompile: bool) -> Result","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"record_precompiled","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":853,"end_line":874,"signature":"(dir: &Path, cwasm_name: &str) -> Result","qualified_name":null,"doc_comment":"Record `precompiled = \"<name>\"` under the `[wasm]` table in package.toml,\npreserving the rest of the file (string-insert; skips if already present).","visibility":"private"},{"name":"wit","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":882,"end_line":897,"signature":"(dir: Option<&Path>) -> Result","qualified_name":null,"doc_comment":"Generate `<dir>/wit/<interface>.wit` from `<dir>/src/lib.rs` (the\n`#[plugin_interface]` trait + `#[derive(WitType)]` types). The `build.rs`\nhelper (`fidius_build::emit_wit`) does this automatically on every build;\nthis subcommand is for CI / manual inspection.","visibility":"public"},{"name":"package_unpack","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":901,"end_line":906,"signature":"(archive: &Path, dest: Option<&Path>) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"python_stub","kind":"function","file_path":"crates/fidius-cli/src/commands.rs","start_line":910,"end_line":912,"signature":"(interface_src: &Path, out: &Path, trait_name: Option<&str>) -> Result","qualified_name":null,"doc_comment":null,"visibility":"public"}],"crates/fidius-cli/src/main.rs":[{"name":"commands","kind":"module","file_path":"crates/fidius-cli/src/main.rs","start_line":20,"end_line":20,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"python_stub","kind":"module","file_path":"crates/fidius-cli/src/main.rs","start_line":21,"end_line":21,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Cli","kind":"struct","file_path":"crates/fidius-cli/src/main.rs","start_line":25,"end_line":28,"signature":"{ command: Commands }","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Commands","kind":"enum","file_path":"crates/fidius-cli/src/main.rs","start_line":31,"end_line":145,"signature":"InitInterface | InitPlugin | InitHost | Keygen | Sign | Verify | Inspect | Test | Package | PythonStub | Wit","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"PackageCommands","kind":"enum","file_path":"crates/fidius-cli/src/main.rs","start_line":148,"end_line":203,"signature":"Validate | Build | Inspect | Sign | Verify | Pack | Unpack","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"main","kind":"function","file_path":"crates/fidius-cli/src/main.rs","start_line":205,"end_line":280,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-cli/src/python_stub.rs":[{"name":"Result","kind":"type","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":29,"end_line":29,"signature":"= std::result::Result<T, Box<dyn std::error::Error>>","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"MethodSpec","kind":"struct","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":32,"end_line":49,"signature":"{ name: String, arg_types: Vec<String>, arg_names_with_py_types: Vec<(String, String)>, return_type_string: String, retu...","qualified_name":null,"doc_comment":"One method extracted from a trait, ready for stub emission.","visibility":"private"},{"name":"generate_stub","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":54,"end_line":72,"signature":"(interface_src: &Path, requested_trait: Option<&str>) -> Result<String>","qualified_name":null,"doc_comment":"Generate the contents of a Python stub file for the named trait found in\n`interface_src`. If `requested_trait` is `None`, exactly one\n`#[plugin_interface]` trait must be present.","visibility":"public"},{"name":"write_stub","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":75,"end_line":89,"signature":"(interface_src: &Path, out_path: &Path, requested_trait: Option<&str>) -> Result","qualified_name":null,"doc_comment":"Write the stub for the named trait to `out_path`. Special-cases `-` for stdout.","visibility":"public"},{"name":"has_plugin_interface_attr","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":91,"end_line":99,"signature":"(item: &ItemTrait) -> bool","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"pick_trait","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":101,"end_line":135,"signature":"(\n    traits: &'a [&'a ItemTrait],\n    requested: Option<&str>,\n    src: &Path,\n) -> Result<&'a ItemTrait>","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"extract_methods","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":137,"end_line":146,"signature":"(item: &ItemTrait) -> Result<Vec<MethodSpec>>","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"method_to_spec","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":148,"end_line":208,"signature":"(method: &TraitItemFn) -> Result<MethodSpec>","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"is_wire_raw_attr","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":210,"end_line":222,"signature":"(attr: &syn::Attribute) -> bool","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"token_string","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":224,"end_line":226,"signature":"(t: &T) -> String","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"extract_doc_line","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":228,"end_line":242,"signature":"(attr: &syn::Attribute) -> Option<String>","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"rust_type_to_python","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":246,"end_line":313,"signature":"(ty: &Type) -> String","qualified_name":null,"doc_comment":"Map a Rust type to its Python type-hint counterpart. Unknown types fall\nback to `Any` with a TODO comment surfaced in the rendered stub.","visibility":"private"},{"name":"is_u8","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":315,"end_line":320,"signature":"(ty: &Type) -> bool","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"render_python_stub","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":322,"end_line":393,"signature":"(trait_name: &str, methods: &[MethodSpec]) -> String","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":396,"end_line":537,"signature":null,"qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"parse_methods","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":399,"end_line":411,"signature":"(src: &str) -> (String, Vec<MethodSpec>)","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"primitive_type_mapping","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":414,"end_line":429,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"vec_u8_maps_to_bytes_even_without_wire_raw","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":432,"end_line":442,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"wire_raw_signatures_are_bytes","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":445,"end_line":459,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"unknown_types_get_todo_marker","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":462,"end_line":472,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"rendered_stub_hash_matches_macro","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":475,"end_line":507,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"},{"name":"picks_named_trait_when_multiple_present","kind":"function","file_path":"crates/fidius-cli/src/python_stub.rs","start_line":510,"end_line":536,"signature":"()","qualified_name":null,"doc_comment":"agree byte-for-byte.\nthe same call path the proc macro uses, so the two are guaranteed to\nsignature strings produced by `fidius_core::hash::signature_string` —\nThe stub computes its hash via `fidius_core::hash::interface_hash` over\n\nthe `__interface_hash__` constant the host expects at load time.\nfile the plugin author imports. Gives them type-hinted signatures and\nPython stub generator: turn a `#[plugin_interface]` trait into a `.py`","visibility":"private"}],"crates/fidius-cli/tests/cli.rs":[{"name":"fidius_cmd","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":23,"end_line":25,"signature":"() -> Command","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"plugin_source_dir","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":27,"end_line":29,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"plugin_dir","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":31,"end_line":39,"signature":"() -> &'static Path","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"DIR","kind":"variable","file_path":"crates/fidius-cli/tests/cli.rs","start_line":32,"end_line":32,"signature":": std::sync::OnceLock<PathBuf>","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"smoke_dylib_name","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":41,"end_line":49,"signature":"() -> &'static str","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"help_works","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":52,"end_line":63,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"init_interface_creates_files","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":66,"end_line":95,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"init_interface_errors_if_exists","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":98,"end_line":127,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"init_plugin_creates_files","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":130,"end_line":162,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"keygen_sign_verify_roundtrip","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":165,"end_line":199,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"},{"name":"inspect_shows_plugin_info","kind":"function","file_path":"crates/fidius-cli/tests/cli.rs","start_line":202,"end_line":212,"signature":"()","qualified_name":null,"doc_comment":"CLI integration tests using assert_cmd.","visibility":"private"}],"crates/fidius-cli/tests/full_pipeline.rs":[{"name":"fides_cmd","kind":"function","file_path":"crates/fidius-cli/tests/full_pipeline.rs","start_line":23,"end_line":25,"signature":"() -> Command","qualified_name":null,"doc_comment":"Everything is generated from scratch by the CLI. No pre-written fixtures.\n\nTrue end-to-end test: scaffold → package → build → sign → load → call.","visibility":"private"},{"name":"workspace_fidius_path","kind":"function","file_path":"crates/fidius-cli/tests/full_pipeline.rs","start_line":28,"end_line":30,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"Path to the workspace root's `fidius` facade crate (for local dep resolution).","visibility":"private"},{"name":"full_pipeline_scaffold_package_build_sign_load_call","kind":"function","file_path":"crates/fidius-cli/tests/full_pipeline.rs","start_line":33,"end_line":363,"signature":"()","qualified_name":null,"doc_comment":"Everything is generated from scratch by the CLI. No pre-written fixtures.\n\nTrue end-to-end test: scaffold → package → build → sign → load → call.","visibility":"private"}],"crates/fidius-cli/tests/wasm_pack.rs":[{"name":"stage_wasm_pkg","kind":"function","file_path":"crates/fidius-cli/tests/wasm_pack.rs","start_line":28,"end_line":50,"signature":"(dir: &std::path::Path)","qualified_name":null,"doc_comment":"`fidius-host --features wasm` tests (`wasm_executor.rs`).\nactual validate/precompile + load round-trips are covered by the\nskipped with a warning, and `--precompile` errors with a rebuild hint. The\nThese cover the default (no-`wasm`-feature) build: validation/precompile are\n\nCLI wiring for `fidius package pack` on wasm packages (FIDIUS-T-0107).","visibility":"private"},{"name":"pack_wasm_package_archives_with_a_skip_warning","kind":"function","file_path":"crates/fidius-cli/tests/wasm_pack.rs","start_line":53,"end_line":73,"signature":"()","qualified_name":null,"doc_comment":"`fidius-host --features wasm` tests (`wasm_executor.rs`).\nactual validate/precompile + load round-trips are covered by the\nskipped with a warning, and `--precompile` errors with a rebuild hint. The\nThese cover the default (no-`wasm`-feature) build: validation/precompile are\n\nCLI wiring for `fidius package pack` on wasm packages (FIDIUS-T-0107).","visibility":"private"},{"name":"precompile_without_wasm_feature_errors","kind":"function","file_path":"crates/fidius-cli/tests/wasm_pack.rs","start_line":76,"end_line":87,"signature":"()","qualified_name":null,"doc_comment":"`fidius-host --features wasm` tests (`wasm_executor.rs`).\nactual validate/precompile + load round-trips are covered by the\nskipped with a warning, and `--precompile` errors with a rebuild hint. The\nThese cover the default (no-`wasm`-feature) build: validation/precompile are\n\nCLI wiring for `fidius package pack` on wasm packages (FIDIUS-T-0107).","visibility":"private"},{"name":"inspect_renders_wasm_fields","kind":"function","file_path":"crates/fidius-cli/tests/wasm_pack.rs","start_line":90,"end_line":127,"signature":"()","qualified_name":null,"doc_comment":"`fidius-host --features wasm` tests (`wasm_executor.rs`).\nactual validate/precompile + load round-trips are covered by the\nskipped with a warning, and `--precompile` errors with a rebuild hint. The\nThese cover the default (no-`wasm`-feature) build: validation/precompile are\n\nCLI wiring for `fidius package pack` on wasm packages (FIDIUS-T-0107).","visibility":"private"},{"name":"sign_verify_and_tamper_wasm_package","kind":"function","file_path":"crates/fidius-cli/tests/wasm_pack.rs","start_line":130,"end_line":163,"signature":"()","qualified_name":null,"doc_comment":"`fidius-host --features wasm` tests (`wasm_executor.rs`).\nactual validate/precompile + load round-trips are covered by the\nskipped with a warning, and `--precompile` errors with a rebuild hint. The\nThese cover the default (no-`wasm`-feature) build: validation/precompile are\n\nCLI wiring for `fidius package pack` on wasm packages (FIDIUS-T-0107).","visibility":"private"}],"crates/fidius-core/src/async_runtime.rs":[{"name":"FIDIUS_RUNTIME","kind":"variable","file_path":"crates/fidius-core/src/async_runtime.rs","start_line":25,"end_line":31,"signature":": std::sync::LazyLock<tokio::runtime::Runtime>","qualified_name":null,"doc_comment":"The shared tokio runtime for this dylib.\n\nInitialized on first use. One runtime per dylib, shared across all\nplugin implementations.","visibility":"public"}],"crates/fidius-core/src/lib.rs":[{"name":"package","kind":"module","file_path":"crates/fidius-core/src/lib.rs","start_line":16,"end_line":16,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"registry","kind":"module","file_path":"crates/fidius-core/src/lib.rs","start_line":17,"end_line":17,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"async_runtime","kind":"module","file_path":"crates/fidius-core/src/lib.rs","start_line":20,"end_line":20,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"}],"crates/fidius-core/src/package.rs":[{"name":"PackageManifest","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":32,"end_line":47,"signature":"{ package: PackageHeader, metadata: M, python: Option<PythonPackageMeta>, wasm: Option<WasmPackageMeta> }","qualified_name":null,"doc_comment":"A parsed package manifest, generic over the host-defined metadata schema.\n\nThe `M` type parameter is the host's metadata schema. If the `[metadata]`\nsection of `package.toml` doesn't deserialize into `M`, parsing fails —\nthis is how schema validation works.","visibility":"public"},{"name":"validate_runtime","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":56,"end_line":100,"signature":"(&self) -> Result<(), PackageError>","qualified_name":null,"doc_comment":"Cross-section validation: runtime + python section must agree.\n\n- `runtime = \"rust\"` (or absent → \"rust\") with a `[python]` section is rejected.\n- `runtime = \"python\"` without a `[python]` section is rejected.\n- Unknown runtime values are rejected (forward compat: a future\n`runtime = \"node\"` package shouldn't silently fall back to rust).","visibility":"public"},{"name":"PackageHeader","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":105,"end_line":123,"signature":"{ name: String, version: String, interface: String, interface_version: u32, extension: Option<String>, runtime: Option<S...","qualified_name":null,"doc_comment":"Fixed header fields that every package manifest must have.","visibility":"public"},{"name":"PackageHeader","kind":"type","file_path":"crates/fidius-core/src/package.rs","start_line":125,"end_line":157,"signature":"= PackageHeader","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"extension","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":127,"end_line":129,"signature":"(&self) -> &str","qualified_name":null,"doc_comment":"Returns the package extension, defaulting to `\"fid\"`.","visibility":"public"},{"name":"runtime","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":134,"end_line":144,"signature":"(&self) -> PackageRuntime","qualified_name":null,"doc_comment":"Returns the runtime kind, defaulting to `Rust` when absent. Returns\n`PackageRuntime::Rust` for unknown values; callers that need to reject\nunknown runtimes should use [`Self::runtime_strict`].","visibility":"public"},{"name":"runtime_strict","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":147,"end_line":156,"signature":"(&self) -> Result<PackageRuntime, PackageError>","qualified_name":null,"doc_comment":"Returns the runtime kind, erroring on unknown values.","visibility":"public"},{"name":"PackageRuntime","kind":"enum","file_path":"crates/fidius-core/src/package.rs","start_line":162,"end_line":175,"signature":"Rust | Python | Wasm","qualified_name":null,"doc_comment":"Plugin runtime kind. Determines which loader the host's `PluginHost`\ndispatches to.","visibility":"public"},{"name":"PackageRuntime","kind":"type","file_path":"crates/fidius-core/src/package.rs","start_line":177,"end_line":186,"signature":"= PackageRuntime","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"as_str","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":179,"end_line":185,"signature":"(&self) -> &'static str","qualified_name":null,"doc_comment":"Returns the canonical string form used in `package.toml`.","visibility":"public"},{"name":"PackageRuntime","kind":"type","file_path":"crates/fidius-core/src/package.rs","start_line":188,"end_line":192,"signature":"= PackageRuntime","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":189,"end_line":191,"signature":"(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"PythonPackageMeta","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":197,"end_line":206,"signature":"{ entry_module: String, requirements: Option<String> }","qualified_name":null,"doc_comment":"Fields under the `[python]` section of `package.toml`. Required when\n`package.runtime == \"python\"`, rejected otherwise.","visibility":"public"},{"name":"WasmPackageMeta","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":211,"end_line":225,"signature":"{ component: String, precompiled: Option<String>, capabilities: Vec<String> }","qualified_name":null,"doc_comment":"Fields under the `[wasm]` section of `package.toml`. Required when\n`package.runtime == \"wasm\"`, rejected otherwise.","visibility":"public"},{"name":"PythonPackageMeta","kind":"type","file_path":"crates/fidius-core/src/package.rs","start_line":227,"end_line":232,"signature":"= PythonPackageMeta","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"requirements_path","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":229,"end_line":231,"signature":"(&self) -> &str","qualified_name":null,"doc_comment":"Returns the requirements file path, defaulting to `\"requirements.txt\"`.","visibility":"public"},{"name":"PackageError","kind":"enum","file_path":"crates/fidius-core/src/package.rs","start_line":236,"end_line":300,"signature":"ManifestNotFound | ParseError | Io | BuildFailed | SignatureNotFound | SignatureInvalid | ArchiveError | InvalidArchive ...","qualified_name":null,"doc_comment":"Errors that can occur when loading a package manifest.","visibility":"public"},{"name":"UnpackOptions","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":309,"end_line":319,"signature":"{ max_decompressed: u64, max_ratio: u64, max_entries: u32 }","qualified_name":null,"doc_comment":"Options controlling archive extraction safety limits.\n\nConstruct with `UnpackOptions::default()` for strict defaults suitable for\nuntrusted input. Override individual fields for known-trusted archives that\nlegitimately exceed the default caps (e.g. packages that vendor large\nnative dependencies).","visibility":"public"},{"name":"UnpackOptions","kind":"type","file_path":"crates/fidius-core/src/package.rs","start_line":321,"end_line":329,"signature":"impl Default for UnpackOptions","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"default","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":322,"end_line":328,"signature":"() -> Self","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"load_manifest","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":349,"end_line":366,"signature":"(dir: &Path) -> Result<PackageManifest<M>, PackageError>","qualified_name":null,"doc_comment":"Load and parse a `package.toml` manifest from a package directory.\n\nThe type parameter `M` is the host's metadata schema. If the `[metadata]`\nsection doesn't deserialize into `M`, this returns `PackageError::ParseError`.\n\n# Example\n\n```ignore\n#[derive(Deserialize)]\nstruct MySchema {\ncategory: String,\nmin_host_version: String,\n}\n\nlet manifest = load_manifest::<MySchema>(Path::new(\"./my-package/\"))?;\nprintln!(\"Package: {} v{}\", manifest.package.name, manifest.package.version);\nprintln!(\"Category: {}\", manifest.metadata.category);\n```","visibility":"public"},{"name":"load_manifest_untyped","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":372,"end_line":374,"signature":"(dir: &Path) -> Result<PackageManifest<toml::Value>, PackageError>","qualified_name":null,"doc_comment":"Load a manifest validating only the fixed header (accepting any metadata).\n\nUses `toml::Value` as the metadata type so any `[metadata]` section is accepted.\nUseful for CLI tools that validate structure without knowing the host's schema.","visibility":"public"},{"name":"package_digest","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":384,"end_line":405,"signature":"(dir: &Path) -> Result<[u8; 32], PackageError>","qualified_name":null,"doc_comment":"Compute a deterministic SHA-256 digest over all package source files.\n\nWalks the package directory, collects all files (excluding `target/`,\n`.git/`, and `*.sig` files), sorts by relative path, and feeds each\nfile's relative path and contents into a SHA-256 hasher.\n\nThe resulting 32-byte digest covers the entire package contents.\nSign this digest to protect against tampering.","visibility":"public"},{"name":"collect_files","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":408,"end_line":439,"signature":"(root: &Path, dir: &Path, out: &mut Vec<String>) -> Result<(), PackageError>","qualified_name":null,"doc_comment":"Recursively collect file paths relative to `root`, skipping excluded dirs/files.","visibility":"private"},{"name":"collect_archive_files","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":442,"end_line":470,"signature":"(\n    root: &Path,\n    dir: &Path,\n    out: &mut Vec<String>,\n) -> Result<(), PackageError>","qualified_name":null,"doc_comment":"Recursively collect file paths for archiving (includes `.sig` files).","visibility":"private"},{"name":"PackResult","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":474,"end_line":479,"signature":"{ path: PathBuf, unsigned: bool }","qualified_name":null,"doc_comment":"Result of packing a package, including any warnings.","visibility":"public"},{"name":"vendor_python_deps","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":490,"end_line":545,"signature":"(dir: &Path, py: &PythonPackageMeta) -> Result<(), PackageError>","qualified_name":null,"doc_comment":"Vendor Python dependencies into `<dir>/vendor/` by invoking\n`python3 -m pip install -r <requirements> --target ./vendor/`.\n\n- If `vendor/` already exists, leave it alone — the plugin author may have\npre-vendored deliberately for reproducibility.\n- If the declared requirements file is missing AND `vendor/` is missing,\nemit a tracing warning and proceed (zero-dep python plugin).\n- If pip fails, surface its stderr as `PackageError::ArchiveError` so the\nuser sees the resolver/build error directly.","visibility":"private"},{"name":"pack_package","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":560,"end_line":613,"signature":"(dir: &Path, output: Option<&Path>) -> Result<PackResult, PackageError>","qualified_name":null,"doc_comment":"Create a `.fid` archive (tar + bzip2) from a package directory.\n\nThe archive contains a single top-level directory `{name}-{version}/`\nwith all source files. Excludes `target/` and `.git/` directories.\nIncludes `package.sig` if present.\n\nFor Python packages (`runtime = \"python\"`), if a `requirements.txt` is\ndeclared and a `vendor/` directory does not yet exist, `pip install -r\n<requirements> --target ./vendor/` runs first and the result is included\nin the archive. Pre-existing `vendor/` is respected and used as-is.\n\nIf `output` is `None`, the archive is written to the current directory\nas `{name}-{version}.fid`.","visibility":"public"},{"name":"unpack_package","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":632,"end_line":634,"signature":"(archive: &Path, dest: &Path) -> Result<PathBuf, PackageError>","qualified_name":null,"doc_comment":"Extract a `.fid` archive (tar + bzip2) to a destination directory using\nstrict safety defaults.\n\nReturns the path to the extracted top-level package directory, which is\nguaranteed to exist inside `dest` and contain a `package.toml`.\n\nThis function validates every archive entry before extracting and rejects\narchives containing: path-traversal components (`..`), absolute paths,\nsymlinks, hardlinks, more than 10,000 entries, or a cumulative declared\ndecompressed size exceeding 500 MB or 10× the compressed archive size.\n\nExtraction is staged inside a temporary directory under `dest` and the\npackage directory is moved into place atomically on success. If validation\nfails mid-archive, no files are left in `dest`.\n\nFor archives that legitimately exceed the default caps, use\n[`unpack_package_with_options`].","visibility":"public"},{"name":"unpack_package_with_options","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":640,"end_line":777,"signature":"(\n    archive: &Path,\n    dest: &Path,\n    options: &UnpackOptions,\n) -> Result<PathBuf, PackageError>","qualified_name":null,"doc_comment":"Extract a `.fid` archive with caller-provided safety limits.\n\nSee [`unpack_package`] for the default-strict variant. Use this when the\narchive's size or entry count legitimately exceeds the defaults.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-core/src/package.rs","start_line":780,"end_line":1696,"signature":null,"qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"write_manifest","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":784,"end_line":786,"signature":"(dir: &Path, content: &str)","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"TestMeta","kind":"struct","file_path":"crates/fidius-core/src/package.rs","start_line":789,"end_line":793,"signature":"{ category: String, tags: Vec<String> }","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"valid_manifest_parses","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":796,"end_line":820,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"missing_required_metadata_field_fails","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":823,"end_line":847,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"missing_manifest_returns_not_found","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":850,"end_line":854,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"extra_metadata_fields_ignored","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":857,"end_line":878,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"untyped_manifest_accepts_any_metadata","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":881,"end_line":902,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"digest_is_deterministic","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":905,"end_line":913,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"digest_changes_on_file_modification","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":916,"end_line":927,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"digest_excludes_target_and_sig","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":930,"end_line":944,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"make_package","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":946,"end_line":962,"signature":"(dir: &Path)","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_unpack_round_trip","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":965,"end_line":986,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_includes_sig_file","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":989,"end_line":1003,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_excludes_target_and_git","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1006,"end_line":1022,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_invalid_archive_no_manifest","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1025,"end_line":1055,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_default_output_name","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1058,"end_line":1068,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_custom_extension","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1071,"end_line":1101,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"extension_defaults_to_fid","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1104,"end_line":1120,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"rust_runtime_default_when_absent","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1125,"end_line":1143,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"python_runtime_with_python_section_parses","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1146,"end_line":1171,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"python_runtime_requirements_default","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1174,"end_line":1198,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"python_runtime_without_python_section_rejected","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1201,"end_line":1227,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"python_section_without_python_runtime_rejected","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1230,"end_line":1250,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unknown_runtime_rejected","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1253,"end_line":1276,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"package_runtime_display_and_str","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1279,"end_line":1283,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"build_archive","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1293,"end_line":1302,"signature":"(path: &Path, build: F)","qualified_name":null,"doc_comment":"Build a bz2-compressed tar archive from a builder callback.","visibility":"private"},{"name":"write_name","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1307,"end_line":1315,"signature":"(header: &mut Header, path: &str)","qualified_name":null,"doc_comment":"Write a raw entry name directly into a GNU tar header, bypassing\n`set_path`'s safety validation. This is only safe in tests where we\ndeliberately craft malicious paths.","visibility":"private"},{"name":"write_linkname","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1317,"end_line":1325,"signature":"(header: &mut Header, link: &str)","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"append_regular","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1330,"end_line":1338,"signature":"(tar: &mut tar::Builder<BzEncoder<std::fs::File>>, path: &str, data: &[u8])","qualified_name":null,"doc_comment":"Append a regular file entry with explicit path and content bytes.\nUses the low-level name-writing helper so arbitrary (including\nmalicious) paths can be tested.","visibility":"private"},{"name":"append_link","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1341,"end_line":1355,"signature":"(\n        tar: &mut tar::Builder<BzEncoder<std::fs::File>>,\n        path: &str,\n        link_target: &str,\n        ty: EntryType,\n    )","qualified_name":null,"doc_comment":"Append a link entry with a chosen EntryType (symlink/hardlink).","visibility":"private"},{"name":"unpack_rejects_parent_dir_component","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1358,"end_line":1373,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_absolute_path","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1376,"end_line":1389,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_symlink","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1392,"end_line":1405,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_hardlink","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1408,"end_line":1421,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_symlink_then_file_rejected_at_first_entry","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1424,"end_line":1445,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_declared_size_bomb","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1448,"end_line":1477,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_ratio_bomb","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1480,"end_line":1507,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_rejects_too_many_entries","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1510,"end_line":1529,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_staging_cleans_up_on_rejection","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1532,"end_line":1553,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"unpack_with_options_accepts_large_archive","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1556,"end_line":1574,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"make_python_package","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1579,"end_line":1613,"signature":"(dir: &Path, with_requirements: Option<&str>)","qualified_name":null,"doc_comment":"Build a minimal Python package directory (manifest + entry .py).","visibility":"private"},{"name":"pack_python_with_prevendored_directory_skips_pip","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1616,"end_line":1642,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_python_with_no_requirements_or_vendor_warns_but_succeeds","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1645,"end_line":1658,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"},{"name":"pack_python_with_unresolvable_requirement_surfaces_pip_error","kind":"function","file_path":"crates/fidius-core/src/package.rs","start_line":1661,"end_line":1695,"signature":"()","qualified_name":null,"doc_comment":"host-defined schema type.\nan extensible `[metadata]` section validated via serde against a\nmanifest. The manifest has a fixed header (name, version, interface) and\nA package is a directory containing plugin source code and a `package.toml`\n\nSource package manifest types and parsing.","visibility":"private"}],"crates/fidius-core/src/registry.rs":[{"name":"DescriptorEntry","kind":"struct","file_path":"crates/fidius-core/src/registry.rs","start_line":24,"end_line":26,"signature":"{ descriptor: &'static PluginDescriptor }","qualified_name":null,"doc_comment":"A submitted descriptor pointer. Used with `inventory` for distributed collection.","visibility":"public"},{"name":"build_registry","kind":"function","file_path":"crates/fidius-core/src/registry.rs","start_line":34,"end_line":49,"signature":"() -> PluginRegistry","qualified_name":null,"doc_comment":"Build the plugin registry from all submitted descriptors.\n\nAllocates a `Vec` of descriptor pointers and leaks it to get a `'static` pointer.\nCalled once; the result is cached in `OnceLock`.","visibility":"private"},{"name":"get_registry","kind":"function","file_path":"crates/fidius-core/src/registry.rs","start_line":55,"end_line":58,"signature":"() -> &'static PluginRegistry","qualified_name":null,"doc_comment":"Get or build the plugin registry.\n\nReturns a `'static` reference to the registry. Built on first call,\ncached for subsequent calls.","visibility":"public"},{"name":"REGISTRY","kind":"variable","file_path":"crates/fidius-core/src/registry.rs","start_line":56,"end_line":56,"signature":": std::sync::OnceLock<PluginRegistry>","qualified_name":null,"doc_comment":"`fidius_get_registry` export function that the host calls via `dlsym`.\nPlugin crates call `fidius_plugin_registry!()` once in their lib.rs to emit the\nEach `#[plugin_impl]` submits its `PluginDescriptor` pointer via `inventory::submit!`.\n\nPlugin registry assembly for multi-plugin dylibs.","visibility":"private"},{"name":"fidius_plugin_registry","kind":"macro","file_path":"crates/fidius-core/src/registry.rs","start_line":69,"end_line":76,"signature":null,"qualified_name":null,"doc_comment":"Emit the `fidius_get_registry` export function.\n\nCall this once in your plugin cdylib's `lib.rs`. The host calls\n`dlsym(\"fidius_get_registry\")` and invokes it to get the registry.\n\n```ignore\nfidius::fidius_plugin_registry!();\n```","visibility":"private"}],"crates/fidius-core/tests/layout_and_roundtrip.rs":[{"name":"registry_size_and_align","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":32,"end_line":36,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"registry_field_offsets","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":39,"end_line":44,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"descriptor_size_and_align","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":49,"end_line":58,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"descriptor_field_offsets","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":61,"end_line":83,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"buffer_strategy_kind_layout","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":88,"end_line":93,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"status_code_values","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":98,"end_line":104,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"TestPayload","kind":"struct","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":109,"end_line":113,"signature":"{ name: String, value: i64, tags: Vec<String> }","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"wire_roundtrip","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":116,"end_line":126,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"wire_is_bincode_always","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":129,"end_line":143,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"plugin_error_roundtrip_without_details","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":148,"end_line":155,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"plugin_error_roundtrip_with_details","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":158,"end_line":165,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"plugin_error_display","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":168,"end_line":171,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"hash_known_vectors","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":176,"end_line":204,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"hash_const_fnv1a","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":207,"end_line":212,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"HASH","kind":"variable","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":209,"end_line":209,"signature":": u64","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"magic_bytes_value","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":217,"end_line":220,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"},{"name":"version_constants","kind":"function","file_path":"crates/fidius-core/tests/layout_and_roundtrip.rs","start_line":223,"end_line":230,"signature":"()","qualified_name":null,"doc_comment":"and interface hash determinism.\nand field offsets. They also verify wire format round-trip correctness\nThese tests guard ABI stability by asserting struct sizes, alignments,\n\nLayout assertion tests and serialization round-trip tests for fidius-core.","visibility":"private"}],"crates/fidius-guest/src/descriptor.rs":[{"name":"FIDIUS_MAGIC","kind":"variable","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":24,"end_line":24,"signature":": [u8; 8]","qualified_name":null,"doc_comment":"Magic bytes identifying a Fidius plugin registry.","visibility":"public"},{"name":"REGISTRY_VERSION","kind":"variable","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":27,"end_line":27,"signature":": u32","qualified_name":null,"doc_comment":"Current version of the `PluginRegistry` struct layout.","visibility":"public"},{"name":"parse_u32_const","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":34,"end_line":43,"signature":"(s: &str) -> u32","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"CRATE_MAJOR","kind":"variable","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":45,"end_line":45,"signature":": u32","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"CRATE_MINOR","kind":"variable","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":46,"end_line":46,"signature":": u32","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"ABI_VERSION","kind":"variable","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":50,"end_line":54,"signature":": u32","qualified_name":null,"doc_comment":"Current version of the `PluginDescriptor` struct layout. Derived from the\n`fidius-core` crate version per ADR-0002.","visibility":"public"},{"name":"BufferStrategyKind","kind":"enum","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":65,"end_line":84,"signature":"PluginAllocated | Arena","qualified_name":null,"doc_comment":"Buffer management strategy for an interface.\n\nSelected per-trait via `#[plugin_interface(buffer = ...)]`.\nDetermines the FFI function pointer signatures in the vtable.\n\nDiscriminant value `0` is reserved (previously `CallerAllocated`, removed\nin 0.1.0 — its value proposition was subsumed by `PluginAllocated`).","visibility":"public"},{"name":"MetaKv","kind":"struct","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":94,"end_line":99,"signature":"{ key: *const c_char, value: *const c_char }","qualified_name":null,"doc_comment":"Static key/value pair for method-level or trait-level metadata.\n\nBoth `key` and `value` point to null-terminated UTF-8 C strings with\n`'static` lifetime (typically string literals embedded in the plugin's\n`.rodata`). Fidius treats values as opaque — hosts define conventions\nvia their own metadata schemas. See ADR/spec for the `fidius.*`\nreserved namespace.","visibility":"public"},{"name":"MetaKv","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":102,"end_line":102,"signature":"impl Send for MetaKv","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"MetaKv","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":103,"end_line":103,"signature":"impl Sync for MetaKv","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"MethodMetaEntry","kind":"struct","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":112,"end_line":118,"signature":"{ kvs: *const MetaKv, kv_count: u32 }","qualified_name":null,"doc_comment":"Per-method metadata entry. One entry per method in declaration order,\nstored in the array referenced by `PluginDescriptor::method_metadata`.\n\nMethods with no `#[method_meta(...)]` annotations have `kvs: null` and\n`kv_count: 0` — the entry exists but is empty, so hosts can index\nuniformly by method index.","visibility":"public"},{"name":"MethodMetaEntry","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":121,"end_line":121,"signature":"impl Send for MethodMetaEntry","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"MethodMetaEntry","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":122,"end_line":122,"signature":"impl Sync for MethodMetaEntry","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"BufferStrategyKind","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":124,"end_line":131,"signature":"= BufferStrategyKind","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":125,"end_line":130,"signature":"(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginRegistry","kind":"struct","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":145,"end_line":154,"signature":"{ magic: [u8; 8], registry_version: u32, plugin_count: u32, descriptors: *const *const PluginDescriptor }","qualified_name":null,"doc_comment":"Top-level registry exported by every Fidius plugin dylib.\n\nEach dylib exports exactly one `FIDIUS_PLUGIN_REGISTRY` static symbol\npointing to this struct. The registry contains pointers to one or more\n`PluginDescriptor`s (supporting multiple plugins per dylib).\n\n# Safety\n\n- `descriptors` must point to a valid array of `plugin_count` pointers.\n- Each pointer in the array must point to a valid `PluginDescriptor`.\n- All pointed-to data must have `'static` lifetime (typically link-time constants).","visibility":"public"},{"name":"PluginRegistry","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":159,"end_line":159,"signature":"impl Send for PluginRegistry","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginRegistry","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":160,"end_line":160,"signature":"impl Sync for PluginRegistry","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginDescriptor","kind":"struct","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":177,"end_line":236,"signature":"{ descriptor_size: u32, abi_version: u32, interface_name: *const c_char, interface_hash: u64, interface_version: u32, ca...","qualified_name":null,"doc_comment":"Metadata descriptor for a single plugin within a dylib.\n\nContains all information the host needs to validate and call the plugin\nwithout executing any plugin code. All string fields are pointers to\nstatic, null-terminated C strings embedded in the dylib.\n\n# Safety\n\n- `interface_name` and `plugin_name` must point to valid, null-terminated,\nUTF-8 C strings with `'static` lifetime.\n- `vtable` must point to a valid `#[repr(C)]` vtable struct matching the\ninterface identified by `interface_name` and `interface_hash`.\n- When `buffer_strategy == PluginAllocated`, `free_buffer` must be `Some`.\n- All pointed-to data must outlive any `PluginHandle` derived from this descriptor.","visibility":"public"},{"name":"PluginDescriptor","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":241,"end_line":241,"signature":"impl Send for PluginDescriptor","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginDescriptor","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":242,"end_line":242,"signature":"impl Sync for PluginDescriptor","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"DescriptorPtr","kind":"struct","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":250,"end_line":250,"signature":null,"qualified_name":null,"doc_comment":"A `Sync` wrapper for a raw pointer to a `PluginDescriptor`.\n\nUsed in static contexts where a `*const PluginDescriptor` needs to live\nin a `static` variable (which requires `Sync`). The pointed-to descriptor\nmust have `'static` lifetime.","visibility":"public"},{"name":"DescriptorPtr","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":253,"end_line":253,"signature":"impl Send for DescriptorPtr","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"DescriptorPtr","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":254,"end_line":254,"signature":"impl Sync for DescriptorPtr","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginDescriptor","kind":"type","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":256,"end_line":300,"signature":"= PluginDescriptor","qualified_name":null,"doc_comment":"All types use `#[repr(C)]` layout and are read directly from dylib memory.\nThese types form the stable C ABI contract between host and plugin.\n\nFFI descriptor and registry types for the Fidius plugin framework.","visibility":"private"},{"name":"interface_name_str","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":263,"end_line":266,"signature":"(&self) -> &str","qualified_name":null,"doc_comment":"Read the `interface_name` field as a Rust `&str`.\n\n# Safety\n\n`interface_name` must point to a valid, null-terminated, UTF-8 C string\nthat outlives the returned reference.","visibility":"public"},{"name":"plugin_name_str","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":274,"end_line":277,"signature":"(&self) -> &str","qualified_name":null,"doc_comment":"Read the `plugin_name` field as a Rust `&str`.\n\n# Safety\n\n`plugin_name` must point to a valid, null-terminated, UTF-8 C string\nthat outlives the returned reference.","visibility":"public"},{"name":"buffer_strategy_kind","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":283,"end_line":289,"signature":"(&self) -> Result<BufferStrategyKind, u8>","qualified_name":null,"doc_comment":"Returns the `buffer_strategy` field as a `BufferStrategyKind`.\n\nReturns `Err(value)` if the discriminant is unknown. This can happen\nwith malformed plugins — callers should reject rather than panic.","visibility":"public"},{"name":"has_capability","kind":"function","file_path":"crates/fidius-guest/src/descriptor.rs","start_line":294,"end_line":299,"signature":"(&self, bit: u32) -> bool","qualified_name":null,"doc_comment":"Check if the given optional method capability bit is set.\n\nReturns `false` for bit indices >= 64 rather than panicking.","visibility":"public"}],"crates/fidius-guest/src/error.rs":[{"name":"PluginError","kind":"struct","file_path":"crates/fidius-guest/src/error.rs","start_line":30,"end_line":37,"signature":"{ code: String, message: String, details: Option<String> }","qualified_name":null,"doc_comment":"Error returned by plugin method implementations to signal business logic failures.\n\nSerialized as bincode across the FFI boundary. The host deserializes\nthis from the output buffer when the FFI shim returns `STATUS_PLUGIN_ERROR`.\n\nThe `details` field is stored as a JSON string (not `serde_json::Value`)\nbecause bincode cannot deserialize a free-form `serde_json::Value`\n(it lacks `deserialize_any`). Storing it as a `String` keeps the error\nstruct bincode-compatible.","visibility":"public"},{"name":"PluginError","kind":"type","file_path":"crates/fidius-guest/src/error.rs","start_line":39,"end_line":72,"signature":"= PluginError","qualified_name":null,"doc_comment":"Error types for the Fidius plugin framework.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-guest/src/error.rs","start_line":41,"end_line":47,"signature":"(code: impl Into<String>, message: impl Into<String>) -> Self","qualified_name":null,"doc_comment":"Create a new `PluginError` without details.","visibility":"public"},{"name":"with_details","kind":"function","file_path":"crates/fidius-guest/src/error.rs","start_line":52,"end_line":62,"signature":"(\n        code: impl Into<String>,\n        message: impl Into<String>,\n        details: serde_json::Value,\n    ) -> Self","qualified_name":null,"doc_comment":"Create a new `PluginError` with structured details.\n\nThe `serde_json::Value` is serialized to a JSON string for storage.","visibility":"public"},{"name":"details_value","kind":"function","file_path":"crates/fidius-guest/src/error.rs","start_line":67,"end_line":71,"signature":"(&self) -> Option<serde_json::Value>","qualified_name":null,"doc_comment":"Parse the `details` field back into a `serde_json::Value`.\n\nReturns `None` if details is absent or fails to parse.","visibility":"public"},{"name":"PluginError","kind":"type","file_path":"crates/fidius-guest/src/error.rs","start_line":74,"end_line":78,"signature":"= PluginError","qualified_name":null,"doc_comment":"Error types for the Fidius plugin framework.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-guest/src/error.rs","start_line":75,"end_line":77,"signature":"(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result","qualified_name":null,"doc_comment":"Error types for the Fidius plugin framework.","visibility":"private"},{"name":"PluginError","kind":"type","file_path":"crates/fidius-guest/src/error.rs","start_line":80,"end_line":80,"signature":"= PluginError","qualified_name":null,"doc_comment":"Error types for the Fidius plugin framework.","visibility":"private"}],"crates/fidius-guest/src/frame.rs":[{"name":"FRAME_ITEM","kind":"variable","file_path":"crates/fidius-guest/src/frame.rs","start_line":45,"end_line":45,"signature":": u8","qualified_name":null,"doc_comment":"Frame tag: one streamed item. Payload is bincode/`Value` wire bytes.","visibility":"public"},{"name":"FRAME_END","kind":"variable","file_path":"crates/fidius-guest/src/frame.rs","start_line":47,"end_line":47,"signature":": u8","qualified_name":null,"doc_comment":"Frame tag: clean end of stream. No payload.","visibility":"public"},{"name":"FRAME_ERROR","kind":"variable","file_path":"crates/fidius-guest/src/frame.rs","start_line":49,"end_line":49,"signature":": u8","qualified_name":null,"doc_comment":"Frame tag: producer error. Payload is a bincode [`PluginError`].","visibility":"public"},{"name":"FRAME_HEADER_LEN","kind":"variable","file_path":"crates/fidius-guest/src/frame.rs","start_line":52,"end_line":52,"signature":": usize","qualified_name":null,"doc_comment":"Fixed size of a frame header: one tag byte plus a `u32` length.","visibility":"public"},{"name":"Frame","kind":"enum","file_path":"crates/fidius-guest/src/frame.rs","start_line":60,"end_line":67,"signature":"Item | End | Error","qualified_name":null,"doc_comment":"One frame crossing the streaming boundary.\n\n`Item` carries opaque payload bytes (the caller decides how to decode them —\ntypically `wire::deserialize::<T>()` or a `Value`), so this type stays\nbackend- and schema-neutral.","visibility":"public"},{"name":"FrameError","kind":"enum","file_path":"crates/fidius-guest/src/frame.rs","start_line":71,"end_line":88,"signature":"Truncated | UnknownTag | Payload | Malformed","qualified_name":null,"doc_comment":"Errors decoding a [`Frame`] from bytes.","visibility":"public"},{"name":"Frame","kind":"type","file_path":"crates/fidius-guest/src/frame.rs","start_line":90,"end_line":154,"signature":"= Frame","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"encode","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":92,"end_line":103,"signature":"(&self) -> Result<Vec<u8>, WireError>","qualified_name":null,"doc_comment":"Encode this frame as `[tag][len][payload]`.","visibility":"public"},{"name":"decode","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":108,"end_line":117,"signature":"(bytes: &[u8]) -> Result<Frame, FrameError>","qualified_name":null,"doc_comment":"Decode exactly one frame from `bytes`, which must contain a single frame\nand nothing more. Use [`Frame::read`] to pull one frame from a larger\nbuffer and learn how many bytes were consumed.","visibility":"public"},{"name":"read","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":122,"end_line":153,"signature":"(bytes: &[u8]) -> Result<(Frame, usize), FrameError>","qualified_name":null,"doc_comment":"Read one frame from the front of `bytes`, returning the frame and the\nnumber of bytes consumed. Lets a decoder walk a buffer holding several\nconcatenated frames (the cdylib `next_batch` / `Value`-rail path).","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-guest/src/frame.rs","start_line":157,"end_line":265,"signature":null,"qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"item","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":160,"end_line":162,"signature":"(payload: &[u8]) -> Frame","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"item_round_trip","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":165,"end_line":170,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"end_round_trip","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":173,"end_line":178,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"error_round_trip","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":181,"end_line":187,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"empty_item_is_valid","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":190,"end_line":194,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"read_walks_concatenated_frames","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":197,"end_line":210,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"truncated_header_is_rejected","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":213,"end_line":216,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"truncated_payload_is_rejected","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":219,"end_line":226,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"unknown_tag_is_rejected","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":229,"end_line":236,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"end_with_payload_is_rejected","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":239,"end_line":247,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"trailing_bytes_after_single_decode_rejected","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":250,"end_line":257,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"},{"name":"garbage_is_rejected_not_panicking","kind":"function","file_path":"crates/fidius-guest/src/frame.rs","start_line":260,"end_line":264,"signature":"()","qualified_name":null,"doc_comment":"D5) is simply *n* `ITEM` frames concatenated, needing no wire change.\nis deliberately batch-compatible: a future `next_batch(n)` (design decision\nhost-allocated buffer filled by an FFI `next()` export (cdylib). The scheme\nper `next()` (WASM), a `bytes` object yielded by a generator (Python), or a\nThis framing is identical across every backend — a WIT `list<u8>` returned\n\nfollow it (enforced by the host-side decoder, [`crate::frame`] consumers).\n[`PluginError`]. An error frame *terminates* the stream: no frames may\n- [`FRAME_ERROR`] — the producer failed mid-stream; `payload` is a bincode\n- [`FRAME_END`] — clean end of stream; `len == 0`, no payload.\nthe unary wire format verbatim (one item == one unary payload).\nwire bytes, identical to a unary return value. Streaming therefore inherits\n- [`FRAME_ITEM`] — one streamed item; `payload` is the existing bincode/`Value`\n\nThree tags exist:\n\n```\n[ tag: u8 ][ len: u32 little-endian ][ payload: len bytes ]\n```text\n\nEach frame is length-delimited and self-identifying:\nA streaming method call yields a *sequence of frames* rather than one buffer.\n\n(FIDIUS-I-0026, design decision D2).\nStreaming frame format for the server-streaming plugin boundary","visibility":"private"}],"crates/fidius-guest/src/hash.rs":[{"name":"FNV_OFFSET_BASIS","kind":"variable","file_path":"crates/fidius-guest/src/hash.rs","start_line":22,"end_line":22,"signature":": u64","qualified_name":null,"doc_comment":"FNV-1a 64-bit offset basis.","visibility":"private"},{"name":"FNV_PRIME","kind":"variable","file_path":"crates/fidius-guest/src/hash.rs","start_line":25,"end_line":25,"signature":": u64","qualified_name":null,"doc_comment":"FNV-1a 64-bit prime.","visibility":"private"},{"name":"fnv1a","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":28,"end_line":37,"signature":"(bytes: &[u8]) -> u64","qualified_name":null,"doc_comment":"Compute the FNV-1a 64-bit hash of a byte slice.","visibility":"public"},{"name":"interface_hash","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":47,"end_line":52,"signature":"(signatures: &[&str]) -> u64","qualified_name":null,"doc_comment":"Compute the interface hash from a set of method signatures.\n\nSignatures are sorted lexicographically before hashing to ensure\norder-independence. Each signature is joined with `\\n` as a separator.\n\nThis function is **not** `const` because it allocates for sorting.\nThe proc macro calls this at compile time via a build-script-like pattern,\nor uses `fnv1a` directly on pre-sorted, concatenated signatures.","visibility":"public"},{"name":"signature_string","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":80,"end_line":97,"signature":"(\n    name: &str,\n    arg_types: &[String],\n    ret: &str,\n    wire_raw: bool,\n    streaming: bool,\n) -> String","qualified_name":null,"doc_comment":"Build the canonical signature string for one method.\n\nFormat: `\"{name}:{arg_type_1},{arg_type_2}->{return_type}{!raw?}\"`.\n\n- `arg_types` are pre-stringified (typically by `syn::Type` →\n`to_token_stream().to_string()` — the proc macro and any other\ntooling that wants to compute the same hash must use the same\nformatter).\n- `return_type` is the stringified return type, or empty string for\nmethods returning `()`.\n- `wire_raw = true` appends a trailing `!raw` marker so methods opted\ninto raw wire mode hash differently from bincode-typed methods of\nthe same Rust signature. This is the protection that makes a\nwire-mode mismatch surface as a load-time hash mismatch instead of\nsilent data corruption.\n- `streaming = true` appends a trailing `!stream` marker (after any\n`!raw`) so a server-streaming method (`-> fidius::Stream<T>`, where\n`ret` is the per-item type `T`) hashes differently from a unary\n`-> T` method of the same name/args. Same protection as `!raw`, for\nthe streaming/unary split (FIDIUS-I-0026, D4).\n\nThis function lives in `fidius-core` (not `fidius-macro`) so the proc\nmacro and downstream tooling like `fidius python-stub` share a single\nsource of truth for the format. Drift between them = silent hash\nmismatch, which is exactly what the load-time check is meant to catch\n— but better to never have the drift in the first place.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-guest/src/hash.rs","start_line":100,"end_line":148,"signature":null,"qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"},{"name":"empty_input","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":104,"end_line":108,"signature":"()","qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"},{"name":"known_vector","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":111,"end_line":117,"signature":"()","qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"},{"name":"order_independence","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":120,"end_line":130,"signature":"()","qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"},{"name":"sensitivity","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":133,"end_line":137,"signature":"()","qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"},{"name":"different_signatures_differ","kind":"function","file_path":"crates/fidius-guest/src/hash.rs","start_line":140,"end_line":147,"signature":"()","qualified_name":null,"doc_comment":"plugins compiled against a different interface.\nsignatures of a trait. The host checks this hash at load time to reject\nThe proc macro computes an `interface_hash` from the sorted required method\n\nFNV-1a interface hashing for compile-time ABI drift detection.","visibility":"private"}],"crates/fidius-guest/src/http.rs":[{"name":"bindings","kind":"module","file_path":"crates/fidius-guest/src/http.rs","start_line":40,"end_line":46,"signature":null,"qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"Request","kind":"struct","file_path":"crates/fidius-guest/src/http.rs","start_line":57,"end_line":66,"signature":"{ method: String, url: String, headers: Vec<(String, String)>, body: Vec<u8> }","qualified_name":null,"doc_comment":"An outbound request. Build with [`Request::get`]/[`Request::post`] or the\nstruct literal, then [`send`].","visibility":"public"},{"name":"Request","kind":"type","file_path":"crates/fidius-guest/src/http.rs","start_line":68,"end_line":94,"signature":"= Request","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"get","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":70,"end_line":77,"signature":"(url: impl Into<String>) -> Self","qualified_name":null,"doc_comment":"A GET request for `url`.","visibility":"public"},{"name":"post","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":80,"end_line":87,"signature":"(url: impl Into<String>, body: impl Into<Vec<u8>>) -> Self","qualified_name":null,"doc_comment":"A POST request for `url` with `body`.","visibility":"public"},{"name":"header","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":90,"end_line":93,"signature":"(mut self, name: impl Into<String>, value: impl Into<String>) -> Self","qualified_name":null,"doc_comment":"Add a header (builder style).","visibility":"public"},{"name":"Response","kind":"struct","file_path":"crates/fidius-guest/src/http.rs","start_line":98,"end_line":105,"signature":"{ status: u16, headers: Vec<(String, String)>, body: Vec<u8> }","qualified_name":null,"doc_comment":"A response. `body` is the fully-read response body.","visibility":"public"},{"name":"Response","kind":"type","file_path":"crates/fidius-guest/src/http.rs","start_line":107,"end_line":117,"signature":"= Response","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"is_success","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":109,"end_line":111,"signature":"(&self) -> bool","qualified_name":null,"doc_comment":"`true` for a 2xx status.","visibility":"public"},{"name":"text","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":114,"end_line":116,"signature":"(&self) -> String","qualified_name":null,"doc_comment":"The body as UTF-8 (lossy).","visibility":"public"},{"name":"HttpError","kind":"struct","file_path":"crates/fidius-guest/src/http.rs","start_line":123,"end_line":126,"signature":"{ message: String }","qualified_name":null,"doc_comment":"A failed request. The most common cause in a sandbox is a **denied egress**:\nthe host's `EgressPolicy` refused the request, surfaced here as a transport\nerror (the guest never learns the policy's reasoning — by design).","visibility":"public"},{"name":"HttpError","kind":"type","file_path":"crates/fidius-guest/src/http.rs","start_line":128,"end_line":134,"signature":"= HttpError","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":129,"end_line":133,"signature":"(msg: impl Into<String>) -> Self","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"HttpError","kind":"type","file_path":"crates/fidius-guest/src/http.rs","start_line":136,"end_line":140,"signature":"= HttpError","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":137,"end_line":139,"signature":"(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"HttpError","kind":"type","file_path":"crates/fidius-guest/src/http.rs","start_line":142,"end_line":142,"signature":"= HttpError","qualified_name":null,"doc_comment":"```\n}\n// … parse resp.body into records …\nlet resp = fidius::http::get(&cfg.url).expect(\"fetch\");\nfn read(&self, cfg: Config) -> fidius::Stream<Record> {\n```ignore\n\n`wasmtime-wasi-http`); see [[FIDIUS-A-0005]] for the stability contract.\npinned (vendored WIT at `crates/fidius-guest/wit`, matched to the host's\nbuilt for `wasm32-wasip2`. It is **the one place** the `wasi:http` version is\nThis module is `#[cfg(target_family = \"wasm\")]` and exists only in components\n\n`http` capability **and** the host supplied a policy (the two-key gate).\nno ambient network: the request only succeeds when the package declares the\ndecides allow/deny/decorate before the request leaves the process. There is\nsandbox via `wasi:http/outgoing-handler`, where the **host's `EgressPolicy`**\nA connector's `read()` calls [`get`]/[`post`]/[`send`]; the call crosses the\n\nconnectors (FIDIUS-I-0028).\n`fidius::http` — the brokered outbound HTTP client for sandboxed WASM","visibility":"private"},{"name":"get","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":145,"end_line":147,"signature":"(url: &str) -> Result<Response, HttpError>","qualified_name":null,"doc_comment":"GET `url`.","visibility":"public"},{"name":"post","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":150,"end_line":152,"signature":"(url: &str, body: &[u8]) -> Result<Response, HttpError>","qualified_name":null,"doc_comment":"POST `body` to `url`.","visibility":"public"},{"name":"send","kind":"function","file_path":"crates/fidius-guest/src/http.rs","start_line":156,"end_line":252,"signature":"(req: Request) -> Result<Response, HttpError>","qualified_name":null,"doc_comment":"Send an arbitrary [`Request`], blocking until the response is read. The host\n`EgressPolicy` is consulted before dispatch.","visibility":"public"}],"crates/fidius-guest/src/lib.rs":[{"name":"descriptor","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":32,"end_line":32,"signature":null,"qualified_name":null,"doc_comment":"`fidius-guest` — the wasm-buildable subset of the Fidius shared types.\n\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\nRust WASM components built from `#[plugin_impl]` — can depend on the\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nbincode wire format **without** dragging in the host-only packaging stack\n(archive/compression/filesystem). This crate has no such dependencies and\ncompiles cleanly to `wasm32-wasip2`.\n\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\nre-exports) resolve unchanged — this split is internal, with no public-API\nchurn.\n\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.","visibility":"public"},{"name":"error","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":33,"end_line":33,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"frame","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":34,"end_line":34,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"hash","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":35,"end_line":35,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"http","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":39,"end_line":39,"signature":null,"qualified_name":null,"doc_comment":"Brokered outbound HTTP for sandboxed WASM connectors (FIDIUS-I-0028).\nOnly present in components built for `wasm32-wasip2`.","visibility":"public"},{"name":"python_descriptor","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":40,"end_line":40,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"status","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":41,"end_line":41,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"stream_ffi","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":42,"end_line":42,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"stream_marker","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":43,"end_line":43,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"value","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":44,"end_line":44,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"wasm_descriptor","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":45,"end_line":45,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"},{"name":"wire","kind":"module","file_path":"crates/fidius-guest/src/lib.rs","start_line":46,"end_line":46,"signature":null,"qualified_name":null,"doc_comment":"(per ADR-0002), so `fidius-guest` is versioned in lockstep with `fidius-core`.\nNote: `descriptor::ABI_VERSION` derives from this crate's `CARGO_PKG_VERSION`\n\nchurn.\nre-exports) resolve unchanged — this split is internal, with no public-API\n`fidius_core::hash`, `fidius_core::value`, … (and the `fidius` facade\n`fidius-core` re-exports every module here, so `fidius_core::descriptor`,\n\ncompiles cleanly to `wasm32-wasip2`.\n(archive/compression/filesystem). This crate has no such dependencies and\nbincode wire format **without** dragging in the host-only packaging stack\ninterface hashing, descriptors, the neutral [`value::Value`] model, and the\nRust WASM components built from `#[plugin_impl]` — can depend on the\nExtracted from `fidius-core` (FIDIUS-I-0022) so plugin guests — including\n\n`fidius-guest` — the wasm-buildable subset of the Fidius shared types.","visibility":"public"}],"crates/fidius-guest/src/python_descriptor.rs":[{"name":"PythonInterfaceDescriptor","kind":"struct","file_path":"crates/fidius-guest/src/python_descriptor.rs","start_line":31,"end_line":42,"signature":"{ interface_name: &'static str, interface_hash: u64, methods: &'static [PythonMethodDesc] }","qualified_name":null,"doc_comment":"Static descriptor for one fidius interface, consumed by the Python\nloader to validate and dispatch into a Python plugin.","visibility":"public"},{"name":"PythonMethodDesc","kind":"struct","file_path":"crates/fidius-guest/src/python_descriptor.rs","start_line":46,"end_line":54,"signature":"{ name: &'static str, wire_raw: bool }","qualified_name":null,"doc_comment":"One method on the interface.","visibility":"public"}],"crates/fidius-guest/src/status.rs":[{"name":"STATUS_OK","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":22,"end_line":22,"signature":": i32","qualified_name":null,"doc_comment":"Method executed successfully. Output buffer contains the serialized result.","visibility":"public"},{"name":"STATUS_BUFFER_TOO_SMALL","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":26,"end_line":26,"signature":": i32","qualified_name":null,"doc_comment":"Output buffer was too small (CallerAllocated/Arena strategies only).\nThe `out_len` parameter contains the required size. Retry with a larger buffer.","visibility":"public"},{"name":"STATUS_SERIALIZATION_ERROR","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":30,"end_line":30,"signature":": i32","qualified_name":null,"doc_comment":"Serialization or deserialization failed at the FFI boundary.\nThis indicates a bug in the generated shims or a type mismatch.","visibility":"public"},{"name":"STATUS_PLUGIN_ERROR","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":34,"end_line":34,"signature":": i32","qualified_name":null,"doc_comment":"The plugin method returned an error. The output buffer contains a\nserialized `PluginError` with details.","visibility":"public"},{"name":"STATUS_PANIC","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":38,"end_line":38,"signature":": i32","qualified_name":null,"doc_comment":"A panic was caught at the `extern \"C\"` boundary via `catch_unwind`.\nThe output buffer may contain a panic message string, but this is not guaranteed.","visibility":"public"},{"name":"STATUS_STREAM_END","kind":"variable","file_path":"crates/fidius-guest/src/status.rs","start_line":43,"end_line":43,"signature":": i32","qualified_name":null,"doc_comment":"Clean end of a server-stream: the streaming `next()` shim has no more items\n(FIDIUS-I-0026 / FIDIUS-T-0138). Distinct from `STATUS_OK` with `out_len == 0`,\nwhich is a real zero-byte item (e.g. a unit `()` item).","visibility":"public"}],"crates/fidius-guest/src/stream_ffi.rs":[{"name":"FidiusStreamHandle","kind":"struct","file_path":"crates/fidius-guest/src/stream_ffi.rs","start_line":50,"end_line":59,"signature":"{ next: unsafe extern \"C\" fn(*mut FidiusStreamHandle, *mut u8, u32, *mut u32) -> i32, drop_fn: unsafe extern \"C\" fn(*mut...","qualified_name":null,"doc_comment":"Per-stream handle returned by a cdylib streaming method's init shim. See the\nmodule docs for the pull protocol. `#[repr(C)]` so the guest (which builds it)\nand the host (which drives it) agree on the layout across the FFI boundary.","visibility":"public"},{"name":"NextStatus","kind":"enum","file_path":"crates/fidius-guest/src/stream_ffi.rs","start_line":63,"end_line":73,"signature":"Item | End | TooSmall | SerErr","qualified_name":null,"doc_comment":"Outcome of [`StreamState::next_into`] — mapped to FFI status codes by the\nmacro-generated `next` shim.","visibility":"public"},{"name":"StreamState","kind":"struct","file_path":"crates/fidius-guest/src/stream_ffi.rs","start_line":81,"end_line":85,"signature":"{ stream: crate::stream_marker::Stream<T>, pending: Option<T> }","qualified_name":null,"doc_comment":"Guest-side driver for an arena-style cdylib stream (FIDIUS-T-0138). Wraps the\nproducer [`Stream<T>`](crate::stream_marker::Stream) and holds the *current\nitem value* (not its bytes), so a `BUFFER_TOO_SMALL` retry re-serializes the\nsame item rather than advancing the iterator (which would drop it) — and so\nthe happy path serializes **directly into the host buffer with no `Vec`\nallocation**.","visibility":"public"},{"name":"new","kind":"function","file_path":"crates/fidius-guest/src/stream_ffi.rs","start_line":89,"end_line":94,"signature":"(stream: crate::stream_marker::Stream<T>) -> Self","qualified_name":null,"doc_comment":"Wrap a producer stream.","visibility":"public"},{"name":"next_into","kind":"function","file_path":"crates/fidius-guest/src/stream_ffi.rs","start_line":99,"end_line":120,"signature":"(&mut self, buf: &mut [u8]) -> NextStatus","qualified_name":null,"doc_comment":"Pull the next item (if needed) and serialize it **directly into `buf`** —\nno intermediate allocation. On `TooSmall` the item value is retained and\nre-serialized on the next (larger-buffer) call, so nothing is lost.","visibility":"public"}],"crates/fidius-guest/src/stream_marker.rs":[{"name":"Stream","kind":"struct","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":61,"end_line":65,"signature":"{ iter: Option<Box<dyn Iterator<Item = T> + Send>>, _marker: PhantomData<fn() -> T> }","qualified_name":null,"doc_comment":"Marker type a plugin interface uses to declare a **server-streaming** method:\n\n```ignore\n#[fidius::plugin_interface(version = 1, buffer = PluginAllocated)]\npub trait Source: Send + Sync {\nfn read(&self, config: String) -> fidius::Stream<Row>;\n}\n```\n\n`#[plugin_interface]` recognises a return type whose final path segment is\n`Stream<T>` and, for that method:\n\n1. folds a `!stream` marker into the interface hash (so a streaming method\ncan never be confused with a unary `-> T` method of the same name/args —\na producer/consumer mismatch is rejected at load), and\n2. (Phase 1+) generates a host-side client method returning the runtime\npull handle (`fidius_host::ChunkStream`).\n\nThe marker carries no data — the *runtime* representation of a stream is the\nhost-side `ChunkStream`, not this type. It exists so the interface trait\ntype-checks and so the macro has an explicit, unambiguous thing to detect\n(rather than guessing from `impl Stream`). `T` is the per-item type and\nfollows the same wire/`WitType` rules as a unary return.\n\nArgument-position `Stream<T>` (client-streaming / bidirectional) is rejected\nin v1.\n\n## Two forms (FIDIUS-I-0026)\n\n- **Marker form** ([`Stream::new`]): no items. Used purely to *declare* a\nstreaming method in an interface trait — the Python path (Phase 1) never\niterates it in Rust (its generator is bridged to a `ChunkStream`\nhost-side).\n- **Iterator-backed form** ([`Stream::from_iter`]): a Rust **WASM** guest\n(Phase 2) returns real items. The macro-generated component resource pulls\nthem one at a time via [`Stream::next_item`] to satisfy the WIT contract:\n```wit\nresource <m>-stream { next: func() -> result<option<T>, plugin-error>; }\n<m>: func(args) -> own<<m>-stream>;\n```\n`some(item)` = item, `none` = clean end, `err` = mid-stream error;\ndropping the resource runs the guest dtor = cancel (design decision D3).","visibility":"public"},{"name":"new","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":70,"end_line":75,"signature":"() -> Self","qualified_name":null,"doc_comment":"The marker form — declares a streaming method without producing items.\nThe interface/Python path uses this; it is never iterated in Rust.","visibility":"public"},{"name":"from_iter","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":81,"end_line":90,"signature":"(items: I) -> Self","qualified_name":null,"doc_comment":"Build a stream from any iterator — how a Rust WASM guest produces its\nitems. The iterator must be `Send + 'static` so the host can drive the\ncomponent resource across its pump thread.","visibility":"public"},{"name":"next_item","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":95,"end_line":97,"signature":"(&mut self) -> Option<T>","qualified_name":null,"doc_comment":"Advance the underlying iterator by one item. Driven by the\nmacro-generated component resource's `next()` (WS.2). Returns `None` for\nthe marker form and at end of iteration.","visibility":"public"},{"name":"default","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":101,"end_line":103,"signature":"() -> Self","qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":107,"end_line":142,"signature":null,"qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"},{"name":"from_iter_yields_then_none","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":111,"end_line":118,"signature":"()","qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"},{"name":"from_iter_accepts_a_range","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":121,"end_line":125,"signature":"()","qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"},{"name":"marker_form_is_empty","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":128,"end_line":133,"signature":"()","qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"},{"name":"collect","kind":"function","file_path":"crates/fidius-guest/src/stream_marker.rs","start_line":135,"end_line":141,"signature":"(mut s: Stream<T>) -> Vec<T>","qualified_name":null,"doc_comment":"The `fidius::Stream<T>` server-streaming return marker (FIDIUS-I-0026, D4).","visibility":"private"}],"crates/fidius-guest/src/value.rs":[{"name":"Value","kind":"enum","file_path":"crates/fidius-guest/src/value.rs","start_line":48,"end_line":97,"signature":"Bool | S8 | S16 | S32 | S64 | U8 | U16 | U32 | U64 | F32 | F64 | Char | String | Bytes | Option | List | Record | Map | ...","qualified_name":null,"doc_comment":"A self-describing value crossing the plugin-call boundary.\n\nConstruct one from any `Serialize` type with [`to_value`] and read it back\ninto any `DeserializeOwned` type with [`from_value`].","visibility":"public"},{"name":"ValueError","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":102,"end_line":102,"signature":null,"qualified_name":null,"doc_comment":"Error produced while converting to or from [`Value`].","visibility":"public"},{"name":"ValueError","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":104,"end_line":108,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"custom","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":105,"end_line":107,"signature":"(msg: T) -> Self","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"ValueError","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":110,"end_line":114,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"custom","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":111,"end_line":113,"signature":"(msg: T) -> Self","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"to_value","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":117,"end_line":119,"signature":"(value: &T) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"Convert any [`Serialize`] type into a [`Value`].","visibility":"public"},{"name":"from_value","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":122,"end_line":127,"signature":"(value: Value) -> Result<T, ValueError>","qualified_name":null,"doc_comment":"Convert a [`Value`] into any [`Deserialize`] type.","visibility":"public"},{"name":"ValueSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":133,"end_line":133,"signature":null,"qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"ValueSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":135,"end_line":296,"signature":"= ValueSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":136,"end_line":136,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":137,"end_line":137,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeSeq","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":139,"end_line":139,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeTuple","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":140,"end_line":140,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeTupleStruct","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":141,"end_line":141,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeTupleVariant","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":142,"end_line":142,"signature":"= TupleVariantSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeMap","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":143,"end_line":143,"signature":"= MapSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeStruct","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":144,"end_line":144,"signature":"= StructSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SerializeStructVariant","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":145,"end_line":145,"signature":"= StructVariantSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_bool","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":147,"end_line":149,"signature":"(self, v: bool) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_i8","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":150,"end_line":152,"signature":"(self, v: i8) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_i16","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":153,"end_line":155,"signature":"(self, v: i16) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_i32","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":156,"end_line":158,"signature":"(self, v: i32) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_i64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":159,"end_line":161,"signature":"(self, v: i64) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_u8","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":162,"end_line":164,"signature":"(self, v: u8) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_u16","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":165,"end_line":167,"signature":"(self, v: u16) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_u32","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":168,"end_line":170,"signature":"(self, v: u32) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_u64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":171,"end_line":173,"signature":"(self, v: u64) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_f32","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":174,"end_line":176,"signature":"(self, v: f32) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_f64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":177,"end_line":179,"signature":"(self, v: f64) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_char","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":180,"end_line":182,"signature":"(self, v: char) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_str","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":183,"end_line":185,"signature":"(self, v: &str) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_bytes","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":186,"end_line":188,"signature":"(self, v: &[u8]) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_none","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":189,"end_line":191,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_some","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":192,"end_line":199,"signature":"(self, value: &T) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_unit","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":200,"end_line":202,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_unit_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":203,"end_line":205,"signature":"(self, _name: &'static str) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_unit_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":206,"end_line":216,"signature":"(\n        self,\n        _name: &'static str,\n        _variant_index: u32,\n        variant: &'static str,\n    ) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_newtype_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":217,"end_line":226,"signature":"(\n        self,\n        _name: &'static str,\n        value: &T,\n    ) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_newtype_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":227,"end_line":241,"signature":"(\n        self,\n        _name: &'static str,\n        _variant_index: u32,\n        variant: &'static str,\n        value: &T,\n    ) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_seq","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":242,"end_line":246,"signature":"(self, len: Option<usize>) -> Result<SeqSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_tuple","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":247,"end_line":249,"signature":"(self, len: usize) -> Result<SeqSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_tuple_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":250,"end_line":256,"signature":"(\n        self,\n        _name: &'static str,\n        len: usize,\n    ) -> Result<SeqSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_tuple_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":257,"end_line":268,"signature":"(\n        self,\n        _name: &'static str,\n        _variant_index: u32,\n        variant: &'static str,\n        len: usize,\n    ) -> Result<TupleVariantSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_map","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":269,"end_line":274,"signature":"(self, _len: Option<usize>) -> Result<MapSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":275,"end_line":283,"signature":"(\n        self,\n        _name: &'static str,\n        len: usize,\n    ) -> Result<StructSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_struct_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":284,"end_line":295,"signature":"(\n        self,\n        _name: &'static str,\n        _variant_index: u32,\n        variant: &'static str,\n        len: usize,\n    ) -> Result<StructVariantSerializer, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":298,"end_line":300,"signature":"{ items: Vec<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":301,"end_line":314,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":302,"end_line":302,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":303,"end_line":303,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_element","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":304,"end_line":310,"signature":"(&mut self, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":311,"end_line":313,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":315,"end_line":327,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":316,"end_line":316,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":317,"end_line":317,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_element","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":318,"end_line":323,"signature":"(&mut self, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":324,"end_line":326,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":328,"end_line":340,"signature":"= SeqSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":329,"end_line":329,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":330,"end_line":330,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_field","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":331,"end_line":336,"signature":"(&mut self, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":337,"end_line":339,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"TupleVariantSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":342,"end_line":345,"signature":"{ name: String, items: Vec<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"TupleVariantSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":346,"end_line":362,"signature":"= TupleVariantSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":347,"end_line":347,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":348,"end_line":348,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_field","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":349,"end_line":355,"signature":"(&mut self, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":356,"end_line":361,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"MapSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":364,"end_line":367,"signature":"{ entries: Vec<(Value, Value)>, next_key: Option<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"MapSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":368,"end_line":410,"signature":"= MapSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":369,"end_line":369,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":370,"end_line":370,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_key","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":371,"end_line":377,"signature":"(&mut self, key: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_value","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":378,"end_line":388,"signature":"(&mut self, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":389,"end_line":409,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"StructSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":412,"end_line":414,"signature":"{ fields: Vec<(String, Value)> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"StructSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":415,"end_line":429,"signature":"= StructSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":416,"end_line":416,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":417,"end_line":417,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_field","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":418,"end_line":425,"signature":"(&mut self, key: &'static str, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":426,"end_line":428,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"StructVariantSerializer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":431,"end_line":434,"signature":"{ name: String, fields: Vec<(String, Value)> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"StructVariantSerializer","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":435,"end_line":452,"signature":"= StructVariantSerializer","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Ok","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":436,"end_line":436,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":437,"end_line":437,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize_field","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":438,"end_line":445,"signature":"(&mut self, key: &'static str, value: &T) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"end","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":446,"end_line":451,"signature":"(self) -> Result<Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Value","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":458,"end_line":578,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":459,"end_line":459,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_any","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":461,"end_line":499,"signature":"(self, visitor: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_option","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":501,"end_line":510,"signature":"(self, visitor: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_enum","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":512,"end_line":537,"signature":"(\n        self,\n        _name: &'static str,\n        _variants: &'static [&'static str],\n        visitor: V,\n    ) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_newtype_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":539,"end_line":548,"signature":"(\n        self,\n        _name: &'static str,\n        visitor: V,\n    ) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_unit_struct","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":550,"end_line":559,"signature":"(\n        self,\n        _name: &'static str,\n        visitor: V,\n    ) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize_unit","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":561,"end_line":571,"signature":"(self, visitor: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Value","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":580,"end_line":598,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"kind","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":581,"end_line":597,"signature":"(&self) -> &'static str","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":600,"end_line":602,"signature":"{ iter: std::vec::IntoIter<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SeqAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":603,"end_line":617,"signature":"= SeqAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":604,"end_line":604,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_element_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":605,"end_line":613,"signature":"(&mut self, seed: T) -> Result<Option<T::Value>, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"size_hint","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":614,"end_line":616,"signature":"(&self) -> Option<usize>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"RecordAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":619,"end_line":622,"signature":"{ iter: std::vec::IntoIter<(String, Value)>, value: Option<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"RecordAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":623,"end_line":650,"signature":"= RecordAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":624,"end_line":624,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_key_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":625,"end_line":636,"signature":"(&mut self, seed: K) -> Result<Option<K::Value>, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_value_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":637,"end_line":646,"signature":"(&mut self, seed: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"size_hint","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":647,"end_line":649,"signature":"(&self) -> Option<usize>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"MapAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":652,"end_line":655,"signature":"{ iter: std::vec::IntoIter<(Value, Value)>, value: Option<Value> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"MapAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":656,"end_line":683,"signature":"= MapAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":657,"end_line":657,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_key_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":658,"end_line":669,"signature":"(&mut self, seed: K) -> Result<Option<K::Value>, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_value_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":670,"end_line":679,"signature":"(&mut self, seed: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"size_hint","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":680,"end_line":682,"signature":"(&self) -> Option<usize>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"SingletonMapAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":687,"end_line":690,"signature":"{ key: Option<String>, value: Option<Value> }","qualified_name":null,"doc_comment":"Presents a `Value::Variant` as a single-entry map for `deserialize_any`\nconsumers (e.g. deserializing into `serde_json::Value`).","visibility":"private"},{"name":"SingletonMapAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":691,"end_line":712,"signature":"= SingletonMapAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":692,"end_line":692,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_key_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":693,"end_line":701,"signature":"(&mut self, seed: K) -> Result<Option<K::Value>, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"next_value_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":702,"end_line":711,"signature":"(&mut self, seed: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"EnumAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":714,"end_line":717,"signature":"{ name: String, value: Value }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"EnumAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":718,"end_line":728,"signature":"= EnumAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":719,"end_line":719,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Variant","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":720,"end_line":720,"signature":"= VariantAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"variant_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":721,"end_line":727,"signature":"(self, seed: V) -> Result<(V::Value, VariantAccess), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"VariantAccess","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":730,"end_line":732,"signature":"{ value: Value }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"VariantAccess","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":733,"end_line":783,"signature":"= VariantAccess","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Error","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":734,"end_line":734,"signature":"= ValueError","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"unit_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":735,"end_line":743,"signature":"(self) -> Result<(), ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"newtype_variant_seed","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":744,"end_line":749,"signature":"(self, seed: T) -> Result<T::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"tuple_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":750,"end_line":763,"signature":"(self, _len: usize, visitor: V) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"struct_variant","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":764,"end_line":782,"signature":"(\n        self,\n        _fields: &'static [&'static str],\n        visitor: V,\n    ) -> Result<V::Value, ValueError>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-guest/src/value.rs","start_line":786,"end_line":898,"signature":null,"qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"round_trip","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":790,"end_line":797,"signature":"(value: T)","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Greeting","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":800,"end_line":804,"signature":"{ name: String, times: u32, loud: bool }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Wrapper","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":807,"end_line":807,"signature":null,"qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Shape","kind":"enum","file_path":"crates/fidius-guest/src/value.rs","start_line":810,"end_line":815,"signature":"Unit | Newtype | Tuple | Struct","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"primitives","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":818,"end_line":828,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"collections","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":831,"end_line":837,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"structs_and_maps","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":840,"end_line":858,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"enums","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":861,"end_line":866,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"nested","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":869,"end_line":879,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Outer","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":871,"end_line":874,"signature":"{ shapes: Vec<Shape>, tag: Option<String> }","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"struct_shape_is_record","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":882,"end_line":897,"signature":"()","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Value","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":902,"end_line":957,"signature":"impl Serialize for Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"serialize","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":903,"end_line":956,"signature":"(&self, serializer: S) -> Result<S::Ok, S::Error>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Value","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":959,"end_line":1050,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"deserialize","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":960,"end_line":1049,"signature":"(deserializer: D) -> Result<Value, D::Error>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"ValueVisitor","kind":"struct","file_path":"crates/fidius-guest/src/value.rs","start_line":964,"end_line":964,"signature":null,"qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"ValueVisitor","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":965,"end_line":1047,"signature":"= ValueVisitor","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"Value","kind":"type","file_path":"crates/fidius-guest/src/value.rs","start_line":966,"end_line":966,"signature":"= Value","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"expecting","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":967,"end_line":969,"signature":"(&self, f: &mut fmt::Formatter) -> fmt::Result","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_bool","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":970,"end_line":972,"signature":"(self, v: bool) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_i64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":973,"end_line":975,"signature":"(self, v: i64) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_i128","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":976,"end_line":983,"signature":"(self, v: i128) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_u64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":984,"end_line":986,"signature":"(self, v: u64) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_u128","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":987,"end_line":994,"signature":"(self, v: u128) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_f64","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":995,"end_line":997,"signature":"(self, v: f64) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_char","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":998,"end_line":1000,"signature":"(self, v: char) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_str","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1001,"end_line":1003,"signature":"(self, v: &str) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_string","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1004,"end_line":1006,"signature":"(self, v: String) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_bytes","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1007,"end_line":1009,"signature":"(self, v: &[u8]) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_byte_buf","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1010,"end_line":1012,"signature":"(self, v: Vec<u8>) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_unit","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1013,"end_line":1015,"signature":"(self) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_none","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1016,"end_line":1018,"signature":"(self) -> Result<Value, E>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_some","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1019,"end_line":1026,"signature":"(self, deserializer: D) -> Result<Value, D::Error>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_seq","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1027,"end_line":1036,"signature":"(self, mut seq: A) -> Result<Value, A::Error>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"},{"name":"visit_map","kind":"function","file_path":"crates/fidius-guest/src/value.rs","start_line":1037,"end_line":1046,"signature":"(self, mut map: A) -> Result<Value, A::Error>","qualified_name":null,"doc_comment":"records, options, and variants.\n`f32`/`f64`, `char`, `string`, `list<u8>` as [`Value::Bytes`], lists,\nPhase-2 mapping is mechanical: distinct signed/unsigned integer widths,\nThe variant set mirrors the WebAssembly Component Model value space so the\n\nto `wasmtime::component::Val`; cdylib and Python never see wasmtime.\nfree of any backend dependency. Only the WASM executor (Phase 2) maps it\n**Layering rule:** `Value` lives in `fidius-core` and is deliberately\n\nexecutor.\n`I → Value` ([`to_value`]) then `Value → O` ([`from_value`]) around the\nThe host's generic `call_method<I, O>` stays caller-identical by going\n\n- **wasm**: `Value` → `wasmtime` `component::Val` (Canonical ABI) → `Value`\n- **python**: `Value` → `PyObject` → call → `PyObject` → `Value`\n- **cdylib**: `Value` → bincode → vtable FFI → bincode → `Value`\n\nnative representation:\n`fidius-host`). `Value` is the lingua franca each backend maps to its\nsingle `PluginExecutor::call(method, Value) -> Value` seam (see\nEvery fidius execution backend dispatches typed method calls through a\n\n`Value` — a neutral, self-describing value tree for the plugin boundary.","visibility":"private"}],"crates/fidius-guest/src/wasm_descriptor.rs":[{"name":"WasmInterfaceDescriptor","kind":"struct","file_path":"crates/fidius-guest/src/wasm_descriptor.rs","start_line":27,"end_line":40,"signature":"{ interface_name: &'static str, interface_export: &'static str, interface_hash: u64, methods: &'static [WasmMethodDesc] ...","qualified_name":null,"doc_comment":"Static descriptor for one fidius interface, consumed by the WASM loader to\nvalidate and dispatch into a component.","visibility":"public"},{"name":"WasmMethodDesc","kind":"struct","file_path":"crates/fidius-guest/src/wasm_descriptor.rs","start_line":44,"end_line":53,"signature":"{ name: &'static str, wire_raw: bool, streaming: bool }","qualified_name":null,"doc_comment":"One method on the interface.","visibility":"public"}],"crates/fidius-guest/src/wire.rs":[{"name":"WireError","kind":"enum","file_path":"crates/fidius-guest/src/wire.rs","start_line":28,"end_line":32,"signature":"Bincode","qualified_name":null,"doc_comment":"Errors that can occur during wire serialization or deserialization.","visibility":"public"},{"name":"serialize","kind":"function","file_path":"crates/fidius-guest/src/wire.rs","start_line":35,"end_line":37,"signature":"(val: &T) -> Result<Vec<u8>, WireError>","qualified_name":null,"doc_comment":"Serialize a value as bincode for transport across the FFI boundary.","visibility":"public"},{"name":"deserialize","kind":"function","file_path":"crates/fidius-guest/src/wire.rs","start_line":40,"end_line":42,"signature":"(bytes: &[u8]) -> Result<T, WireError>","qualified_name":null,"doc_comment":"Deserialize a value from bincode bytes received across the FFI boundary.","visibility":"public"},{"name":"serialized_size","kind":"function","file_path":"crates/fidius-guest/src/wire.rs","start_line":47,"end_line":49,"signature":"(val: &T) -> Result<u64, WireError>","qualified_name":null,"doc_comment":"The exact serialized size of `val` in bytes, without allocating. Matches\n[`serialize`]'s output length. Used to size/validate a buffer before\n[`serialize_into`] (e.g. the cdylib streaming arena path, FIDIUS-T-0138).","visibility":"public"},{"name":"serialize_into","kind":"function","file_path":"crates/fidius-guest/src/wire.rs","start_line":54,"end_line":56,"signature":"(buf: &mut [u8], val: &T) -> Result<(), WireError>","qualified_name":null,"doc_comment":"Serialize `val` directly into a caller-provided buffer — no intermediate\n`Vec` allocation. `buf` must be at least [`serialized_size`] bytes (bincode\nerrors otherwise). Byte-identical to [`serialize`].","visibility":"public"}],"crates/fidius-guest/tests/wasi_http_pin.rs":[{"name":"PINNED","kind":"variable","file_path":"crates/fidius-guest/tests/wasi_http_pin.rs","start_line":24,"end_line":24,"signature":": &str","qualified_name":null,"doc_comment":"Drift tripwire (FIDIUS-A-0005). `fidius_guest::http` vendors ONE `wasi:http`\nversion — the single pinned contract for the whole ecosystem, matched to the\nhost's `wasmtime-wasi-http`. This asserts the pin is what we think it is, so an\naccidental re-vendor is caught with a clear message. The macro-authored\nconnector E2E (`fidius-host/tests/macro_egress_e2e.rs`) is the *runtime* guard:\nif the guest's version ever diverges from the host's, that test fails to\ninstantiate. When you bump `wasmtime-wasi-http` in the host, re-vendor\n`crates/fidius-guest/wit/` and update `PINNED` here in the same change.","visibility":"private"},{"name":"vendored_wasi_http_version_is_pinned","kind":"function","file_path":"crates/fidius-guest/tests/wasi_http_pin.rs","start_line":27,"end_line":35,"signature":"()","qualified_name":null,"doc_comment":"`crates/fidius-guest/wit/` and update `PINNED` here in the same change.\ninstantiate. When you bump `wasmtime-wasi-http` in the host, re-vendor\nif the guest's version ever diverges from the host's, that test fails to\nconnector E2E (`fidius-host/tests/macro_egress_e2e.rs`) is the *runtime* guard:\naccidental re-vendor is caught with a clear message. The macro-authored\nhost's `wasmtime-wasi-http`. This asserts the pin is what we think it is, so an\nversion — the single pinned contract for the whole ecosystem, matched to the\nDrift tripwire (FIDIUS-A-0005). `fidius_guest::http` vendors ONE `wasi:http`","visibility":"private"}],"crates/fidius-host/benches/backends.rs":[{"name":"IFACE","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":45,"end_line":45,"signature":": &str","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"HASH","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":46,"end_line":46,"signature":": u64","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"METHODS","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":47,"end_line":68,"signature":": [WasmMethodDesc; 4]","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"GREETER","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":69,"end_line":74,"signature":": WasmInterfaceDescriptor","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"W_ADD","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":76,"end_line":76,"signature":": usize","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"W_ECHO","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":77,"end_line":77,"signature":": usize","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"T_IFACE","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":85,"end_line":85,"signature":": &str","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"T_HASH","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":87,"end_line":87,"signature":": u64","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"T_METHODS","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":89,"end_line":93,"signature":": [WasmMethodDesc; 1]","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"TICKER","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":95,"end_line":100,"signature":": WasmInterfaceDescriptor","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"C_ADD","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":102,"end_line":102,"signature":": usize","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"C_ECHO","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":103,"end_line":103,"signature":": usize","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"SIZES","kind":"variable","file_path":"crates/fidius-host/benches/backends.rs","start_line":105,"end_line":105,"signature":": &[usize]","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"greeter_component","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":107,"end_line":117,"signature":"() -> Vec<u8>","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"load_wasm","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":120,"end_line":144,"signature":"(host: &PluginHost, root: &std::path::Path, bytes: &[u8], aot: bool) -> PluginHandle","qualified_name":null,"doc_comment":"Stage a wasm package dir (optionally with a precompiled `.cwasm`) and load it.","visibility":"private"},{"name":"compute","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":148,"end_line":156,"signature":"(op_is_add: bool, body: &[u8]) -> Vec<u8>","qualified_name":null,"doc_comment":"The op a request asks the server to do. `add` sums two LE i64s; `echo`\nreverses the payload — the same work the plugin backends do.","visibility":"private"},{"name":"serve_lenprefix","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":161,"end_line":179,"signature":"(mut s: S)","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"lenprefix_call","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":181,"end_line":191,"signature":"(s: &mut S, op: u8, payload: &[u8]) -> Vec<u8>","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"spawn_tcp","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":193,"end_line":203,"signature":"() -> u16","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"spawn_uds","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":205,"end_line":212,"signature":"(path: PathBuf)","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"spawn_http","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":217,"end_line":261,"signature":"() -> u16","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"http_call","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":263,"end_line":287,"signature":"(s: &mut TcpStream, path: &str, payload: &[u8]) -> Vec<u8>","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"find_subslice","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":289,"end_line":291,"signature":"(hay: &[u8], needle: &[u8]) -> Option<usize>","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"content_length","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":293,"end_line":301,"signature":"(head: &str) -> usize","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"cdylib_handle","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":303,"end_line":305,"signature":"(host: &PluginHost, name: &str) -> PluginHandle","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"ticker_component","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":309,"end_line":319,"signature":"() -> Vec<u8>","qualified_name":null,"doc_comment":"Build the (hand-authored) ticker streaming component for the per-item bench.","visibility":"private"},{"name":"stage_load_wasm_ticker","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":326,"end_line":345,"signature":"(\n    host: &PluginHost,\n    root: &std::path::Path,\n    pkg: &str,\n    bytes: &[u8],\n) -> PluginHandle","qualified_name":null,"doc_comment":"Stage + load a ticker streaming **wasm** component (Rust or JS guest) as a\npackage named `pkg`, then `load_wasm` it against the shared `TICKER` descriptor\n(both guests are built from the same `ticker` WIT, so the same descriptor +\nhash applies).","visibility":"private"},{"name":"ticker_component_file","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":350,"end_line":352,"signature":"(rel: &str) -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"A committed polyglot ticker component (JS/Python/C), if built. `None` → that\ncomparison series is skipped (e.g. an env without that toolchain).","visibility":"private"},{"name":"stage_load_python_ticker","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":358,"end_line":381,"signature":"(host: &PluginHost, root: &std::path::Path) -> PluginHandle","qualified_name":null,"doc_comment":"Stage the py-ticker package (copy fixture + vendor the SDK + inject the macro\nhash) and `load_python` it against the `Ticker` Python descriptor — the same\nshape as the ST.5 E2E. Only built with the `python` feature.","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":384,"end_line":396,"signature":"(src: &std::path::Path, dst: &std::path::Path)","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"},{"name":"benches","kind":"function","file_path":"crates/fidius-host/benches/backends.rs","start_line":398,"end_line":580,"signature":"(c: &mut Criterion)","qualified_name":null,"doc_comment":"- `echo(bytes) -> bytes` at 64 B / 4 KiB / 256 KiB — payload marshalling/throughput.\n- `add(i64, i64) -> i64` — tiny payload; dominated by call/dispatch overhead.\nTwo ops, chosen so the same logical work runs on every backend:\n\nno per-call connect. A real microservice is strictly slower than this.\nHTTP/gRPC framing, no TLS, no serialization library, no cross-host network,\necho/add server. This is a *generous lower bound* for a microservice: no\n- **localhost TCP** — a persistent-connection round-trip to an in-process\n- **wasm (AOT)** — same, loaded from a precompiled `.cwasm`.\n- **wasm (JIT)** — Component Model in the wasmtime sandbox (JIT-compiled).\n- **cdylib** — native dynamic library, in-process FFI (bincode over the vtable).\nBackends:\n\npushback with numbers. Run: `cargo bench -p fidius-host --features wasm`.\n(FIDIUS-I-0024). Answers the \"plugins are too slow / costly vs microservices\"\nPer-call latency: fidius plugin backends vs a localhost network round-trip","visibility":"private"}],"crates/fidius-host/build.rs":[{"name":"main","kind":"function","file_path":"crates/fidius-host/build.rs","start_line":26,"end_line":45,"signature":"()","qualified_name":null,"doc_comment":"Build script: when the `python` feature is enabled, embed a runtime\nrpath so this crate's test binaries (and downstream binaries that\ndepend on `fidius-host` with `python`) can find libpython at process\nlaunch.\n\nMirrors `crates/fidius-python/build.rs`. Both build scripts must run\nbecause each one's outputs only apply to the linker invocation that\nconsumes its compilation unit — `fidius-python`'s build.rs sets rpath\nfor fidius-python's own test binaries, and fidius-host's needs to do\nthe same for fidius-host's test binaries.","visibility":"private"}],"crates/fidius-host/src/arch.rs":[{"name":"BinaryInfo","kind":"struct","file_path":"crates/fidius-host/src/arch.rs","start_line":26,"end_line":29,"signature":"{ format: BinaryFormat, arch: Arch }","qualified_name":null,"doc_comment":"Detected binary format and architecture.","visibility":"public"},{"name":"BinaryFormat","kind":"enum","file_path":"crates/fidius-host/src/arch.rs","start_line":32,"end_line":37,"signature":"Elf | MachO | Pe | Unknown","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"public"},{"name":"Arch","kind":"enum","file_path":"crates/fidius-host/src/arch.rs","start_line":40,"end_line":44,"signature":"X86_64 | Aarch64 | Unknown","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"public"},{"name":"BinaryFormat","kind":"type","file_path":"crates/fidius-host/src/arch.rs","start_line":46,"end_line":55,"signature":"= BinaryFormat","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":47,"end_line":54,"signature":"(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"Arch","kind":"type","file_path":"crates/fidius-host/src/arch.rs","start_line":57,"end_line":65,"signature":"= Arch","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":58,"end_line":64,"signature":"(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"detect_architecture","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":68,"end_line":147,"signature":"(path: &Path) -> Result<BinaryInfo, LoadError>","qualified_name":null,"doc_comment":"Detect the binary format and architecture of a file.","visibility":"public"},{"name":"check_architecture","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":150,"end_line":185,"signature":"(path: &Path) -> Result<(), LoadError>","qualified_name":null,"doc_comment":"Check that a dylib matches the current platform's expected format.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-host/src/arch.rs","start_line":188,"end_line":243,"signature":null,"qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"detects_elf","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":192,"end_line":204,"signature":"()","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"detects_macho_le","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":207,"end_line":219,"signature":"()","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"detects_pe","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":222,"end_line":231,"signature":"()","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"},{"name":"unknown_format","kind":"function","file_path":"crates/fidius-host/src/arch.rs","start_line":234,"end_line":242,"signature":"()","qualified_name":null,"doc_comment":"architecture before attempting to dlopen.\nReads binary headers to determine format (ELF/Mach-O/PE) and target\n\nArchitecture detection for dylib files.","visibility":"private"}],"crates/fidius-host/src/arena.rs":[{"name":"DEFAULT_ARENA_CAPACITY","kind":"variable","file_path":"crates/fidius-host/src/arena.rs","start_line":27,"end_line":27,"signature":": usize","qualified_name":null,"doc_comment":"Default initial arena capacity (4 KB) when the pool is empty and a\ncaller hasn't indicated a larger minimum. Most serialized outputs fit\ncomfortably within this; larger ones grow via the retry path.","visibility":"public"},{"name":"acquire_arena","kind":"function","file_path":"crates/fidius-host/src/arena.rs","start_line":40,"end_line":59,"signature":"(min_capacity: usize) -> Vec<u8>","qualified_name":null,"doc_comment":"Acquire an arena buffer with at least `min_capacity` bytes. Prefers to\nreuse a pooled buffer if one is available; otherwise allocates a fresh\n`Vec<u8>`. The returned buffer is filled with zero bytes up to its\nlength (which equals its capacity after this call) — callers write into\nit via raw pointers and track their own output length.","visibility":"public"},{"name":"release_arena","kind":"function","file_path":"crates/fidius-host/src/arena.rs","start_line":64,"end_line":66,"signature":"(buf: Vec<u8>)","qualified_name":null,"doc_comment":"Return an arena buffer to the pool for future reuse. The buffer's\ncapacity is retained; length is irrelevant (callers set length via\ndirect writes, not Vec::extend).","visibility":"public"},{"name":"grow_arena","kind":"function","file_path":"crates/fidius-host/src/arena.rs","start_line":70,"end_line":78,"signature":"(buf: &mut Vec<u8>, needed_capacity: usize)","qualified_name":null,"doc_comment":"Grow an in-flight arena buffer to hold at least `needed_capacity` bytes.\nUsed by the retry path when a plugin returns `STATUS_BUFFER_TOO_SMALL`.","visibility":"public"}],"crates/fidius-host/src/error.rs":[{"name":"LoadError","kind":"enum","file_path":"crates/fidius-host/src/error.rs","start_line":21,"end_line":83,"signature":"LibraryNotFound | SymbolNotFound | InvalidMagic | IncompatibleRegistryVersion | IncompatibleAbiVersion | InterfaceHashMi...","qualified_name":null,"doc_comment":"Errors that can occur when loading a plugin.","visibility":"public"},{"name":"CallError","kind":"enum","file_path":"crates/fidius-host/src/error.rs","start_line":87,"end_line":148,"signature":"Serialization | Deserialization | Plugin | Panic | BufferTooSmall | NotImplemented | InvalidMethodIndex | UnknownStatus ...","qualified_name":null,"doc_comment":"Errors that can occur when calling a plugin method.","visibility":"public"},{"name":"CallError","kind":"type","file_path":"crates/fidius-host/src/error.rs","start_line":158,"end_line":180,"signature":"= CallError","qualified_name":null,"doc_comment":"Fold the Python backend's call error into the unified [`CallError`].\n\n`fidius-python` deliberately does not depend on `fidius-host` (that would\ncycle — the host optionally depends on it), so the conversion lives here,\nbehind the `python` feature, where both types are visible. Plugin-raised\nPython exceptions map to [`CallError::Plugin`] with the traceback preserved\nin `PluginError.details` (built by `fidius_python::pyerr_to_plugin_error`).","visibility":"private"},{"name":"from","kind":"function","file_path":"crates/fidius-host/src/error.rs","start_line":159,"end_line":179,"signature":"(e: fidius_python::PythonCallError) -> Self","qualified_name":null,"doc_comment":"Error types for fidius-host plugin loading and calling.","visibility":"private"}],"crates/fidius-host/src/executor.rs":[{"name":"cdylib","kind":"module","file_path":"crates/fidius-host/src/executor.rs","start_line":42,"end_line":42,"signature":null,"qualified_name":null,"doc_comment":"`PluginExecutor` — the dispatch seam across execution backends.\n\nFidius historically carried one dispatch implementation per backend: the\ncdylib vtable/FFI path lived inside `PluginHandle`, and the Python (PyO3)\npath lived in a *separate* `PythonPluginHandle` in `fidius-python`. This\nmodule collapses that duplication: each backend is an executor, and the\ncaller-facing [`crate::handle::PluginHandle`] wraps them in an **enum**\n(`Backend`) so its generic `call_method<I, O>` can serialise with each\nbackend's native currency.\n\n## Why an enum, not `Box<dyn>` — and why two traits\n\nThe backends do **not** share a typed wire. cdylib decodes concrete-type\n**bincode** (not self-describing — it can't be reconstructed from an erased\nvalue), while Python (and the future WASM component backend) consume a\nself-describing [`fidius_core::Value`]. A single `call(method, Value)` trait\nmethod therefore cannot serve cdylib without breaking its ABI (see\nFIDIUS-A-0003 / FIDIUS-I-0021 amendment). So:\n\n- [`PluginExecutor`] is the **common** surface every backend shares:\nmetadata plus the raw byte path. For cdylib, `call_raw` is *also* the\ncarrier for typed calls (the wrapper bincode-wraps the concrete type).\n- [`ValueExecutor`] adds the typed [`fidius_core::Value`] call, implemented\nonly by the self-describing backends (Python, WASM). cdylib does not\nimplement it — `PluginHandle` routes cdylib typed calls through its own\nbincode `call_method`, keeping the bytes byte-identical to pre-refactor.","visibility":"public"},{"name":"python","kind":"module","file_path":"crates/fidius-host/src/executor.rs","start_line":44,"end_line":44,"signature":null,"qualified_name":null,"doc_comment":"bincode `call_method`, keeping the bytes byte-identical to pre-refactor.\nimplement it — `PluginHandle` routes cdylib typed calls through its own\nonly by the self-describing backends (Python, WASM). cdylib does not\n- [`ValueExecutor`] adds the typed [`fidius_core::Value`] call, implemented\ncarrier for typed calls (the wrapper bincode-wraps the concrete type).\nmetadata plus the raw byte path. For cdylib, `call_raw` is *also* the\n- [`PluginExecutor`] is the **common** surface every backend shares:\n\nFIDIUS-A-0003 / FIDIUS-I-0021 amendment). So:\nmethod therefore cannot serve cdylib without breaking its ABI (see\nself-describing [`fidius_core::Value`]. A single `call(method, Value)` trait\nvalue), while Python (and the future WASM component backend) consume a\n**bincode** (not self-describing — it can't be reconstructed from an erased\nThe backends do **not** share a typed wire. cdylib decodes concrete-type\n\n## Why an enum, not `Box<dyn>` — and why two traits\n\nbackend's native currency.\n(`Backend`) so its generic `call_method<I, O>` can serialise with each\ncaller-facing [`crate::handle::PluginHandle`] wraps them in an **enum**\nmodule collapses that duplication: each backend is an executor, and the\npath lived in a *separate* `PythonPluginHandle` in `fidius-python`. This\ncdylib vtable/FFI path lived inside `PluginHandle`, and the Python (PyO3)\nFidius historically carried one dispatch implementation per backend: the\n\n`PluginExecutor` — the dispatch seam across execution backends.","visibility":"public"},{"name":"wasm","kind":"module","file_path":"crates/fidius-host/src/executor.rs","start_line":46,"end_line":46,"signature":null,"qualified_name":null,"doc_comment":"bincode `call_method`, keeping the bytes byte-identical to pre-refactor.\nimplement it — `PluginHandle` routes cdylib typed calls through its own\nonly by the self-describing backends (Python, WASM). cdylib does not\n- [`ValueExecutor`] adds the typed [`fidius_core::Value`] call, implemented\ncarrier for typed calls (the wrapper bincode-wraps the concrete type).\nmetadata plus the raw byte path. For cdylib, `call_raw` is *also* the\n- [`PluginExecutor`] is the **common** surface every backend shares:\n\nFIDIUS-A-0003 / FIDIUS-I-0021 amendment). So:\nmethod therefore cannot serve cdylib without breaking its ABI (see\nself-describing [`fidius_core::Value`]. A single `call(method, Value)` trait\nvalue), while Python (and the future WASM component backend) consume a\n**bincode** (not self-describing — it can't be reconstructed from an erased\nThe backends do **not** share a typed wire. cdylib decodes concrete-type\n\n## Why an enum, not `Box<dyn>` — and why two traits\n\nbackend's native currency.\n(`Backend`) so its generic `call_method<I, O>` can serialise with each\ncaller-facing [`crate::handle::PluginHandle`] wraps them in an **enum**\nmodule collapses that duplication: each backend is an executor, and the\npath lived in a *separate* `PythonPluginHandle` in `fidius-python`. This\ncdylib vtable/FFI path lived inside `PluginHandle`, and the Python (PyO3)\nFidius historically carried one dispatch implementation per backend: the\n\n`PluginExecutor` — the dispatch seam across execution backends.","visibility":"public"},{"name":"PluginExecutor","kind":"interface","file_path":"crates/fidius-host/src/executor.rs","start_line":66,"end_line":77,"signature":"{ fn info(), fn method_count(), fn call_raw() }","qualified_name":null,"doc_comment":"The surface every execution backend shares.\n\nImplementations must be `Send + Sync`: methods take `&self`, so a handle can\nbe shared across threads as long as the backend is internally thread-safe.","visibility":"public"},{"name":"ValueExecutor","kind":"interface","file_path":"crates/fidius-host/src/executor.rs","start_line":85,"end_line":90,"signature":"{ fn call() }","qualified_name":null,"doc_comment":"Backends whose typed boundary is the self-describing [`Value`] model —\nPython today, and the Phase-2 WASM component executor.\n\ncdylib deliberately does **not** implement this: its typed path is\nconcrete-type bincode, which `Value` cannot reproduce. `PluginHandle`\ndispatches cdylib typed calls directly via bincode instead.","visibility":"public"}],"crates/fidius-host/src/executor/cdylib.rs":[{"name":"FfiFn","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":45,"end_line":45,"signature":"= unsafe extern \"C\" fn(*mut c_void, *const u8, u32, *mut *mut u8, *mut u32) -> i32","qualified_name":null,"doc_comment":"Type alias for the PluginAllocated FFI function pointer signature.\nFIDIUS-A-0006: every method takes the instance pointer first.","visibility":"private"},{"name":"ArenaFn","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":48,"end_line":49,"signature":"= unsafe extern \"C\" fn(*mut c_void, *const u8, u32, *mut u8, u32, *mut u32, *mut u32) -> i32","qualified_name":null,"doc_comment":"Type alias for the Arena FFI function pointer signature.","visibility":"private"},{"name":"construct_instance","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":56,"end_line":61,"signature":"(descriptor: *const PluginDescriptor, cfg: &[u8]) -> *mut c_void","qualified_name":null,"doc_comment":"Construct the plugin instance via the descriptor's `construct` (FIDIUS-A-0006).\nEmpty config bytes = the zero-config / singleton case (CI.1; typed config is CI.2).\n\n# Safety\n`descriptor` must point to a valid `PluginDescriptor`.","visibility":"private"},{"name":"CdylibExecutor","kind":"struct","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":71,"end_line":96,"signature":"{ _library: Option<Arc<Library>>, vtable: *const c_void, descriptor: *const PluginDescriptor, free_buffer: Option<unsafe...","qualified_name":null,"doc_comment":"A handle to a loaded plugin, ready for calling methods.\n\nHolds an `Arc<Library>` to keep the dylib loaded as long as any handle exists.\nCall methods via `call_method()` which handles serialization, FFI, and cleanup.\n\n`CdylibExecutor` is `Send + Sync`. Plugin methods take `&self` (enforced by\nthe macro), so concurrent calls from multiple threads are safe as long as\nthe plugin implementation is thread-safe internally.","visibility":"public"},{"name":"CdylibExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":106,"end_line":106,"signature":"impl Send for CdylibExecutor","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"CdylibExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":107,"end_line":107,"signature":"impl Sync for CdylibExecutor","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"CdylibExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":109,"end_line":118,"signature":"impl Drop for CdylibExecutor","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"drop","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":110,"end_line":117,"signature":"(&mut self)","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"CdylibExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":120,"end_line":870,"signature":"= CdylibExecutor","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":123,"end_line":145,"signature":"(\n        library: Arc<Library>,\n        vtable: *const c_void,\n        descriptor: *const PluginDescriptor,\n        free_buffer: Option<unsafe extern \"C\" fn(*mut u8, usize)>,\n        capabilities: u64,\n        method_count: u32,\n        info: PluginInfo,\n    ) -> Self","qualified_name":null,"doc_comment":"Create a new CdylibExecutor. Crate-private — use `from_loaded()` instead.","visibility":"crate"},{"name":"from_loaded","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":148,"end_line":162,"signature":"(plugin: crate::loader::LoadedPlugin) -> Self","qualified_name":null,"doc_comment":"Create a CdylibExecutor from a LoadedPlugin.","visibility":"public"},{"name":"from_descriptor","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":171,"end_line":173,"signature":"(desc: &'static PluginDescriptor) -> Result<Self, LoadError>","qualified_name":null,"doc_comment":"Create a CdylibExecutor from a plugin descriptor already registered in\nthe current process's inventory (via a `#[plugin_impl]` linked into\nthe current binary as a normal rlib). No dylib is loaded — the\ndescriptor's vtable points at code in the current binary.\n\nUsed by the generated `Client::in_process(plugin_name)` constructor.\nHost applications normally use [`CdylibExecutor::from_loaded`] instead.","visibility":"public"},{"name":"from_descriptor_with_config","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":178,"end_line":206,"signature":"(\n        desc: &'static PluginDescriptor,\n        cfg: &[u8],\n    ) -> Result<Self, LoadError>","qualified_name":null,"doc_comment":"Like [`Self::from_descriptor`] but constructs the instance from serialized\nconfig bytes (FIDIUS-A-0006 / CI.2) — the in-process *configured* path.\n`cfg` is bincode of the plugin's config type (empty = the singleton).","visibility":"public"},{"name":"find_in_process_descriptor","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":214,"end_line":228,"signature":"(\n        plugin_name: &str,\n    ) -> Result<&'static PluginDescriptor, LoadError>","qualified_name":null,"doc_comment":"Look up a descriptor in the current process's inventory registry by\n`plugin_name` (the Rust struct name that was passed to `#[plugin_impl]`).\nReturns `LoadError::PluginNotFound` if no descriptor has that name.\n\nThe returned reference has `'static` lifetime because descriptors\nemitted by `#[plugin_impl]` live in the binary's `.rodata`.","visibility":"public"},{"name":"call_method","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":246,"end_line":266,"signature":"(\n        &self,\n        index: usize,\n        input: &I,\n    ) -> Result<O, CallError>","qualified_name":null,"doc_comment":"Call a plugin method by vtable index.\n\nSerializes the input, calls the FFI function pointer at the given index,\nchecks the status code, deserializes the output, and frees the plugin-allocated buffer.\n\n# Arguments\n* `index` - The method index in the vtable (0-based, in declaration order)\n* `input` - The input argument to serialize and pass to the plugin\n\n# No timeout\n\nThis call runs synchronously on the calling thread and has no built-in\ntimeout or cancellation. A misbehaving plugin will block the caller\nindefinitely. See the `fidius` crate top-level docs (\"What fidius\ndoes not provide\") for the rationale and the recommended consumer-side\nmitigation.","visibility":"public"},{"name":"call_method_raw","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":277,"end_line":288,"signature":"(&self, index: usize, input: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"Call a plugin method whose argument and successful return value are\nraw bytes — bypassing bincode on both sides. Used by methods declared\nwith `#[wire(raw)]` on the interface trait.\n\nErrors and panic messages still use bincode (small typed payloads).\nReturns the success bytes on `Ok`, or a `CallError::Plugin(_)` whose\ninner `PluginError` was bincode-decoded from the plugin's error payload.\n\nSame no-timeout caveat as [`Self::call_method`].","visibility":"public"},{"name":"call_plugin_allocated","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":292,"end_line":371,"signature":"(\n        &self,\n        index: usize,\n        input_bytes: &[u8],\n    ) -> Result<O, CallError>","qualified_name":null,"doc_comment":"PluginAllocated path: plugin allocates an output buffer via\n`Box::into_raw(Box<[u8]>)`, host deserializes and calls free_buffer.","visibility":"private"},{"name":"call_arena","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":377,"end_line":464,"signature":"(\n        &self,\n        index: usize,\n        input_bytes: &[u8],\n    ) -> Result<O, CallError>","qualified_name":null,"doc_comment":"Arena path: host supplies a buffer from the thread-local pool. If the\nplugin reports `STATUS_BUFFER_TOO_SMALL`, grow the buffer to the\nrequested size and retry exactly once (second too-small would indicate\na misbehaving plugin — bail with `CallError::BufferTooSmall`).","visibility":"private"},{"name":"call_plugin_allocated_raw","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":469,"end_line":548,"signature":"(\n        &self,\n        index: usize,\n        input_bytes: &[u8],\n    ) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"PluginAllocated raw path — same FFI shape as `call_plugin_allocated`,\nbut the success buffer is returned to the caller as-is rather than\nfed to bincode.","visibility":"private"},{"name":"call_arena_raw","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":552,"end_line":630,"signature":"(&self, index: usize, input_bytes: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"Arena raw path — same FFI shape as `call_arena`, success bytes\nreturned as a `Vec<u8>` copied out of the arena.","visibility":"private"},{"name":"call_streaming_raw","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":647,"end_line":782,"signature":"(\n        &self,\n        index: usize,\n        input_bytes: &[u8],\n        decode_item: fn(&[u8]) -> Result<fidius_core::Value, CallError>,\n    ) -> Result<crate::stream::ChunkStream, CallError>","qualified_name":null,"doc_comment":"Start a server-streaming cdylib call (FIDIUS-I-0026 CS.1). `input_bytes`\nis the **concrete bincode** of the args tuple (the cdylib path never goes\nthrough `Value` — same as `call_method`), so the caller serialises with\n`wire::serialize` directly.\n\nCalls the streaming method's vtable slot (an *init* shim with the ordinary\n`FfiFn` shape) to obtain a `FidiusStreamHandle`, then pumps `next()` on a\ndedicated thread (cdylib is synchronous) into a bounded channel →\n`ChunkStream`. The pump owns **one reusable buffer** the guest writes each\nitem into (FIDIUS-T-0138 arena-style `next`) — so there's no per-item heap\nalloc and no `free_buffer` crossing, just one `next` call per item. Each\nitem crosses as **concrete bincode** and is turned into a `Value` by the\ncaller-supplied `decode_item` (`wire::deserialize::<O>` + `to_value`,\nFIDIUS-T-0137). Dropping the stream runs the guest's `drop_fn` (cancel).","visibility":"public"},{"name":"STREAM_CHANNEL_CAP","kind":"variable","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":658,"end_line":658,"signature":": usize","qualified_name":null,"doc_comment":"Bounded backpressure/memory window between the pump thread and the\nasync consumer (mirrors the Python/WASM bridges).","visibility":"private"},{"name":"SendHandle","kind":"struct","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":699,"end_line":699,"signature":null,"qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"SendHandle","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":700,"end_line":700,"signature":"impl Send for SendHandle","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"INITIAL_ITEM_CAP","kind":"variable","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":715,"end_line":715,"signature":": usize","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"has_capability","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":787,"end_line":792,"signature":"(&self, bit: u32) -> bool","qualified_name":null,"doc_comment":"Check if an optional method is supported (capability bit is set).\n\nReturns `false` for bit indices >= 64 rather than panicking.","visibility":"public"},{"name":"info","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":795,"end_line":797,"signature":"(&self) -> &PluginInfo","qualified_name":null,"doc_comment":"Access the plugin's owned metadata.","visibility":"public"},{"name":"method_metadata","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":810,"end_line":842,"signature":"(&self, method_id: u32) -> Vec<(&str, &str)>","qualified_name":null,"doc_comment":"Returns the static key/value metadata declared on the given method via\n`#[method_meta(...)]` attributes on the trait, in declaration order.\n\nReturns an empty `Vec` if:\n- `method_id >= method_count` (out of range)\n- the interface declared no method metadata on any method\n- this specific method has no metadata declared\n\nThe returned `&str` slices borrow from the loaded library's `.rodata`\n(for dylib-loaded handles) or from the current binary's `.rodata`\n(for in-process handles). The handle's lifetime bounds them safely.","visibility":"public"},{"name":"trait_metadata","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":848,"end_line":869,"signature":"(&self) -> Vec<(&str, &str)>","qualified_name":null,"doc_comment":"Returns the static key/value metadata declared on the trait via\n`#[trait_meta(...)]` attributes, in declaration order.\n\nReturns an empty `Vec` if no trait-level metadata was declared.","visibility":"public"},{"name":"CdylibExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":872,"end_line":888,"signature":"impl PluginExecutor for CdylibExecutor","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"info","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":873,"end_line":875,"signature":"(&self) -> &PluginInfo","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"method_count","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":877,"end_line":879,"signature":"(&self) -> u32","qualified_name":null,"doc_comment":"(and future WASM) backends.\n[`crate::handle::PluginHandle`] wraps this in an enum alongside the Python\nplugin decodes are byte-identical to pre-refactor. The public-facing\ntype with bincode **directly** — `Value` is never involved, so the bytes the\ngeneric `call_method<I, O>` so the cdylib typed path serializes the concrete\n[`crate::executor::PluginExecutor`] seam (FIDIUS-I-0021). It keeps its own\nThis is the original `PluginHandle` dispatch logic, moved behind the\n\nthe bincode wire format.\n`CdylibExecutor` — the cdylib execution backend: vtable/FFI dispatch with","visibility":"private"},{"name":"call_raw","kind":"function","file_path":"crates/fidius-host/src/executor/cdylib.rs","start_line":885,"end_line":887,"signature":"(&self, method: usize, input: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"Raw byte dispatch. This is also the carrier for the cdylib *typed* path:\n[`CdylibExecutor::call_method`] bincode-wraps the concrete type and the\n`PluginHandle` wrapper routes typed cdylib calls through `call_method`\ndirectly, so the bytes a plugin receives are unchanged from pre-refactor.","visibility":"private"}],"crates/fidius-host/src/executor/python.rs":[{"name":"Pyo3Executor","kind":"struct","file_path":"crates/fidius-host/src/executor/python.rs","start_line":39,"end_line":42,"signature":"{ py: PythonPluginHandle, info: PluginInfo }","qualified_name":null,"doc_comment":"Python-backed executor: an embedded-interpreter plugin handle plus the\nhost-facing [`PluginInfo`] (built from the package manifest + interface\ndescriptor at load time).","visibility":"public"},{"name":"Pyo3Executor","kind":"type","file_path":"crates/fidius-host/src/executor/python.rs","start_line":44,"end_line":49,"signature":"= Pyo3Executor","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":46,"end_line":48,"signature":"(py: PythonPluginHandle, info: PluginInfo) -> Self","qualified_name":null,"doc_comment":"Wrap a loaded `PythonPluginHandle` with its owned metadata.","visibility":"public"},{"name":"Pyo3Executor","kind":"type","file_path":"crates/fidius-host/src/executor/python.rs","start_line":51,"end_line":64,"signature":"impl PluginExecutor for Pyo3Executor","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"info","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":52,"end_line":54,"signature":"(&self) -> &PluginInfo","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"method_count","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":56,"end_line":58,"signature":"(&self) -> u32","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"call_raw","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":60,"end_line":63,"signature":"(&self, method: usize, input: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"Pyo3Executor","kind":"type","file_path":"crates/fidius-host/src/executor/python.rs","start_line":66,"end_line":79,"signature":"impl ValueExecutor for Pyo3Executor","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"call","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":67,"end_line":78,"signature":"(&self, method: usize, args: Value) -> Result<Value, CallError>","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"STREAM_CHANNEL_CAP","kind":"variable","file_path":"crates/fidius-host/src/executor/python.rs","start_line":86,"end_line":86,"signature":": usize","qualified_name":null,"doc_comment":"Bounded channel depth between the GIL-holding pump thread and the host's\nasync consumer. Small on purpose: it *is* the backpressure window (REQ-003)\nand the bound on buffered items (NFR-003) — the pump blocks once it's full,\nso a paused consumer parks the generator rather than draining it into memory.","visibility":"private"},{"name":"Pyo3Executor","kind":"type","file_path":"crates/fidius-host/src/executor/python.rs","start_line":90,"end_line":150,"signature":"= Pyo3Executor","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"},{"name":"call_streaming","kind":"function","file_path":"crates/fidius-host/src/executor/python.rs","start_line":91,"end_line":149,"signature":"(\n        &self,\n        method: usize,\n        args: Value,\n    ) -> Result<crate::stream::ChunkStream, CallError>","qualified_name":null,"doc_comment":"routed through the neutral `Value` currency.\npre-unification path (`serde_json::to_vec(input) → call_typed_json`), just\nthe Python `value_bridge` expects, so this is behaviour-identical to the\n`Value ↔ JSON` with `serde_json` — `Value` serialises to exactly the JSON\nspeaks self-describing JSON (`call_typed_json`), so the adapter bridges\nTyped calls cross as a self-describing [`Value`]. The Python layer already\n\n[`crate::handle::PluginHandle`] as cdylib plugins.\n[`crate::executor`] traits so Python plugins flow through the same\nthe embedded-interpreter dispatch) that adapts it to the\nThin host-side wrapper over `fidius_python::PythonPluginHandle` (which owns\n\n`Pyo3Executor` — the Python execution backend, behind the `python` feature.","visibility":"private"}],"crates/fidius-host/src/executor/wasm.rs":[{"name":"EgressDenied","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":54,"end_line":58,"signature":"{ reason: String }","qualified_name":null,"doc_comment":"Denial returned by an [`EgressPolicy`] to refuse an outbound request.","visibility":"public"},{"name":"EgressDenied","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":60,"end_line":67,"signature":"= EgressDenied","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":62,"end_line":66,"signature":"(reason: impl Into<String>) -> Self","qualified_name":null,"doc_comment":"A denial with a reason.","visibility":"public"},{"name":"EgressPolicy","kind":"interface","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":82,"end_line":85,"signature":"{ fn authorize() }","qualified_name":null,"doc_comment":"Embedder-supplied policy governing a sandboxed WASM guest's **outbound HTTP**\n(FIDIUS-I-0027). This is the *only* egress seam fidius ships — it contains\n**no** allow-list, SSRF, or credential logic; those are deployment-specific\npolicy the embedder implements here.\n\n`wasi:http` is enabled for a guest only when its package declares the `http`\ncapability **and** a `PluginHost`/executor was given one of these (two-key,\nfail-closed). [`authorize`](EgressPolicy::authorize) is then called for\n**every** outbound request the guest makes — every request is a host call\nacross the sandbox boundary, so this is a true per-request checkpoint, not a\none-time gate. Inspect `parts.uri` / `parts.method`, mutate `parts.headers`\nto inject credentials, or return `Err(EgressDenied)` to refuse (the guest\nthen sees an HTTP error and the request is never dispatched).","visibility":"public"},{"name":"EgressHooks","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":92,"end_line":94,"signature":"{ policy: Option<Arc<dyn EgressPolicy>> }","qualified_name":null,"doc_comment":"fidius's [`WasiHttpHooks`] adapter: routes every outbound request through the\nembedder's [`EgressPolicy`] before handing off to wasi-http's\n`default_send_request`. `policy: None` denies everything (defensive — the\nhttp imports are never linked without a policy, so this is unreachable in\npractice).","visibility":"private"},{"name":"EgressHooks","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":96,"end_line":116,"signature":"impl WasiHttpHooks for EgressHooks","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"send_request","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":97,"end_line":115,"signature":"(\n        &mut self,\n        request: http::Request<HyperOutgoingBody>,\n        config: OutgoingRequestConfig,\n    ) -> HttpResult<HostFutureIncomingResponse>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"HostState","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":121,"end_line":126,"signature":"{ ctx: WasiCtx, table: ResourceTable, http_ctx: WasiHttpCtx, hooks: EgressHooks }","qualified_name":null,"doc_comment":"Per-store host state. The `WasiCtx` is built from the capability allow-list\n(deny-all baseline) by `build_wasi_ctx`. `http_ctx`/`hooks` back the optional\n`wasi:http` egress (FIDIUS-I-0027); they're inert unless egress was enabled.","visibility":"private"},{"name":"HostState","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":128,"end_line":136,"signature":"impl WasiHttpView for HostState","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"http","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":129,"end_line":135,"signature":"(&mut self) -> WasiHttpCtxView<'_>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"KNOWN_CAPABILITIES","kind":"variable","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":142,"end_line":151,"signature":": &[&str]","qualified_name":null,"doc_comment":"Capabilities the host knows how to grant. **Filesystem is intentionally\nabsent** — it is never granted in v1 (no preopens, ever). `clocks`/`random`\nare always available in WASI and are accepted as no-ops so manifests can\ndeclare intent without error.","visibility":"private"},{"name":"validate_capabilities","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":155,"end_line":191,"signature":"(caps: &[String]) -> Result<(), CallError>","qualified_name":null,"doc_comment":"Reject unknown capability names early (at load) so a typo fails closed and\nloud rather than silently granting nothing.","visibility":"private"},{"name":"build_wasi_ctx","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":196,"end_line":244,"signature":"(caps: &[String]) -> WasiCtx","qualified_name":null,"doc_comment":"Build a `WasiCtx` from the allow-list. Starts deny-all (a fresh builder\ninherits nothing and has no preopens) and grants only what's listed.\nFilesystem is never granted.","visibility":"private"},{"name":"is_blocked_ip","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":251,"end_line":270,"signature":"(ip: &IpAddr) -> bool","qualified_name":null,"doc_comment":"Baseline SSRF denylist for the raw-socket grant (FIDIUS-T-0143): an address a\nsandboxed guest must never reach — loopback, link-local (incl. the cloud\nmetadata IP `169.254.169.254`), private (RFC-1918), unique-local, unspecified,\nor broadcast. This is a safety *floor* (like deny-all), not a full egress\npolicy; per-host policy is the embedder's job via the `http` capability.","visibility":"private"},{"name":"HOST_WASI_HTTP","kind":"variable","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":275,"end_line":275,"signature":": (u32, u32, u32)","qualified_name":null,"doc_comment":"The `wasi:http` version this host provides — matched to `wasmtime-wasi-http`\nand the vendored guest WIT (FIDIUS-A-0005). Bump together with a wasmtime\nupgrade; the `fidius-guest` pin tripwire + the macro-egress E2E guard the match.","visibility":"private"},{"name":"wasi_http_incompatibility","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":287,"end_line":317,"signature":"(import_names: impl Iterator<Item = &'a str>) -> Option<String>","qualified_name":null,"doc_comment":"Scan a component's import names for a `wasi:http` version this host can't\nsatisfy, returning a clear, actionable message if so (FIDIUS-A-0005, fail\nloud — the same discipline as the `ABI_VERSION` check, on a new axis).\n\nCompatible iff the import is on the host's `major.minor` line and the host's\npatch is `>=` the plugin's (WASI 0.2 is forward-compatible: a newer host\nsatisfies an older import, never the reverse). A host *behind* the plugin, or\na different line (`0.2`→`0.3`), is rejected up front instead of surfacing as a\ncryptic instantiate trap. Pulled out as a free fn so it unit-tests without a\nreal component.","visibility":"private"},{"name":"HostState","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":321,"end_line":328,"signature":"impl WasiView for HostState","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"ctx","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":322,"end_line":327,"signature":"(&mut self) -> WasiCtxView<'_>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"WasmMethod","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":332,"end_line":340,"signature":"{ name: String, wire_raw: bool, streaming: bool }","qualified_name":null,"doc_comment":"A method on the WASM interface, in declaration (vtable) order.","visibility":"public"},{"name":"WasmComponentExecutor","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":343,"end_line":372,"signature":"{ engine: Engine, instance_pre: InstancePre<HostState>, interface: String, methods: Vec<WasmMethod>, capabilities: Vec<S...","qualified_name":null,"doc_comment":"WASM component execution backend.","visibility":"public"},{"name":"ConfiguredStore","kind":"struct","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":375,"end_line":378,"signature":"{ store: Store<HostState>, instance: wasmtime::component::Instance }","qualified_name":null,"doc_comment":"A configured instance's persistent store + instance (FIDIUS-A-0006 / CI.3).","visibility":"private"},{"name":"WasmComponentExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":380,"end_line":669,"signature":"= WasmComponentExecutor","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"from_component_bytes","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":383,"end_line":391,"signature":"(\n        bytes: &[u8],\n        interface: String,\n        methods: Vec<WasmMethod>,\n        capabilities: Vec<String>,\n        info: PluginInfo,\n    ) -> Result<Self, CallError>","qualified_name":null,"doc_comment":"Build an executor from raw component bytes (a `.wasm` component). For the\nAOT fast path, prefer [`Self::from_cwasm`].","visibility":"public"},{"name":"from_component_bytes_with_egress","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":396,"end_line":419,"signature":"(\n        bytes: &[u8],\n        interface: String,\n        methods: Vec<WasmMethod>,\n        capabilities: Vec<String>,\n        egress: Option<Arc<dyn EgressPolicy>>,\n        info: PluginInfo,\n    ) -> Result<Self, CallError>","qualified_name":null,"doc_comment":"Like [`Self::from_component_bytes`] but with an embedder [`EgressPolicy`]\n(FIDIUS-I-0027). `wasi:http` outbound egress is linked only when the\npackage declares the `http` capability **and** `egress` is `Some`.","visibility":"public"},{"name":"from_cwasm","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":427,"end_line":435,"signature":"(\n        cwasm: &[u8],\n        interface: String,\n        methods: Vec<WasmMethod>,\n        capabilities: Vec<String>,\n        info: PluginInfo,\n    ) -> Result<Self, CallError>","qualified_name":null,"doc_comment":"Build from a precompiled `.cwasm` (engine/version-specific). ~83 µs load\nper the spike vs ~6.6 ms JIT.\n\n# Safety\nThe bytes must have been produced by `Engine::precompile_component` with a\ncompatible engine; wasmtime validates the header and refuses a mismatch.","visibility":"public"},{"name":"from_cwasm_with_egress","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":443,"end_line":466,"signature":"(\n        cwasm: &[u8],\n        interface: String,\n        methods: Vec<WasmMethod>,\n        capabilities: Vec<String>,\n        egress: Option<Arc<dyn EgressPolicy>>,\n        info: PluginInfo,\n    ) -> Result<Self, CallError>","qualified_name":null,"doc_comment":"Like [`Self::from_cwasm`] but with an embedder [`EgressPolicy`]\n(FIDIUS-I-0027) — the AOT counterpart of\n[`Self::from_component_bytes_with_egress`].\n\n# Safety\nSame as [`Self::from_cwasm`].","visibility":"public"},{"name":"build","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":470,"end_line":528,"signature":"(\n        engine: Engine,\n        component: &Component,\n        interface: String,\n        methods: Vec<WasmMethod>,\n        capabilities: Vec<String>,\n        egress: Option<Arc<dyn EgressPolicy>>,\n        info: PluginInfo,\n    ) -> Result<Self, CallError>","qualified_name":null,"doc_comment":"Shared constructor: wire WASI into a `Linker` and pre-instantiate the\ncomponent **once**. The resulting `InstancePre` is reused for every call.","visibility":"private"},{"name":"configure","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":534,"end_line":558,"signature":"(&mut self, cfg: &[u8]) -> Result<(), CallError>","qualified_name":null,"doc_comment":"Bind config once (FIDIUS-A-0006 / CI.3): instantiate a *persistent* store,\ncall the guest's `fidius-configure` export with `cfg`, and retain the store\nso subsequent method calls dispatch on the configured instance. `cfg` is\nthe bincode of the plugin's config type (empty = the zero-config no-op).","visibility":"public"},{"name":"with_store","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":562,"end_line":577,"signature":"(\n        &self,\n        f: impl FnOnce(&mut Store<HostState>, &wasmtime::component::Instance) -> Result<R, CallError>,\n    ) -> Result<R, CallError>","qualified_name":null,"doc_comment":"Run `f` with a `(store, instance)`: the persistent configured store if\nconfigured (FIDIUS-A-0006 / CI.3), else a fresh per-call one (isolation).","visibility":"private"},{"name":"instantiate","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":582,"end_line":600,"signature":"(&self) -> Result<(Store<HostState>, wasmtime::component::Instance), CallError>","qualified_name":null,"doc_comment":"Instantiate a fresh sandboxed `Store` + component instance from the cached\n`InstancePre`. Per-call instantiation gives isolation; the linking cost is\nalready paid in `build` (FIDIUS-I-0024).","visibility":"private"},{"name":"func","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":603,"end_line":630,"signature":"(\n        &self,\n        store: &mut Store<HostState>,\n        instance: &wasmtime::component::Instance,\n        name: &str,\n    ) -> Result<wasmtime::component::Func, CallError>","qualified_name":null,"doc_comment":"Resolve an exported function within the plugin's interface by name.","visibility":"private"},{"name":"method","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":632,"end_line":648,"signature":"(&self, index: usize, want_raw: bool) -> Result<&WasmMethod, CallError>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"interface_hash","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":652,"end_line":668,"signature":"(&self) -> Result<u64, CallError>","qualified_name":null,"doc_comment":"Call the `fidius-interface-hash` export — the integrity check the loader\n(T-0103) runs against the expected interface hash.","visibility":"public"},{"name":"WasmComponentExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":671,"end_line":712,"signature":"impl PluginExecutor for WasmComponentExecutor","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"info","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":672,"end_line":674,"signature":"(&self) -> &PluginInfo","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"method_count","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":676,"end_line":678,"signature":"(&self) -> u32","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"call_raw","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":680,"end_line":711,"signature":"(&self, method: usize, input: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"WasmComponentExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":714,"end_line":747,"signature":"impl ValueExecutor for WasmComponentExecutor","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"call","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":715,"end_line":746,"signature":"(&self, method: usize, args: Value) -> Result<Value, CallError>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"STREAM_CHANNEL_CAP","kind":"variable","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":753,"end_line":753,"signature":": usize","qualified_name":null,"doc_comment":"Bounded channel depth between the wasmtime pump thread and the async\nconsumer — the backpressure/memory window (REQ-003/NFR-003), like the Python\nbackend's.","visibility":"private"},{"name":"WasmComponentExecutor","kind":"type","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":757,"end_line":884,"signature":"= WasmComponentExecutor","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"call_streaming","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":758,"end_line":883,"signature":"(\n        &self,\n        method: usize,\n        args: Value,\n    ) -> Result<crate::stream::ChunkStream, CallError>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"plugin_error_from_val","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":888,"end_line":914,"signature":"(payload: Option<&Val>) -> CallError","qualified_name":null,"doc_comment":"Map a `result::err` payload (expected: a record with `code`/`message`/\n`details`) into a `PluginError`.","visibility":"private"},{"name":"to_kebab","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":919,"end_line":934,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"fidius `Value` → wasmtime `Val`. Mirrors the Phase-1 serde bridge shapes.\nRust identifier (snake_case / PascalCase) → kebab-case, matching the WIT\nnaming the generator uses. `y_pos`→`y-pos`, `Circle`→`circle`.","visibility":"private"},{"name":"kebab_to_snake","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":937,"end_line":939,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"kebab-case → snake_case (WIT record field → serde struct field).","visibility":"private"},{"name":"kebab_to_pascal","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":942,"end_line":952,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"kebab-case → PascalCase (WIT variant case → serde enum variant).","visibility":"private"},{"name":"value_to_val","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":954,"end_line":997,"signature":"(v: &Value) -> Result<Val, CallError>","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"val_to_value","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1000,"end_line":1038,"signature":"(v: &Val) -> Value","qualified_name":null,"doc_comment":"wasmtime `Val` → fidius `Value` (structural; self-describing).","visibility":"private"},{"name":"validate_component","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1048,"end_line":1056,"signature":"(bytes: &[u8]) -> Result<(), CallError>","qualified_name":null,"doc_comment":"Validate that `bytes` is a well-formed WASM **component** (Component Model),\nnot a core module or a corrupt artifact. This is the pack-time gate;\ninterface-name + `fidius-interface-hash` conformance is enforced at load\n(`PluginHost::load_wasm`).","visibility":"public"},{"name":"precompile_component","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1062,"end_line":1070,"signature":"(bytes: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"Ahead-of-time compile a component into engine/version-specific `.cwasm`\nbytes (`Engine::precompile_component`). Written into the package at pack time\nand consumed by the AOT load path; a stale `.cwasm` is ignored at load (JIT\nfallback), so this is purely a load-latency optimization.","visibility":"public"},{"name":"ssrf_tests","kind":"module","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1073,"end_line":1111,"signature":null,"qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"ip","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1077,"end_line":1079,"signature":"(s: &str) -> IpAddr","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"blocks_internal_and_metadata_targets","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1082,"end_line":1098,"signature":"()","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"allows_public_targets","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1101,"end_line":1110,"signature":"()","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"wasi_http_version_tests","kind":"module","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1114,"end_line":1149,"signature":null,"qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"host_matched_version_is_compatible","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1118,"end_line":1124,"signature":"()","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"newer_minor_or_patch_is_rejected_with_a_clear_message","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1127,"end_line":1139,"signature":"()","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"},{"name":"no_wasi_http_import_is_fine","kind":"function","file_path":"crates/fidius-host/src/executor/wasm.rs","start_line":1142,"end_line":1148,"signature":"()","qualified_name":null,"doc_comment":"from the package manifest's allow-list.\nenv, no inherited stdio, no sockets). T-0104 opens specific capabilities\n`Linker` but give the guest a **zero-grant `WasiCtx`** (no FS preopens, no\n*empty* `Linker` can't instantiate them. We wire `wasmtime-wasi` into the\ncomponents import `wasi:cli/io/clocks/filesystem` even when unused, so an\nSandbox model (human-ratified, FIDIUS-T-0102 finding): real std-built\n\ncomponent's exported interface.\n`wasmtime::component::Val` and dispatches by method index into a loaded\ndepends on `wasmtime`; it maps the neutral [`fidius_core::Value`] to/from\nFIDIUS-I-0021 Phase 2, ADR FIDIUS-A-0003 (Path B). The **only** module that\n\n`WasmComponentExecutor` — the sandboxed WASM (Component Model) backend.","visibility":"private"}],"crates/fidius-host/src/handle.rs":[{"name":"Backend","kind":"enum","file_path":"crates/fidius-host/src/handle.rs","start_line":50,"end_line":60,"signature":"Cdylib | Python | Wasm","qualified_name":null,"doc_comment":"The execution backend behind a [`PluginHandle`].\n\nOne variant per runtime. The WASM variant lands in Phase 2.","visibility":"private"},{"name":"PluginHandle","kind":"struct","file_path":"crates/fidius-host/src/handle.rs","start_line":68,"end_line":70,"signature":"{ backend: Backend }","qualified_name":null,"doc_comment":"A handle to a loaded plugin, ready for calling methods.\n\nHolds the active execution backend. `call_method()` handles serialization,\ndispatch, and cleanup; concurrent calls from multiple threads are safe as\nlong as the underlying plugin is thread-safe (the cdylib macro enforces\n`&self`-only methods; the Python backend serialises through the GIL).","visibility":"public"},{"name":"PluginHandle","kind":"type","file_path":"crates/fidius-host/src/handle.rs","start_line":72,"end_line":265,"signature":"= PluginHandle","qualified_name":null,"doc_comment":"refactor (`bincode(input)` straight to the FFI; `Value` is never involved).\ncurrency — so the **cdylib path stays byte-identical** to before this\nthe concrete `I`/`O` in scope and serialise with each backend's native\n`Box<dyn PluginExecutor>`) lets the generic `call_method<I, O>` branch with\nconsume a self-describing [`fidius_core::Value`]. An enum (rather than\n**bincode** (not reconstructable from an erased value) while Python/WASM\nThe backends don't share a typed wire: cdylib decodes concrete-type\n\n## Why an enum backend (FIDIUS-I-0021)\n\nprivate [`Backend`] enum.\nPython package, or (Phase 2) a WASM component. The backend lives in the\n`call_method` / `call_method_raw` API whether the plugin is a cdylib, a\nA `PluginHandle` is backend-agnostic: callers use the same\n\n`PluginHandle` — the unified, caller-facing proxy over a loaded plugin.","visibility":"private"},{"name":"from_loaded","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":74,"end_line":78,"signature":"(plugin: crate::loader::LoadedPlugin) -> Self","qualified_name":null,"doc_comment":"Create a `PluginHandle` from a freshly loaded cdylib plugin.","visibility":"public"},{"name":"from_descriptor","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":83,"end_line":87,"signature":"(desc: &'static PluginDescriptor) -> Result<Self, LoadError>","qualified_name":null,"doc_comment":"Create a `PluginHandle` from a descriptor already registered in the\ncurrent process's inventory (a `#[plugin_impl]` linked as a normal\nrlib). No dylib is loaded. Used by `Client::in_process(plugin_name)`.","visibility":"public"},{"name":"configure_in_process","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":94,"end_line":103,"signature":"(\n        desc: &'static PluginDescriptor,\n        config: &C,\n    ) -> Result<Self, LoadError>","qualified_name":null,"doc_comment":"Construct a **configured** in-process plugin instance (FIDIUS-A-0006 /\nCI.2): serialize `config` and bind it once at construction. The plugin's\n`#[plugin_impl(Trait, config = C)]` `configure` constructor receives it;\nmethods then close over it without re-passing. The config crosses the\nboundary exactly once, and N differently-configured instances can coexist.","visibility":"public"},{"name":"find_in_process_descriptor","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":107,"end_line":111,"signature":"(\n        plugin_name: &str,\n    ) -> Result<&'static PluginDescriptor, LoadError>","qualified_name":null,"doc_comment":"Look up a descriptor in the current process's inventory registry by\n`plugin_name` (the Rust struct name passed to `#[plugin_impl]`).","visibility":"public"},{"name":"from_python","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":117,"end_line":121,"signature":"(py: fidius_python::PythonPluginHandle, info: PluginInfo) -> Self","qualified_name":null,"doc_comment":"Create a `PluginHandle` backed by a loaded Python plugin. `info` is\nbuilt by the loader from the package manifest + interface descriptor.\nOnly available with the `python` feature.","visibility":"public"},{"name":"from_wasm","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":126,"end_line":130,"signature":"(executor: WasmComponentExecutor) -> Self","qualified_name":null,"doc_comment":"Create a `PluginHandle` backed by a loaded WASM component. Only\navailable with the `wasm` feature.","visibility":"public"},{"name":"call_method","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":137,"end_line":165,"signature":"(\n        &self,\n        index: usize,\n        input: &I,\n    ) -> Result<O, CallError>","qualified_name":null,"doc_comment":"Call a plugin method by vtable index.\n\nSerializes the input with the backend's native wire (cdylib → bincode;\nPython/WASM → [`fidius_core::Value`]), dispatches, and decodes the\nresult into `O`. No built-in timeout — see the `fidius` crate docs.","visibility":"public"},{"name":"call_streaming","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":180,"end_line":207,"signature":"(\n        &self,\n        index: usize,\n        input: &I,\n    ) -> Result<crate::stream::ChunkStream, CallError>","qualified_name":null,"doc_comment":"Start a server-streaming method call by vtable index (FIDIUS-I-0026).\n\nReturns a [`crate::stream::ChunkStream`] — a `futures::Stream` of\n`Result<Value, _>` the caller pulls with `.next().await`. Backpressure and\ncancellation are structural: a slow consumer parks the producer, and\ndropping the stream tears the producer down. All three backends stream:\nPython and WASM cross via the self-describing [`Value`] currency; cdylib\ncrosses items as concrete bincode of the item type `O` and decodes them\nhere (FIDIUS-T-0137).\n\n`O` is the stream's item type. Python/WASM ignore it (they're already\n`Value`-native); cdylib uses it to `bincode::<O>`-decode each item.","visibility":"public"},{"name":"call_method_raw","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":210,"end_line":218,"signature":"(&self, index: usize, input: &[u8]) -> Result<Vec<u8>, CallError>","qualified_name":null,"doc_comment":"Call a `#[wire(raw)]` method: raw bytes in, raw bytes out, no bincode.","visibility":"public"},{"name":"has_capability","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":222,"end_line":227,"signature":"(&self, bit: u32) -> bool","qualified_name":null,"doc_comment":"Check if an optional method is supported (capability bit set).\nReturns `false` for `bit >= 64` and for backends without capabilities.","visibility":"public"},{"name":"info","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":230,"end_line":238,"signature":"(&self) -> &PluginInfo","qualified_name":null,"doc_comment":"Access the plugin's owned metadata.","visibility":"public"},{"name":"method_metadata","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":243,"end_line":252,"signature":"(&self, method_id: u32) -> Vec<(&str, &str)>","qualified_name":null,"doc_comment":"Static `#[method_meta(...)]` key/value metadata for the given method,\nin declaration order. Empty for out-of-range ids, for interfaces that\ndeclared none, and for backends without descriptor metadata.","visibility":"public"},{"name":"trait_metadata","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":256,"end_line":264,"signature":"(&self) -> Vec<(&str, &str)>","qualified_name":null,"doc_comment":"Static `#[trait_meta(...)]` key/value metadata declared on the trait.\nEmpty when none was declared or for backends without descriptor metadata.","visibility":"public"},{"name":"cdylib_stream_decode","kind":"function","file_path":"crates/fidius-host/src/handle.rs","start_line":273,"end_line":279,"signature":"(\n    bytes: &[u8],\n) -> Result<fidius_core::Value, CallError>","qualified_name":null,"doc_comment":"Per-item decoder for the cdylib streaming fast path (FIDIUS-T-0137): each item\ncrosses as concrete `bincode(O)` (byte-identical to the unary cdylib wire), so\nwe `wire::deserialize::<O>` then lift to a `Value`. This is the `decode_item`\nfn pointer the typed caller hands to [`CdylibExecutor::call_streaming_raw`] —\n`O` is monomorphised in by `call_streaming::<_, O>`.","visibility":"private"}],"crates/fidius-host/src/host.rs":[{"name":"PluginHost","kind":"struct","file_path":"crates/fidius-host/src/host.rs","start_line":31,"end_line":43,"signature":"{ search_paths: Vec<PathBuf>, load_policy: LoadPolicy, require_signature: bool, trusted_keys: Vec<VerifyingKey>, expecte...","qualified_name":null,"doc_comment":"Host for loading and managing plugins.","visibility":"public"},{"name":"PluginHostBuilder","kind":"struct","file_path":"crates/fidius-host/src/host.rs","start_line":46,"end_line":55,"signature":"{ search_paths: Vec<PathBuf>, load_policy: LoadPolicy, require_signature: bool, trusted_keys: Vec<VerifyingKey>, expecte...","qualified_name":null,"doc_comment":"Builder for configuring a PluginHost.","visibility":"public"},{"name":"PluginHostBuilder","kind":"type","file_path":"crates/fidius-host/src/host.rs","start_line":57,"end_line":142,"signature":"= PluginHostBuilder","qualified_name":null,"doc_comment":"PluginHost builder and plugin discovery.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":58,"end_line":69,"signature":"() -> Self","qualified_name":null,"doc_comment":"PluginHost builder and plugin discovery.","visibility":"private"},{"name":"egress","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":77,"end_line":80,"signature":"(mut self, policy: impl crate::executor::wasm::EgressPolicy) -> Self","qualified_name":null,"doc_comment":"Set a host-wide default `wasi:http` egress policy (FIDIUS-I-0027). Every\n`load_wasm` then enables outbound HTTP for a guest that declares the\n`http` capability, routing each request through `policy`. Without this (and\nwithout a per-load policy), `wasi:http` is never linked and a guest that\nimports it fails closed. Available only with the `wasm` feature.","visibility":"public"},{"name":"egress_policy","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":88,"end_line":91,"signature":"(mut self, policy: Arc<dyn crate::executor::wasm::EgressPolicy>) -> Self","qualified_name":null,"doc_comment":"Like [`Self::egress`] but accepts an already-erased\n`Arc<dyn EgressPolicy>` — for a policy that is shared across hosts, or\nselected/constructed at runtime (where the concrete type isn't known at\nthe call site). Threads through `load_wasm` exactly like `egress`.\nAvailable only with the `wasm` feature.","visibility":"public"},{"name":"search_path","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":94,"end_line":97,"signature":"(mut self, path: impl Into<PathBuf>) -> Self","qualified_name":null,"doc_comment":"Add a directory to search for plugin dylibs.","visibility":"public"},{"name":"load_policy","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":100,"end_line":103,"signature":"(mut self, policy: LoadPolicy) -> Self","qualified_name":null,"doc_comment":"Set the load policy (Strict or Lenient).","visibility":"public"},{"name":"require_signature","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":106,"end_line":109,"signature":"(mut self, require: bool) -> Self","qualified_name":null,"doc_comment":"Require plugins to have valid signatures.","visibility":"public"},{"name":"trusted_keys","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":112,"end_line":115,"signature":"(mut self, keys: &[VerifyingKey]) -> Self","qualified_name":null,"doc_comment":"Set trusted Ed25519 public keys for signature verification.","visibility":"public"},{"name":"interface_hash","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":118,"end_line":121,"signature":"(mut self, hash: u64) -> Self","qualified_name":null,"doc_comment":"Set the expected interface hash for validation.","visibility":"public"},{"name":"buffer_strategy","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":124,"end_line":127,"signature":"(mut self, strategy: BufferStrategyKind) -> Self","qualified_name":null,"doc_comment":"Set the expected buffer strategy for validation.","visibility":"public"},{"name":"build","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":130,"end_line":141,"signature":"(self) -> Result<PluginHost, LoadError>","qualified_name":null,"doc_comment":"Build the PluginHost.","visibility":"public"},{"name":"PluginHost","kind":"type","file_path":"crates/fidius-host/src/host.rs","start_line":144,"end_line":604,"signature":"= PluginHost","qualified_name":null,"doc_comment":"PluginHost builder and plugin discovery.","visibility":"private"},{"name":"builder","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":146,"end_line":148,"signature":"() -> PluginHostBuilder","qualified_name":null,"doc_comment":"Create a new builder.","visibility":"public"},{"name":"discover","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":159,"end_line":184,"signature":"(&self) -> Result<Vec<PluginInfo>, LoadError>","qualified_name":null,"doc_comment":"Discover all valid plugins in the configured search paths.\n\nScans each path for both:\n- dylib files (cdylib plugins, the existing path), and\n- subdirectories containing a `package.toml` with `runtime = \"python\"`\n(when the `python` feature is enabled).\n\nReturns owned `PluginInfo` for every valid plugin found, with\n`PluginInfo::runtime` distinguishing the two kinds.","visibility":"public"},{"name":"discover_cdylib","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":186,"end_line":206,"signature":"(&self, path: &Path, plugins: &mut Vec<PluginInfo>)","qualified_name":null,"doc_comment":"PluginHost builder and plugin discovery.","visibility":"private"},{"name":"discover_package","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":211,"end_line":235,"signature":"(&self, dir: &Path, plugins: &mut Vec<PluginInfo>)","qualified_name":null,"doc_comment":"Discover a directory-based package (`package.toml`) and surface it by\nruntime. Rust source packages are discovered via their built dylib (the\nloadable artifact), not here, so they're skipped.","visibility":"private"},{"name":"load","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":241,"end_line":285,"signature":"(&self, name: &str) -> Result<LoadedPlugin, LoadError>","qualified_name":null,"doc_comment":"Load a specific plugin by name.\n\nSearches all configured paths for a dylib containing a plugin\nwith the given name. Returns the loaded plugin ready for calling.","visibility":"public"},{"name":"find_python_package","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":290,"end_line":320,"signature":"(&self, name: &str) -> Result<PathBuf, LoadError>","qualified_name":null,"doc_comment":"Find a python plugin package directory by name across the configured\nsearch paths. The plugin name is matched against `package.toml`'s\n`[package].name`. Returns the directory path on success.","visibility":"public"},{"name":"load_python","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":332,"end_line":359,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::python_descriptor::PythonInterfaceDescriptor,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"Load a Python plugin package by name and validate it against the\nsupplied interface descriptor.\n\nThe caller passes the static `<TraitName>_PYTHON_DESCRIPTOR` emitted\nby the interface crate's `#[plugin_interface]` macro — that's the\nout-of-band hint the loader needs to map method names to vtable\nindices and to check the interface hash.\n\nAvailable only when fidius-host is built with the `python` feature.","visibility":"public"},{"name":"load_python_configured","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":367,"end_line":393,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::python_descriptor::PythonInterfaceDescriptor,\n        config: &C,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"Load a **configured** Python plugin (FIDIUS-A-0006 / CI.4): serialize\n`config` and bind it once via the module's\n`__fidius_configure__(config) -> instance`; methods then run on the\nconfigured instance. N differently-configured instances coexist.\nAvailable only with the `python` feature.","visibility":"public"},{"name":"find_wasm_package","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":398,"end_line":424,"signature":"(&self, name: &str) -> Result<PathBuf, LoadError>","qualified_name":null,"doc_comment":"Find a WASM package directory by name across the search paths (matches\n`package.toml` `[package].name` with `runtime = \"wasm\"`).","visibility":"public"},{"name":"load_wasm","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":441,"end_line":447,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::wasm_descriptor::WasmInterfaceDescriptor,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"Load a WASM component plugin package by name and validate it against the\nsupplied interface descriptor (the `<TraitName>_WASM_DESCRIPTOR` the\ninterface crate emits). Returns a unified [`crate::handle::PluginHandle`].\n\nThe component is sandboxed: WASI is wired into the `Linker` but the guest\ngets a zero-grant `WasiCtx` (no FS preopens, no env, no sockets). The\ncapability allow-list in `[wasm].capabilities` is applied in T-0104.\n\nOutbound HTTP is governed by the host's egress policy: a guest that\ndeclares the `http` capability gets `wasi:http` only when the host was\ngiven a policy (via [`PluginHostBuilder::egress`] or\n[`Self::load_wasm_with_egress`]) — otherwise it fails closed.\n\nAvailable only with the `wasm` feature.","visibility":"public"},{"name":"load_wasm_configured","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":455,"end_line":464,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::wasm_descriptor::WasmInterfaceDescriptor,\n        config: &C,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"Load a **configured** WASM plugin (FIDIUS-A-0006 / CI.3): serialize\n`config` and bind it once via the guest's `fidius-configure` export. The\ncomponent then runs on a persistent store, so methods dispatch on the\nconfigured instance and config crosses the sandbox boundary exactly once.\nAvailable only with the `wasm` feature.","visibility":"public"},{"name":"load_wasm_with_egress","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":472,"end_line":479,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::wasm_descriptor::WasmInterfaceDescriptor,\n        egress: impl crate::executor::wasm::EgressPolicy,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"Like [`Self::load_wasm`] but with a **per-plugin** `wasi:http` egress\npolicy that overrides any host-wide default (FIDIUS-I-0027). This is the\nright primitive for isolating connectors: a host-wide policy only sees the\noutbound *request*, not which plugin issued it, so per-plugin policies are\nhow you bound connector A to one set of hosts and connector B to another.","visibility":"public"},{"name":"load_wasm_impl","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":482,"end_line":603,"signature":"(\n        &self,\n        name: &str,\n        descriptor: &'static fidius_core::wasm_descriptor::WasmInterfaceDescriptor,\n        egress: Option<Arc<dyn crate::executor::wasm::EgressPolicy>>,\n        config: Option<&[u8]>,\n    ) -> Result<crate::handle::PluginHandle, LoadError>","qualified_name":null,"doc_comment":"PluginHost builder and plugin discovery.","visibility":"private"},{"name":"is_dylib","kind":"function","file_path":"crates/fidius-host/src/host.rs","start_line":607,"end_line":616,"signature":"(path: &Path) -> bool","qualified_name":null,"doc_comment":"Check if a path has a platform-appropriate dylib extension.","visibility":"private"}],"crates/fidius-host/src/lib.rs":[{"name":"arch","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":15,"end_line":15,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"arena","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":16,"end_line":16,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"error","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":17,"end_line":17,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"executor","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":18,"end_line":18,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"handle","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":19,"end_line":19,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"host","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":20,"end_line":20,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"loader","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":21,"end_line":21,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"package","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":22,"end_line":22,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"signing","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":23,"end_line":23,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"stream","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":25,"end_line":25,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"types","kind":"module","file_path":"crates/fidius-host/src/lib.rs","start_line":26,"end_line":26,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"}],"crates/fidius-host/src/loader.rs":[{"name":"LoadedLibrary","kind":"struct","file_path":"crates/fidius-host/src/loader.rs","start_line":28,"end_line":33,"signature":"{ library: Arc<Library>, plugins: Vec<LoadedPlugin> }","qualified_name":null,"doc_comment":"A loaded plugin library with validated descriptors.","visibility":"public"},{"name":"LoadedPlugin","kind":"struct","file_path":"crates/fidius-host/src/loader.rs","start_line":36,"end_line":51,"signature":"{ info: PluginInfo, vtable: *const c_void, free_buffer: Option<unsafe extern \"C\" fn(*mut u8, usize)>, method_count: u32,...","qualified_name":null,"doc_comment":"A single validated plugin from a loaded library.","visibility":"public"},{"name":"LoadedPlugin","kind":"type","file_path":"crates/fidius-host/src/loader.rs","start_line":55,"end_line":55,"signature":"impl Send for LoadedPlugin","qualified_name":null,"doc_comment":"Core plugin loading and descriptor validation.","visibility":"private"},{"name":"LoadedPlugin","kind":"type","file_path":"crates/fidius-host/src/loader.rs","start_line":56,"end_line":56,"signature":"impl Sync for LoadedPlugin","qualified_name":null,"doc_comment":"Core plugin loading and descriptor validation.","visibility":"private"},{"name":"LoadedPlugin","kind":"type","file_path":"crates/fidius-host/src/loader.rs","start_line":58,"end_line":65,"signature":"= LoadedPlugin","qualified_name":null,"doc_comment":"Core plugin loading and descriptor validation.","visibility":"private"},{"name":"fmt","kind":"function","file_path":"crates/fidius-host/src/loader.rs","start_line":59,"end_line":64,"signature":"(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result","qualified_name":null,"doc_comment":"Core plugin loading and descriptor validation.","visibility":"private"},{"name":"load_library","kind":"function","file_path":"crates/fidius-host/src/loader.rs","start_line":71,"end_line":124,"signature":"(path: &Path) -> Result<LoadedLibrary, LoadError>","qualified_name":null,"doc_comment":"Load a plugin library from a path.\n\nOpens the dylib, calls `fidius_get_registry()`, validates the registry\nand all descriptors, copies FFI data to owned types.","visibility":"public"},{"name":"validate_descriptor","kind":"function","file_path":"crates/fidius-host/src/loader.rs","start_line":127,"end_line":163,"signature":"(\n    desc: &PluginDescriptor,\n    library: &Arc<Library>,\n) -> Result<LoadedPlugin, LoadError>","qualified_name":null,"doc_comment":"Validate a single descriptor and copy to owned types.","visibility":"private"},{"name":"validate_against_interface","kind":"function","file_path":"crates/fidius-host/src/loader.rs","start_line":166,"end_line":190,"signature":"(\n    plugin: &LoadedPlugin,\n    expected_hash: Option<u64>,\n    expected_strategy: Option<BufferStrategyKind>,\n) -> Result<(), LoadError>","qualified_name":null,"doc_comment":"Validate a loaded plugin against expected interface parameters.","visibility":"public"}],"crates/fidius-host/src/package.rs":[{"name":"load_package_manifest","kind":"function","file_path":"crates/fidius-host/src/package.rs","start_line":41,"end_line":45,"signature":"(\n    dir: &Path,\n) -> Result<PackageManifest<M>, PackageError>","qualified_name":null,"doc_comment":"Load and validate a package manifest against a host-defined schema.\n\nThis is the primary entry point for host applications working with packages.\nThe type parameter `M` is the host's metadata schema — if the manifest's\n`[metadata]` section doesn't deserialize into `M`, this returns an error.\n\n# Example\n\n```ignore\n#[derive(Deserialize)]\nstruct MySchema {\ncategory: String,\nmin_host_version: String,\n}\n\nlet manifest = load_package_manifest::<MySchema>(Path::new(\"./packages/blur/\"))?;\nassert_eq!(manifest.metadata.category, \"image-processing\");\n```","visibility":"public"},{"name":"discover_packages","kind":"function","file_path":"crates/fidius-host/src/package.rs","start_line":51,"end_line":70,"signature":"(dir: &Path) -> Result<Vec<PathBuf>, PackageError>","qualified_name":null,"doc_comment":"Discover packages in a directory.\n\nScans `dir` for subdirectories containing a `package.toml` file.\nReturns the paths to each package directory found.","visibility":"public"},{"name":"verify_package","kind":"function","file_path":"crates/fidius-host/src/package.rs","start_line":81,"end_line":108,"signature":"(dir: &Path, trusted_keys: &[VerifyingKey]) -> Result<(), PackageError>","qualified_name":null,"doc_comment":"Verify a source package's signature against trusted public keys.\n\nRecomputes the package digest from files on disk and verifies the\n`package.sig` signature against the provided trusted keys.\n\n# Errors\n\n- `PackageError::SignatureNotFound` — if `package.sig` doesn't exist\n- `PackageError::SignatureInvalid` — if no trusted key verifies the signature","visibility":"public"},{"name":"unpack_fid","kind":"function","file_path":"crates/fidius-host/src/package.rs","start_line":127,"end_line":139,"signature":"(archive: &Path, dest: &Path) -> Result<PathBuf, PackageError>","qualified_name":null,"doc_comment":"Extract a `.fid` archive and validate its contents.\n\nDelegates to [`fidius_core::package::unpack_package`], which applies strict\nsafety defaults: entries with `..` components, absolute paths, symlinks, or\nhardlinks are rejected, and the cumulative decompressed size is capped to\nguard against decompression bombs. Emits a `tracing::warn!` if the unpacked\npackage has no `package.sig`.\n\nFor archives that legitimately exceed the default caps, call\n[`fidius_core::package::unpack_package_with_options`] directly.\n\n# Example\n\n```ignore\nlet pkg_dir = unpack_fid(Path::new(\"blur-filter-1.0.0.fid\"), Path::new(\"./plugins/\"))?;\nlet manifest = load_package_manifest::<MySchema>(&pkg_dir)?;\n```","visibility":"public"},{"name":"build_package","kind":"function","file_path":"crates/fidius-host/src/package.rs","start_line":144,"end_line":193,"signature":"(dir: &Path, release: bool) -> Result<PathBuf, PackageError>","qualified_name":null,"doc_comment":"Build a package by running `cargo build` inside the package directory.\n\nReturns the path to the compiled cdylib on success.","visibility":"public"}],"crates/fidius-host/src/signing.rs":[{"name":"sig_path_for","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":27,"end_line":32,"signature":"(path: &Path) -> std::path::PathBuf","qualified_name":null,"doc_comment":"Compute the detached signature file path for a given file.\n\nAppends `.sig` to the full filename (e.g., `foo.dylib` → `foo.dylib.sig`).","visibility":"public"},{"name":"verify_signature","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":43,"end_line":74,"signature":"(dylib_path: &Path, trusted_keys: &[VerifyingKey]) -> Result<(), LoadError>","qualified_name":null,"doc_comment":"Verify a plugin dylib's signature against trusted public keys.\n\nReads the dylib bytes and the detached `.sig` file, then verifies\nthe Ed25519 signature against each trusted key until one matches.\n\n# Errors\n\n- `LoadError::SignatureRequired` — if the `.sig` file doesn't exist\n- `LoadError::SignatureInvalid` — if no trusted key verifies the signature","visibility":"public"},{"name":"verify_package_signature","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":84,"end_line":117,"signature":"(\n    dir: &Path,\n    trusted_keys: &[VerifyingKey],\n) -> Result<(), LoadError>","qualified_name":null,"doc_comment":"Verify a **package** signature: `package.sig` in `dir`, an Ed25519 signature\nover `package_digest(dir)` (the runtime-agnostic content hash), against any\ntrusted key. Used for package-based runtimes (Python, WASM) where the signed\nartifact is the whole package directory, not a single dylib file.\n\n`package_digest` excludes `*.sig`, so the digest is identical at sign and\nverify time, and tampering with any package file (e.g. the `.wasm`\ncomponent) changes the digest and fails verification.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-host/src/signing.rs","start_line":120,"end_line":189,"signature":null,"qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"create_test_file","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":126,"end_line":130,"signature":"(content: &[u8]) -> NamedTempFile","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"sign_file","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":132,"end_line":140,"signature":"(path: &Path, signing_key: &SigningKey)","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"valid_signature_succeeds","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":143,"end_line":152,"signature":"()","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"tampered_file_fails","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":155,"end_line":167,"signature":"()","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"wrong_key_fails","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":170,"end_line":179,"signature":"()","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"},{"name":"missing_sig_file_returns_required","kind":"function","file_path":"crates/fidius-host/src/signing.rs","start_line":182,"end_line":188,"signature":"()","qualified_name":null,"doc_comment":"packages (sign the runtime-agnostic `package_digest`, used by Python/WASM).\nEd25519 signature verification for plugins: dylibs (sign the file bytes) and","visibility":"private"}],"crates/fidius-host/src/stream.rs":[{"name":"ChunkStream","kind":"struct","file_path":"crates/fidius-host/src/stream.rs","start_line":58,"end_line":60,"signature":"{ inner: Pin<Box<dyn Stream<Item = Result<Value, CallError>> + Send>> }","qualified_name":null,"doc_comment":"Host-facing pull handle for a server-streaming plugin call.\n\nA `futures::Stream` of decoded items. Pull with `.next().await`; the stream\nends after the producer's `END` frame, or yields one final `Err` and then\nends on an `ERROR`/abort/malformed frame. Dropping the handle cancels the\ncall — the backend bridge observes the drop and tears its producer down.","visibility":"public"},{"name":"ChunkStream","kind":"type","file_path":"crates/fidius-host/src/stream.rs","start_line":62,"end_line":144,"signature":"= ChunkStream","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":65,"end_line":72,"signature":"(stream: S) -> Self","qualified_name":null,"doc_comment":"Wrap any item stream as a [`ChunkStream`]. Backends that already produce\n`Result<Value, CallError>` items use this directly.","visibility":"public"},{"name":"from_frame_bytes","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":96,"end_line":128,"signature":"(frames: S, decode_item: D) -> Self","qualified_name":null,"doc_comment":"Build a [`ChunkStream`] from a stream of raw, length-delimited frame\n*bytes* (one encoded [`Frame`] per element — the shape a serialized\nbackend's `next()` hands back: a WASM `list<u8>`, a cdylib buffer),\napplying the terminal-frame state machine:\n\n- `ITEM` → `decode_item(payload)` → `Ok(value)`, continue.\n- `END` → stop (no item).\n- `ERROR` → one `Err(CallError::Plugin)`, then stop.\n- a malformed/truncated frame → one `Err(CallError::MalformedFrame)`, stop.\n- an `ITEM` whose payload fails `decode_item` → that `Err`, then stop.\n- the byte source ending **without** a terminal frame → one\n`Err(CallError::StreamAborted)`, stop.\n\n`decode_item` is caller-supplied because the ITEM payload is \"one unary\nreturn value\": for cdylib that is concrete-type bincode the typed client\ndecodes via `wire::deserialize::<T>()` then `to_value`; for a `#[wire(raw)]`\nstream it is the bytes themselves. (Vanilla bincode cannot reconstruct a\nself-describing [`Value`] — `deserialize_any` is unsupported — so there is\ndeliberately no fixed \"bytes → Value\" decode here.) The self-describing\nin-process backends (Python) skip framing entirely and use [`Self::new`].\n\nAfter any error the stream is fused: subsequent polls yield `None`.","visibility":"public"},{"name":"from_frames","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":134,"end_line":143,"signature":"(frames: Vec<Frame>, decode_item: D) -> Self","qualified_name":null,"doc_comment":"Build a [`ChunkStream`] over a fixed, in-memory sequence of [`Frame`]s.\nA convenience for tests and serialized-backend fixtures; runs the same\nterminal-frame state machine as [`Self::from_frame_bytes`] with the same\ncaller-supplied `decode_item`.","visibility":"public"},{"name":"ChunkStream","kind":"type","file_path":"crates/fidius-host/src/stream.rs","start_line":146,"end_line":152,"signature":"impl Stream for ChunkStream","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"Item","kind":"type","file_path":"crates/fidius-host/src/stream.rs","start_line":147,"end_line":147,"signature":"= Result<Value, CallError>","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"poll_next","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":149,"end_line":151,"signature":"(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Option<Self::Item>>","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"StreamExecutor","kind":"interface","file_path":"crates/fidius-host/src/stream.rs","start_line":162,"end_line":167,"signature":"{ fn call_streaming() }","qualified_name":null,"doc_comment":"Backends whose typed boundary can produce a **server-streaming** result.\n\nSits beside [`crate::executor::ValueExecutor`]: that returns one [`Value`],\nthis returns a [`ChunkStream`] of them. Implemented by all three backends\n(Python and WASM via self-describing values; cdylib via its FFI\niterator-handle ABI). `PluginHandle` routes a streaming method to\n`call_streaming` exactly as it routes a unary one to `call`.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-host/src/stream.rs","start_line":170,"end_line":268,"signature":null,"qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"item","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":177,"end_line":179,"signature":"(v: i64) -> Frame","qualified_name":null,"doc_comment":"An ITEM frame carrying a concrete `i64` (bincode of a concrete type\nround-trips fine — unlike a self-describing `Value`).","visibility":"private"},{"name":"decode_i64","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":182,"end_line":186,"signature":"(b: &[u8]) -> Result<Value, CallError>","qualified_name":null,"doc_comment":"The matching item decoder: concrete-bincode `i64` → `Value`.","visibility":"private"},{"name":"collect","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":188,"end_line":194,"signature":"(mut s: ChunkStream) -> Vec<Result<Value, CallError>>","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"items_then_clean_end","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":197,"end_line":205,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"native_value_stream_via_new","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":208,"end_line":219,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"error_frame_terminates_after_one_err","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":222,"end_line":235,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"missing_terminal_is_abort","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":238,"end_line":245,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"malformed_frame_surfaces_then_stops","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":248,"end_line":261,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"},{"name":"empty_stream_just_ends","kind":"function","file_path":"crates/fidius-host/src/stream.rs","start_line":264,"end_line":267,"signature":"()","qualified_name":null,"doc_comment":"turns that byte sequence into the item stream every backend bridge feeds.\n[`ChunkStream::from_frame_bytes`] runs the terminal-frame state machine that\nzero or more `ITEM` frames terminated by exactly one `END` or `ERROR`.\nOn the wire a stream is a sequence of [`Frame`]s (see [`fidius_core::frame`]):\n\n## Wire\n\nthe producer down (D3 / REQ-002).\npulling (REQ-003). Dropping the `ChunkStream` drops the bridge, which tears\na backend bridge that fills a bounded channel blocks when the host stops\n- **Backpressure & cancel are structural.** The host pulls with `.next().await`;\na tokio app, so the handle is awaited, not polled on a blocked thread.\n`Result<Value, CallError>`. **Native async** (D1) — every consuming host is\n- [`ChunkStream`] — the host-side handle: a `futures::Stream` of\nvia its FFI iterator-handle ABI).\nby all three backends (Python and WASM via self-describing values; cdylib\nstarts a server-streaming method and returns a [`ChunkStream`]. Implemented\n- [`StreamExecutor`] — the backend-side trait. `async fn call_streaming`\n\n## The pieces (design decisions D1–D3)\n\nhandle of values out.\nThis module adds the *streaming* seam: one call in, an unbounded **pull**\nThe unary seam ([`crate::executor`]) carries one value in and one value out.\n\nServer-streaming dispatch (FIDIUS-I-0026).","visibility":"private"}],"crates/fidius-host/src/types.rs":[{"name":"PluginRuntimeKind","kind":"enum","file_path":"crates/fidius-host/src/types.rs","start_line":23,"end_line":34,"signature":"Cdylib | Python | Wasm","qualified_name":null,"doc_comment":"Plugin runtime kind. Mirrors `fidius_core::package::PackageRuntime` and\nsurfaces it in the host-facing `PluginInfo`. Re-exported here so host\ncallers don't need a transitive `fidius-core` use.","visibility":"public"},{"name":"PluginInfo","kind":"struct","file_path":"crates/fidius-host/src/types.rs","start_line":43,"end_line":59,"signature":"{ name: String, interface_name: String, interface_hash: u64, interface_version: u32, capabilities: u64, buffer_strategy:...","qualified_name":null,"doc_comment":"Owned metadata for a discovered or loaded plugin.\n\nAll data copied from FFI descriptor — no raw pointers. `capabilities` and\n`buffer_strategy` are cdylib-specific concepts; for python plugins they\ntake their default values (0 / `PluginAllocated`) and have no runtime\nmeaning.","visibility":"public"},{"name":"PluginInfo","kind":"type","file_path":"crates/fidius-host/src/types.rs","start_line":61,"end_line":76,"signature":"= PluginInfo","qualified_name":null,"doc_comment":"Owned metadata types for loaded plugins.","visibility":"private"},{"name":"is_cdylib","kind":"function","file_path":"crates/fidius-host/src/types.rs","start_line":63,"end_line":65,"signature":"(&self) -> bool","qualified_name":null,"doc_comment":"True if this is a cdylib-backed plugin.","visibility":"public"},{"name":"is_python","kind":"function","file_path":"crates/fidius-host/src/types.rs","start_line":68,"end_line":70,"signature":"(&self) -> bool","qualified_name":null,"doc_comment":"True if this is a Python plugin.","visibility":"public"},{"name":"is_wasm","kind":"function","file_path":"crates/fidius-host/src/types.rs","start_line":73,"end_line":75,"signature":"(&self) -> bool","qualified_name":null,"doc_comment":"True if this is a WASM component plugin.","visibility":"public"},{"name":"LoadPolicy","kind":"enum","file_path":"crates/fidius-host/src/types.rs","start_line":80,"end_line":86,"signature":"Strict | Lenient","qualified_name":null,"doc_comment":"Controls how strictly the host validates plugins.","visibility":"public"}],"crates/fidius-host/tests/cdylib_streaming_e2e.rs":[{"name":"ticker_handle","kind":"function","file_path":"crates/fidius-host/tests/cdylib_streaming_e2e.rs","start_line":31,"end_line":43,"signature":"() -> PluginHandle","qualified_name":null,"doc_comment":"streaming peer alongside Python and WASM.\ndriven through `PluginHandle::call_streaming` — proving cdylib is now a true\niterator-handle FFI shims (init/next/drop). Loaded as a real `.dylib` and\n`tick(count) -> fidius::Stream<u64>`; `#[plugin_impl]` generates the\n`TickerImpl` plugin in `tests/test-plugin-smoke` implements\ncdylib server-streaming E2E (FIDIUS-I-0026 CS.1 / Phase 3). The","visibility":"private"},{"name":"cdylib_stream_yields_all_items","kind":"function","file_path":"crates/fidius-host/tests/cdylib_streaming_e2e.rs","start_line":46,"end_line":57,"signature":"()","qualified_name":null,"doc_comment":"streaming peer alongside Python and WASM.\ndriven through `PluginHandle::call_streaming` — proving cdylib is now a true\niterator-handle FFI shims (init/next/drop). Loaded as a real `.dylib` and\n`tick(count) -> fidius::Stream<u64>`; `#[plugin_impl]` generates the\n`TickerImpl` plugin in `tests/test-plugin-smoke` implements\ncdylib server-streaming E2E (FIDIUS-I-0026 CS.1 / Phase 3). The","visibility":"private"},{"name":"cdylib_empty_stream","kind":"function","file_path":"crates/fidius-host/tests/cdylib_streaming_e2e.rs","start_line":60,"end_line":67,"signature":"()","qualified_name":null,"doc_comment":"streaming peer alongside Python and WASM.\ndriven through `PluginHandle::call_streaming` — proving cdylib is now a true\niterator-handle FFI shims (init/next/drop). Loaded as a real `.dylib` and\n`tick(count) -> fidius::Stream<u64>`; `#[plugin_impl]` generates the\n`TickerImpl` plugin in `tests/test-plugin-smoke` implements\ncdylib server-streaming E2E (FIDIUS-I-0026 CS.1 / Phase 3). The","visibility":"private"},{"name":"cdylib_huge_stream_is_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/cdylib_streaming_e2e.rs","start_line":70,"end_line":84,"signature":"()","qualified_name":null,"doc_comment":"streaming peer alongside Python and WASM.\ndriven through `PluginHandle::call_streaming` — proving cdylib is now a true\niterator-handle FFI shims (init/next/drop). Loaded as a real `.dylib` and\n`tick(count) -> fidius::Stream<u64>`; `#[plugin_impl]` generates the\n`TickerImpl` plugin in `tests/test-plugin-smoke` implements\ncdylib server-streaming E2E (FIDIUS-I-0026 CS.1 / Phase 3). The","visibility":"private"}],"crates/fidius-host/tests/configured_cdylib_e2e.rs":[{"name":"GreetConfig","kind":"struct","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":25,"end_line":27,"signature":"{ greeting: String }","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"public"},{"name":"Greeter","kind":"interface","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":30,"end_line":32,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"public"},{"name":"ConfiguredGreeter","kind":"struct","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":34,"end_line":36,"signature":"{ cfg: GreetConfig }","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"public"},{"name":"ConfiguredGreeter","kind":"type","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":39,"end_line":44,"signature":"impl Greeter for ConfiguredGreeter","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"},{"name":"greet","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":40,"end_line":43,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"},{"name":"ConfiguredGreeter","kind":"type","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":46,"end_line":50,"signature":"= ConfiguredGreeter","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"},{"name":"configure","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":47,"end_line":49,"signature":"(cfg: GreetConfig) -> Self","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"},{"name":"config_bound_once_and_used_in_methods","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":55,"end_line":67,"signature":"()","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"},{"name":"n_differently_configured_instances_coexist","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_e2e.rs","start_line":70,"end_line":90,"signature":"()","qualified_name":null,"doc_comment":"differently-configured instances coexist in one host.\nProves config is bound at construct, used in methods, and that N\nconfig once; methods close over it without re-passing it across the boundary.\n`#[plugin_impl(Trait, config = C)]` whose `configure` constructor binds the\nConfigured cdylib plugin instances (FIDIUS-I-0029 / CI.2, ADR-0006): a","visibility":"private"}],"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs":[{"name":"Cfg","kind":"struct","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":29,"end_line":31,"signature":"{ base: u64 }","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"Ticker","kind":"interface","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":34,"end_line":36,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"public"},{"name":"ConfTicker","kind":"struct","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":38,"end_line":40,"signature":"{ cfg: Cfg }","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"public"},{"name":"ConfTicker","kind":"type","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":43,"end_line":48,"signature":"impl Ticker for ConfTicker","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"tick","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":44,"end_line":47,"signature":"(&self, count: u32) -> fidius_core::Stream<u64>","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"ConfTicker","kind":"type","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":50,"end_line":54,"signature":"= ConfTicker","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"configure","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":51,"end_line":53,"signature":"(cfg: Cfg) -> Self","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"configured_cdylib_streaming_reads_bound_config","kind":"function","file_path":"crates/fidius-host/tests/configured_cdylib_stream_e2e.rs","start_line":59,"end_line":69,"signature":"()","qualified_name":null,"doc_comment":"configured cdylib plugin streams from its bound config.\nconstruct/destroy instance pointer (CI.1) flows into the stream-init shim, so a\ninstance whose server-streaming method reads the bound config. Proves the\nConfigured + streaming on the **cdylib** backend (FIDIUS-I-0029): a configured","visibility":"private"}],"crates/fidius-host/tests/configured_python_e2e.rs":[{"name":"PipeConfig","kind":"struct","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":31,"end_line":33,"signature":"{ display_name: String }","qualified_name":null,"doc_comment":"macro-generated descriptor); `name()` returns the configured display name.\ndifferently-configured instances coexist. Implements `BytePipe` (reuses its\n`load_python_configured`, methods run on the configured instance, and N\nmodule-level functions. The host binds the config once via\nplugin that exports `__fidius_configure__(config) -> instance` instead of\nConfigured Python plugin instances (FIDIUS-I-0029 / CI.4, ADR-0006): a Python","visibility":"private"},{"name":"byte_pipe_descriptor","kind":"function","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":35,"end_line":37,"signature":"() -> &'static PythonInterfaceDescriptor","qualified_name":null,"doc_comment":"macro-generated descriptor); `name()` returns the configured display name.\ndifferently-configured instances coexist. Implements `BytePipe` (reuses its\n`load_python_configured`, methods run on the configured instance, and N\nmodule-level functions. The host binds the config once via\nplugin that exports `__fidius_configure__(config) -> instance` instead of\nConfigured Python plugin instances (FIDIUS-I-0029 / CI.4, ADR-0006): a Python","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":39,"end_line":46,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"macro-generated descriptor); `name()` returns the configured display name.\ndifferently-configured instances coexist. Implements `BytePipe` (reuses its\n`load_python_configured`, methods run on the configured instance, and N\nmodule-level functions. The host binds the config once via\nplugin that exports `__fidius_configure__(config) -> instance` instead of\nConfigured Python plugin instances (FIDIUS-I-0029 / CI.4, ADR-0006): a Python","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":48,"end_line":60,"signature":"(src: &Path, dst: &Path)","qualified_name":null,"doc_comment":"macro-generated descriptor); `name()` returns the configured display name.\ndifferently-configured instances coexist. Implements `BytePipe` (reuses its\n`load_python_configured`, methods run on the configured instance, and N\nmodule-level functions. The host binds the config once via\nplugin that exports `__fidius_configure__(config) -> instance` instead of\nConfigured Python plugin instances (FIDIUS-I-0029 / CI.4, ADR-0006): a Python","visibility":"private"},{"name":"stage","kind":"function","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":64,"end_line":81,"signature":"(tmp: &tempfile::TempDir) -> PathBuf","qualified_name":null,"doc_comment":"Stage the configured fixture: copy it, vendor the SDK, and bake the real\nBytePipe interface hash into the `__HASH_PLACEHOLDER__` sentinel.","visibility":"private"},{"name":"config_bound_once_and_used_in_methods","kind":"function","file_path":"crates/fidius-host/tests/configured_python_e2e.rs","start_line":84,"end_line":106,"signature":"()","qualified_name":null,"doc_comment":"macro-generated descriptor); `name()` returns the configured display name.\ndifferently-configured instances coexist. Implements `BytePipe` (reuses its\n`load_python_configured`, methods run on the configured instance, and N\nmodule-level functions. The host binds the config once via\nplugin that exports `__fidius_configure__(config) -> instance` instead of\nConfigured Python plugin instances (FIDIUS-I-0029 / CI.4, ADR-0006): a Python","visibility":"private"}],"crates/fidius-host/tests/configured_python_stream_e2e.rs":[{"name":"Cfg","kind":"struct","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":30,"end_line":32,"signature":"{ base: u64 }","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"ticker_descriptor","kind":"function","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":34,"end_line":36,"signature":"() -> &'static PythonInterfaceDescriptor","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":38,"end_line":45,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":47,"end_line":59,"signature":"(src: &Path, dst: &Path)","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"stage","kind":"function","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":61,"end_line":80,"signature":"(tmp: &tempfile::TempDir) -> PathBuf","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"},{"name":"configured_python_streaming_reads_bound_config","kind":"function","file_path":"crates/fidius-host/tests/configured_python_stream_e2e.rs","start_line":83,"end_line":106,"signature":"()","qualified_name":null,"doc_comment":"config. Completes configured+streaming across all three backends.\ninstance (`__fidius_configure__`) whose streaming generator reads the bound\nConfigured + streaming on the **Python** backend (FIDIUS-I-0029): a configured","visibility":"private"}],"crates/fidius-host/tests/configured_wasm_e2e.rs":[{"name":"Cfg","kind":"struct","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":32,"end_line":34,"signature":"{ greeting: String }","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"},{"name":"Greeter","kind":"interface","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":38,"end_line":40,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"public"},{"name":"component","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":42,"end_line":55,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":43,"end_line":43,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"},{"name":"stage","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":57,"end_line":68,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"},{"name":"config_bound_once_and_used_in_methods","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":71,"end_line":95,"signature":"()","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"},{"name":"n_differently_configured_instances_coexist","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_e2e.rs","start_line":98,"end_line":128,"signature":"()","qualified_name":null,"doc_comment":"configured instances coexist (each its own store).\nthen run on the configured instance without re-passing config. N differently-\nbound once via the `fidius-configure` export onto a persistent store; methods\nmacro-authored `#[plugin_impl(.., config = Cfg)]` connector whose config is\nConfigured WASM plugin instances (FIDIUS-I-0029 / CI.3, ADR-0006): a","visibility":"private"}],"crates/fidius-host/tests/configured_wasm_stream_e2e.rs":[{"name":"Cfg","kind":"struct","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":33,"end_line":35,"signature":"{ base: u64 }","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"private"},{"name":"Ticker","kind":"interface","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":38,"end_line":40,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"public"},{"name":"component","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":42,"end_line":59,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":43,"end_line":43,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"private"},{"name":"stage","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":61,"end_line":72,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"private"},{"name":"configured_streaming_reads_bound_config","kind":"function","file_path":"crates/fidius-host/tests/configured_wasm_stream_e2e.rs","start_line":75,"end_line":101,"signature":"()","qualified_name":null,"doc_comment":"start), so configured + streaming compose.\nstore is configured before the stream starts (config crosses once, at stream\nvia `fidius-configure`, then a server-streaming method reads it. The stream's\nConfigured **streaming** WASM plugin (FIDIUS-I-0029 / CI.3): config bound once","visibility":"private"}],"crates/fidius-host/tests/e2e.rs":[{"name":"plugin_source_dir","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":22,"end_line":24,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"plugin_dir","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":27,"end_line":35,"signature":"() -> &'static Path","qualified_name":null,"doc_comment":"Cached plugin build directory — same fixture shared across all e2e tests.","visibility":"private"},{"name":"DIR","kind":"variable","file_path":"crates/fidius-host/tests/e2e.rs","start_line":28,"end_line":28,"signature":": std::sync::OnceLock<PathBuf>","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"dylib_path","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":37,"end_line":46,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"cleanup_sig","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":48,"end_line":53,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"signed_plugin_loads_with_correct_key","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":57,"end_line":72,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"signed_plugin_fails_with_wrong_key","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":76,"end_line":96,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"unsigned_plugin_fails_when_signature_required","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":100,"end_line":118,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"unsigned_plugin_loads_without_signature_requirement","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":122,"end_line":147,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"AddInput","kind":"struct","file_path":"crates/fidius-host/tests/e2e.rs","start_line":134,"end_line":137,"signature":"{ a: i64, b: i64 }","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"AddOutput","kind":"struct","file_path":"crates/fidius-host/tests/e2e.rs","start_line":139,"end_line":141,"signature":"{ result: i64 }","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"lenient_policy_still_enforces_signatures","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":151,"end_line":172,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"},{"name":"lenient_policy_still_rejects_wrong_key","kind":"function","file_path":"crates/fidius-host/tests/e2e.rs","start_line":176,"end_line":197,"signature":"()","qualified_name":null,"doc_comment":"End-to-end validation tests: signing, negative cases.","visibility":"private"}],"crates/fidius-host/tests/integration.rs":[{"name":"plugin_source_dir","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":29,"end_line":31,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"plugin_dir","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":34,"end_line":45,"signature":"() -> &'static Path","qualified_name":null,"doc_comment":"Directory containing the cached-built test plugin cdylib.","visibility":"private"},{"name":"DIR","kind":"variable","file_path":"crates/fidius-host/tests/integration.rs","start_line":38,"end_line":38,"signature":": std::sync::OnceLock<PathBuf>","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"client","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":48,"end_line":57,"signature":"() -> CalculatorClient","qualified_name":null,"doc_comment":"Build a client from the built+loaded plugin. Used by most method-call tests.","visibility":"private"},{"name":"discover_finds_plugin","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":60,"end_line":73,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"load_plugin_by_name","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":76,"end_line":85,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"call_add_method_via_client","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":88,"end_line":92,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"call_multiply_method_via_client","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":95,"end_line":100,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"call_multi_arg_add_direct_via_client","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":103,"end_line":107,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"call_zero_arg_version_via_client","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":110,"end_line":114,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"plugin_info_is_correct","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":117,"end_line":133,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"load_nonexistent_plugin_returns_not_found","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":136,"end_line":144,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"out_of_bounds_vtable_index_returns_error","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":147,"end_line":169,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"Dummy","kind":"struct","file_path":"crates/fidius-host/tests/integration.rs","start_line":157,"end_line":157,"signature":null,"qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"raw_wire_method_round_trips","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":172,"end_line":192,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"raw_wire_method_handles_large_payload","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":195,"end_line":212,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"arena_plugin_loads_and_round_trips","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":215,"end_line":231,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"arena_plugin_grows_buffer_on_too_small_retry","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":234,"end_line":256,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"trait_and_method_metadata_readable_through_handle","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":259,"end_line":285,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"has_capability_returns_false_for_high_bits","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":288,"end_line":302,"signature":"()","qualified_name":null,"doc_comment":"capability / info assertions where the Client abstracts them away.\ntests and the raw `PluginHandle::call_method` path for out-of-bounds /\nExercises the generated `CalculatorClient` typed proxy for method-call\n\nIntegration test: load the test-plugin-smoke cdylib via fidius-host API.","visibility":"private"},{"name":"discover_surfaces_wasm_package_with_wasm_runtime","kind":"function","file_path":"crates/fidius-host/tests/integration.rs","start_line":308,"end_line":347,"signature":"()","qualified_name":null,"doc_comment":"Routing reserves the WASM seat (FIDIUS-I-0021 Phase 1): a `runtime = \"wasm\"`\npackage is surfaced by discovery with `PluginRuntimeKind::Wasm`, not silently\nmistaken for a Rust package. The loader lands in Phase 2.","visibility":"private"}],"crates/fidius-host/tests/macro_egress_e2e.rs":[{"name":"Fetcher","kind":"interface","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":40,"end_line":42,"signature":"{ fn fetch() }","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"public"},{"name":"macro_fetcher_component","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":45,"end_line":59,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"Build the macro-fetcher component once.","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":46,"end_line":46,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"mock_http_once","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":62,"end_line":80,"signature":"(body: &'static str) -> (String, std::thread::JoinHandle<()>)","qualified_name":null,"doc_comment":"One-shot loopback mock HTTP server serving a single request with `body`.","visibility":"private"},{"name":"AllowAll","kind":"struct","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":82,"end_line":82,"signature":null,"qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"AllowAll","kind":"type","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":83,"end_line":87,"signature":"impl EgressPolicy for AllowAll","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"authorize","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":84,"end_line":86,"signature":"(&self, _parts: &mut http::request::Parts) -> Result<(), EgressDenied>","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"DenyAll","kind":"struct","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":89,"end_line":89,"signature":null,"qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"DenyAll","kind":"type","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":90,"end_line":94,"signature":"impl EgressPolicy for DenyAll","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"authorize","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":91,"end_line":93,"signature":"(&self, _parts: &mut http::request::Parts) -> Result<(), EgressDenied>","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"stage_pkg","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":97,"end_line":108,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"Stage the macro-fetcher as a `runtime = \"wasm\"` package declaring `http`.","visibility":"private"},{"name":"macro_connector_egress_allowed","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":111,"end_line":129,"signature":"()","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"macro_connector_egress_denied","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":132,"end_line":154,"signature":"()","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"},{"name":"macro_connector_no_policy_fails_closed","kind":"function","file_path":"crates/fidius-host/tests/macro_egress_e2e.rs","start_line":157,"end_line":173,"signature":"()","qualified_name":null,"doc_comment":"wasi:http `generate!` compose, and that the result rides the two-key gate.\ncodegen emits. Proves the macro's export `generate!` and `fidius-guest::http`'s\nhand-written WIT, no raw wasi:http bindings — exactly what an adopter's\nbrokered through the **shipped** `EgressPolicy` (FIDIUS-I-0027). No\n`fidius_guest::http::get`, built to a WASM component, loaded by the host and\nconnector written entirely with the fidius macros, whose `fetch` calls\nEnd-to-end, the **full guest→host egress loop** (FIDIUS-I-0028 / GH.2): a","visibility":"private"}],"crates/fidius-host/tests/macro_wasm.rs":[{"name":"Greeter","kind":"interface","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":42,"end_line":47,"signature":"{ fn greet(), fn echo() }","qualified_name":null,"doc_comment":"here via a separate `cargo build --target wasm32-wasip2` invocation.\n(FIDIUS-T-0094). The fixture `tests/wasm-fixtures/macro-greeter` is built\nRuns only with `--features wasm` and requires the wasm component toolchain\n\ngenerated WIT, and the component's `fidius-interface-hash` all agree.\n**macro-emitted** `Greeter_WASM_DESCRIPTOR` — proving the descriptor, the\n`fidius-guest` split), and the host loads it through `load_wasm` using the\nit to a WASM component (FIDIUS-T-0106 auto-export, unblocked by the I-0022\nEnd-to-end: a Rust author defines an interface with the fidius macros, builds","visibility":"public"},{"name":"macro_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":50,"end_line":64,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"Build the macro-greeter component once and return its bytes.","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":51,"end_line":51,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"here via a separate `cargo build --target wasm32-wasip2` invocation.\n(FIDIUS-T-0094). The fixture `tests/wasm-fixtures/macro-greeter` is built\nRuns only with `--features wasm` and requires the wasm component toolchain\n\ngenerated WIT, and the component's `fidius-interface-hash` all agree.\n**macro-emitted** `Greeter_WASM_DESCRIPTOR` — proving the descriptor, the\n`fidius-guest` split), and the host loads it through `load_wasm` using the\nit to a WASM component (FIDIUS-T-0106 auto-export, unblocked by the I-0022\nEnd-to-end: a Rust author defines an interface with the fidius macros, builds","visibility":"private"},{"name":"stage_pkg","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":67,"end_line":89,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"Stage a `runtime = \"wasm\"` package containing the built component.","visibility":"private"},{"name":"macro_built_component_loads_and_calls","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":92,"end_line":118,"signature":"()","qualified_name":null,"doc_comment":"here via a separate `cargo build --target wasm32-wasip2` invocation.\n(FIDIUS-T-0094). The fixture `tests/wasm-fixtures/macro-greeter` is built\nRuns only with `--features wasm` and requires the wasm component toolchain\n\ngenerated WIT, and the component's `fidius-interface-hash` all agree.\n**macro-emitted** `Greeter_WASM_DESCRIPTOR` — proving the descriptor, the\n`fidius-guest` split), and the host loads it through `load_wasm` using the\nit to a WASM component (FIDIUS-T-0106 auto-export, unblocked by the I-0022\nEnd-to-end: a Rust author defines an interface with the fidius macros, builds","visibility":"private"},{"name":"macro_descriptor_export_and_hash_are_self_consistent","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm.rs","start_line":121,"end_line":131,"signature":"()","qualified_name":null,"doc_comment":"here via a separate `cargo build --target wasm32-wasip2` invocation.\n(FIDIUS-T-0094). The fixture `tests/wasm-fixtures/macro-greeter` is built\nRuns only with `--features wasm` and requires the wasm component toolchain\n\ngenerated WIT, and the component's `fidius-interface-hash` all agree.\n**macro-emitted** `Greeter_WASM_DESCRIPTOR` — proving the descriptor, the\n`fidius-guest` split), and the host loads it through `load_wasm` using the\nit to a WASM component (FIDIUS-T-0106 auto-export, unblocked by the I-0022\nEnd-to-end: a Rust author defines an interface with the fidius macros, builds","visibility":"private"}],"crates/fidius-host/tests/macro_wasm_streaming.rs":[{"name":"Ticker","kind":"interface","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":37,"end_line":39,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"public"},{"name":"macro_ticker_component","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":41,"end_line":55,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":42,"end_line":42,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"},{"name":"stage_pkg","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":57,"end_line":79,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"},{"name":"macro_descriptor_marks_tick_streaming","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":82,"end_line":91,"signature":"()","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"},{"name":"macro_streaming_component_loads_and_streams","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":94,"end_line":116,"signature":"()","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"},{"name":"macro_streaming_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/macro_wasm_streaming.rs","start_line":119,"end_line":141,"signature":"()","qualified_name":null,"doc_comment":"Requires the wasm component toolchain (cargo + wasm32-wasip2).\n\nidentically to the hand-authored one — write a trait, get a streaming plugin.\ncodegen (FIDIUS-I-0026 WS.2) produces a component the host streams from\nand driven via `call_streaming`. Proves the `#[plugin_impl]` resource-adapter\ncomponent (`tests/wasm-fixtures/macro-ticker`) loaded through `PluginHost`\nWS.2/WS.4 capstone: a fidius **macro-generated** WASM server-streaming","visibility":"private"}],"crates/fidius-host/tests/multi_plugin_pipeline.rs":[{"name":"Reader","kind":"interface","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":29,"end_line":31,"signature":"{ fn read() }","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"public"},{"name":"Range","kind":"struct","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":32,"end_line":32,"signature":null,"qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"public"},{"name":"Range","kind":"type","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":34,"end_line":38,"signature":"impl Reader for Range","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"read","kind":"function","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":35,"end_line":37,"signature":"(&self, count: u32) -> fidius_core::Stream<u64>","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"Writer","kind":"interface","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":42,"end_line":44,"signature":"{ fn label() }","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"public"},{"name":"Labeler","kind":"struct","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":45,"end_line":45,"signature":null,"qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"public"},{"name":"Labeler","kind":"type","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":47,"end_line":51,"signature":"impl Writer for Labeler","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"label","kind":"function","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":48,"end_line":50,"signature":"(&self, value: u64) -> String","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"PluginSink","kind":"struct","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":57,"end_line":60,"signature":"{ writer: PluginHandle, out: std::sync::Mutex<Vec<String>> }","qualified_name":null,"doc_comment":"A `StreamSink` backed by a real plugin: each streamed item is fed to the\nwriter plugin, and its output collected.","visibility":"private"},{"name":"PluginSink","kind":"type","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":63,"end_line":73,"signature":"impl StreamSink for PluginSink","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"accept","kind":"function","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":64,"end_line":72,"signature":"(&self, item: Value) -> Result<(), CallError>","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"},{"name":"host_pipes_reader_stream_into_writer_plugin","kind":"function","file_path":"crates/fidius-host/tests/multi_plugin_pipeline.rs","start_line":76,"end_line":97,"signature":"()","qualified_name":null,"doc_comment":"framework's `pump` helper exists for.\nProves plugin-A.stream → host → plugin-B.input end to end — the composition the\ninto a SECOND plugin (a plugin-backed `StreamSink`), via `fidius_test::pump`.\nHost-orchestrated **multi-plugin pipeline**: the host wires one plugin's stream","visibility":"private"}],"crates/fidius-host/tests/package_e2e.rs":[{"name":"test_package_dir","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":23,"end_line":25,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"TestSchema","kind":"struct","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":28,"end_line":31,"signature":"{ category: String, description: String }","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"StrictSchema","kind":"struct","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":34,"end_line":38,"signature":"{ category: String, description: String, required_field: String }","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"load_manifest_with_schema","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":41,"end_line":51,"signature":"()","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"schema_mismatch_fails","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":54,"end_line":64,"signature":"()","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"build_and_load_package","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":67,"end_line":105,"signature":"()","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"AddInput","kind":"struct","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":94,"end_line":97,"signature":"{ a: i64, b: i64 }","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"AddOutput","kind":"struct","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":99,"end_line":101,"signature":"{ result: i64 }","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"discover_packages_finds_fixture","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":108,"end_line":122,"signature":"()","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"},{"name":"missing_manifest_returns_error","kind":"function","file_path":"crates/fidius-host/tests/package_e2e.rs","start_line":125,"end_line":129,"signature":"()","qualified_name":null,"doc_comment":"End-to-end package tests: validate, build, load, call.","visibility":"private"}],"crates/fidius-host/tests/plugin_dep_graph.rs":[{"name":"plugin_without_host_feature_does_not_pull_libloading","kind":"function","file_path":"crates/fidius-host/tests/plugin_dep_graph.rs","start_line":26,"end_line":65,"signature":"()","qualified_name":null,"doc_comment":"and asserts `libloading` is not in its dep graph.\nand doing so bloats the dylib. This test runs `cargo tree` on the plugin\ncdylib plugin has no reason to link the host loader (libloading, etc.)\n`test-plugin-smoke` depends on `fidius` without the `host` feature. A\n\nRegression test for the `fidius` crate's `host` feature gating.","visibility":"private"}],"crates/fidius-host/tests/python_plugin_e2e.rs":[{"name":"stage_plugin","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":51,"end_line":66,"signature":"(tmp: &tempfile::TempDir) -> PathBuf","qualified_name":null,"doc_comment":"Directory structure mirrors what a deployer would have:\n\n```\nplugins/\n└── py-byte-pipe/\n├── package.toml\n├── byte_pipe.py\n└── vendor/\n└── fidius/\n└── ...\n```","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":68,"end_line":75,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":77,"end_line":89,"signature":"(src: &std::path::Path, dst: &std::path::Path)","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"},{"name":"byte_pipe_descriptor","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":95,"end_line":97,"signature":"() -> &'static PythonInterfaceDescriptor","qualified_name":null,"doc_comment":"Produce the BytePipe descriptor from the Rust trait via the macro-emitted\ncompanion module — the same hash the Python plugin's `__interface_hash__`\nis derived from. Wrapping in a fn so the test gets a `'static` reference\nwithout exporting it from the test plugin crate.","visibility":"private"},{"name":"discover_lists_python_plugin_with_python_runtime","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":100,"end_line":114,"signature":"()","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"},{"name":"typed_method_round_trips","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":117,"end_line":130,"signature":"()","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"},{"name":"raw_wire_method_round_trips_2mb","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":133,"end_line":152,"signature":"()","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"},{"name":"tampered_interface_hash_is_rejected_at_load","kind":"function","file_path":"crates/fidius-host/tests/python_plugin_e2e.rs","start_line":155,"end_line":190,"signature":"()","qualified_name":null,"doc_comment":"5. A tampered `__interface_hash__` is rejected at load with a clear error.\nmethod round-trip correctly.\n4. Both the `#[wire(raw)]` `reverse` method and the typed `name`\nproduces — so the contract loop is closed end-to-end.\n`fidius python-stub`) matches the runtime constant the macro\n3. The hash compiled into `__interface_hash__` (generated via\ncdylib plugins, with `PluginInfo.runtime = Python`.\n2. `PluginHost::discover()` surfaces the python package alongside any\ntoolchain involved) produces a plugin the host loads transparently.\n1. A Python author shipping `.py` + `package.toml` + `vendor/` (no Rust\n\nWhat this test proves end-to-end:\n\nexercised through the standard `PluginHost` API.\ndefined in Rust (`tests/test-plugin-smoke/src/lib.rs`), loaded and\n(`tests/test-plugin-py-greeter`) implementing the `BytePipe` trait\nCapstone integration test: a real Python plugin package","visibility":"private"}],"crates/fidius-host/tests/python_routing.rs":[{"name":"HASH","kind":"variable","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":26,"end_line":26,"signature":": u64","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"METHODS","kind":"variable","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":27,"end_line":30,"signature":": [PythonMethodDesc; 1]","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"fresh_descriptor","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":32,"end_line":44,"signature":"() -> (&'static PythonInterfaceDescriptor, String)","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"COUNTER","kind":"variable","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":33,"end_line":33,"signature":": AtomicUsize","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":46,"end_line":58,"signature":"(src: &std::path::Path, dst: &std::path::Path)","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"make_python_package","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":60,"end_line":109,"signature":"(\n    plugins_root: &std::path::Path,\n    pkg_name: &str,\n    entry_module: &str,\n) -> PathBuf","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":111,"end_line":118,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"discover_surfaces_python_package","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":121,"end_line":138,"signature":"()","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"load_python_dispatches_through_host","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":141,"end_line":160,"signature":"()","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"load_python_unknown_name_returns_not_found","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":163,"end_line":180,"signature":"()","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"},{"name":"cdylib_load_path_unaffected","kind":"function","file_path":"crates/fidius-host/tests/python_routing.rs","start_line":183,"end_line":201,"signature":"()","qualified_name":null,"doc_comment":"when the `python` feature is enabled. Runs only with `--features python`.\nVerify `PluginHost` routes correctly between cdylib and python packages","visibility":"private"}],"crates/fidius-host/tests/python_streaming_e2e.rs":[{"name":"ticker_descriptor","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":41,"end_line":43,"signature":"() -> &'static PythonInterfaceDescriptor","qualified_name":null,"doc_comment":"The macro-generated descriptor for the `Ticker` interface — its\n`interface_hash` includes the `!stream` marker.","visibility":"private"},{"name":"stage","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":47,"end_line":66,"signature":"(tmp: &tempfile::TempDir) -> PathBuf","qualified_name":null,"doc_comment":"Stage the py-ticker package into a fresh temp dir, vendor the in-tree SDK,\nand inject the real interface hash in place of the `.py` placeholder.","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":68,"end_line":75,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":77,"end_line":89,"signature":"(src: &std::path::Path, dst: &std::path::Path)","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"tick_index","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":91,"end_line":93,"signature":"() -> usize","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"discover_lists_streaming_python_plugin","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":96,"end_line":107,"signature":"()","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"server_stream_yields_all_items","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":110,"end_line":130,"signature":"()","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"huge_stream_is_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":133,"end_line":159,"signature":"()","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"},{"name":"composition_pump_into_sink","kind":"function","file_path":"crates/fidius-host/tests/python_streaming_e2e.rs","start_line":162,"end_line":186,"signature":"()","qualified_name":null,"doc_comment":"- the `fidius-test` composition harness (`pump`) wires the stream to a sink.\nrunning it to completion (REQ-002/REQ-003/NFR-003);\nonly as fast as the host pulls, and dropping the stream stops it without\n- **bounded memory + backpressure + cancel** — a huge generator is consumed\n- **server-streaming** delivers each yielded item (REQ-001);\n- `PluginHandle::call_streaming` pumps the generator onto a `ChunkStream`;\n`__interface_hash__` (cross-language contract closes for streaming);\n- the macro's `!stream` interface hash matches the Python plugin's\n\nwhole Phase-1 stack end-to-end:\nloaded and driven through the standard `PluginHost` API. This validates the\nRust-defined `Ticker` interface (`-> fidius::Stream<u64>`) as a generator,\nA real Python plugin package (`tests/test-plugin-py-ticker`) implements the\n\nCapstone server-streaming integration test (FIDIUS-I-0026, ST.5).","visibility":"private"}],"crates/fidius-host/tests/records_wasm.rs":[{"name":"Point","kind":"struct","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":38,"end_line":41,"signature":"{ x: i32, y: i32 }","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"public"},{"name":"Shape","kind":"enum","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":44,"end_line":49,"signature":"Circle | Rect | Triangle | Dot","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"public"},{"name":"Geo","kind":"interface","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":52,"end_line":55,"signature":"{ fn midpoint(), fn describe() }","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"public"},{"name":"records_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":57,"end_line":71,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":58,"end_line":58,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"private"},{"name":"stage_pkg","kind":"function","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":73,"end_line":99,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"private"},{"name":"record_in_record_out_round_trips","kind":"function","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":102,"end_line":118,"signature":"()","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"private"},{"name":"variant_in_round_trips_all_cases","kind":"function","file_path":"crates/fidius-host/tests/records_wasm.rs","start_line":121,"end_line":149,"signature":"()","qualified_name":null,"doc_comment":"exercising the kebab↔snake/Pascal name normalization end to end.\ndescriptor and round-trips records/variants through the `Value` boundary —\nconversions); the host loads it via `load_wasm` against the macro-emitted\n`records-greeter` fixture is built (its build.rs generates wit/ + the\ninterface passing a `record` (Point) and a `variant` (Shape). The\nEnd-to-end for user-defined types in a WASM plugin (FIDIUS-I-0023): an","visibility":"private"}],"crates/fidius-host/tests/wasm_egress_e2e.rs":[{"name":"IFACE","kind":"variable","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":38,"end_line":38,"signature":": &str","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"fetcher_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":40,"end_line":44,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"mock_http_once","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":48,"end_line":66,"signature":"(body: &'static str) -> (String, std::thread::JoinHandle<()>)","qualified_name":null,"doc_comment":"One-shot mock HTTP server on an ephemeral loopback port; serves a single\nrequest with `body`. Returns the base URL + the server thread handle.","visibility":"private"},{"name":"AllowAll","kind":"struct","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":69,"end_line":69,"signature":null,"qualified_name":null,"doc_comment":"Reference embedder policy: allow everything (the test's loopback grant).","visibility":"private"},{"name":"AllowAll","kind":"type","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":70,"end_line":74,"signature":"impl EgressPolicy for AllowAll","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"authorize","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":71,"end_line":73,"signature":"(&self, _parts: &mut http::request::Parts) -> Result<(), EgressDenied>","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"DenyAll","kind":"struct","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":77,"end_line":77,"signature":null,"qualified_name":null,"doc_comment":"Reference embedder policy: deny everything.","visibility":"private"},{"name":"DenyAll","kind":"type","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":78,"end_line":82,"signature":"impl EgressPolicy for DenyAll","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"authorize","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":79,"end_line":81,"signature":"(&self, _parts: &mut http::request::Parts) -> Result<(), EgressDenied>","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"load","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":84,"end_line":112,"signature":"(\n    caps: Vec<String>,\n    egress: Option<Arc<dyn EgressPolicy>>,\n) -> Result<PluginHandle, CallError>","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"egress_allowed_fetches_body","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":115,"end_line":125,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"egress_denied_by_policy","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":128,"end_line":142,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"no_policy_fails_closed","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":145,"end_line":157,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"no_capability_fails_closed","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":160,"end_line":171,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"FETCHER_METHODS","kind":"variable","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":177,"end_line":181,"signature":": [WasmMethodDesc; 1]","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"FETCHER","kind":"variable","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":182,"end_line":187,"signature":": WasmInterfaceDescriptor","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"stage_fetcher_pkg","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":190,"end_line":201,"signature":"(root: &std::path::Path)","qualified_name":null,"doc_comment":"Stage the fetcher as a loadable wasm package declaring the `http` capability.","visibility":"private"},{"name":"egress_via_builder_default_policy","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":204,"end_line":222,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"egress_via_per_plugin_policy","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":225,"end_line":244,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"load_wasm_without_egress_fails_closed","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":247,"end_line":265,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"},{"name":"egress_via_builder_arc_dyn_policy","kind":"function","file_path":"crates/fidius-host/tests/wasm_egress_e2e.rs","start_line":268,"end_line":287,"signature":"()","qualified_name":null,"doc_comment":"embedder writes — fidius ships none of this (mechanism, not policy).\nThe reference allow/deny policies here are exactly what the docs say an\n\n- no policy / no capability → `wasi:http` isn't linked → fails closed.\n- denied   → the policy refuses before dispatch; the guest gets an error;\n- allowed  → the guest fetches the mock server's body;\nevery request through the embedder's `EgressPolicy`:\nand makes an outbound GET. The host enforces the **two-key** gate and routes\ncomponent (`tests/wasm-fixtures/fetcher`) imports `wasi:http/outgoing-handler`\nCapability-gated `wasi:http` egress E2E (FIDIUS-I-0027 / E2). A real WASM","visibility":"private"}],"crates/fidius-host/tests/wasm_executor.rs":[{"name":"IFACE","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":32,"end_line":32,"signature":": &str","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"EXPECTED_HASH","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":33,"end_line":33,"signature":": u64","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"greeter_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":36,"end_line":50,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"Build the greeter component once (process-wide cache) and return its bytes.","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":37,"end_line":37,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"executor_with","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":52,"end_line":92,"signature":"(capabilities: Vec<String>) -> WasmComponentExecutor","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"executor","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":94,"end_line":96,"signature":"() -> WasmComponentExecutor","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"interface_hash_matches","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":99,"end_line":101,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"typed_call_greet","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":104,"end_line":111,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"typed_call_add_ok_and_err","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":114,"end_line":129,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"raw_call_echo_bytes_reverses","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":132,"end_line":136,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"method_count_and_info","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":139,"end_line":143,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"METHOD_DESCS","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":147,"end_line":168,"signature":": [WasmMethodDesc; 4]","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"GREETER_DESC","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":169,"end_line":174,"signature":": WasmInterfaceDescriptor","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"stage_wasm_package","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":178,"end_line":212,"signature":"(root: &std::path::Path, capabilities: &[&str])","qualified_name":null,"doc_comment":"Stage a `runtime = \"wasm\"` package directory containing the built component,\nwith the given `[wasm].capabilities` allow-list.","visibility":"private"},{"name":"load_wasm_through_host_and_call","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":215,"end_line":234,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"load_wasm_rejects_interface_hash_mismatch","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":237,"end_line":259,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"BAD_DESC","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":238,"end_line":243,"signature":": WasmInterfaceDescriptor","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"discover_surfaces_wasm_package","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":262,"end_line":275,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"PROBE_ENV","kind":"variable","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":279,"end_line":279,"signature":": usize","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"env_capability_denied_by_default","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":282,"end_line":295,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"env_capability_granted_via_allowlist","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":298,"end_line":313,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"bare_env_capability_rejected","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":316,"end_line":333,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"scoped_env_does_not_leak_other_vars","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":336,"end_line":352,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"python_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":360,"end_line":364,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"The Python-authored component, if it's been built (see\n`tests/wasm-fixtures/greeter-py/build.sh`). Returns `None` when absent so\nthe test skips cleanly where `componentize-py` isn't available (e.g. CI\nwithout the toolchain) rather than failing.","visibility":"private"},{"name":"polyglot_python_guest_behaves_identically","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":370,"end_line":419,"signature":"()","qualified_name":null,"doc_comment":"A Python guest implementing the SAME `greeter` WIT is loaded and called\nthrough the identical `PluginHost::load_wasm` path as the Rust guest — the\nconcrete evidence that Path B delivers language-agnostic plugins.","visibility":"private"},{"name":"unknown_capability_rejected_at_load","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":422,"end_line":437,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"set_precompiled","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":442,"end_line":452,"signature":"(pkg_dir: &std::path::Path, cwasm: &str)","qualified_name":null,"doc_comment":"Record `precompiled = \"<name>\"` under `[wasm]` in a staged package.toml.","visibility":"private"},{"name":"precompiled_cwasm_loads_via_aot_and_calls","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":455,"end_line":474,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"stale_cwasm_falls_back_to_jit","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":477,"end_line":495,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"pack_unpack_load_roundtrip","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":498,"end_line":521,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"sign_pkg","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":527,"end_line":534,"signature":"(pkg_dir: &std::path::Path) -> ed25519_dalek::VerifyingKey","qualified_name":null,"doc_comment":"Sign a staged package dir over its `package_digest` (the same scheme\n`fidius package sign` uses) and return the verifying key.","visibility":"private"},{"name":"signed_wasm_package_loads_when_signature_required","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":537,"end_line":553,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"unsigned_wasm_package_rejected_when_signature_required","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":556,"end_line":575,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"tampered_wasm_package_fails_verification","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":578,"end_line":601,"signature":"()","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"js_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":605,"end_line":609,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"polyglot_js_guest_behaves_identically","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":615,"end_line":662,"signature":"()","qualified_name":null,"doc_comment":"A JavaScript guest (jco/ComponentizeJS) implementing the SAME `greeter` WIT\nloads and is called through the identical `PluginHost::load_wasm` path as the\nRust and Python guests — the third language, same host, same descriptor.","visibility":"private"},{"name":"go_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":666,"end_line":670,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"polyglot_go_guest_behaves_identically","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":676,"end_line":722,"signature":"()","qualified_name":null,"doc_comment":"A Go guest (TinyGo + wit-bindgen-go) implementing the SAME `greeter` WIT loads\nand is called through the identical host path as the Rust, Python, and\nJavaScript guests — a fourth language, same host, same descriptor.","visibility":"private"},{"name":"c_greeter_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":726,"end_line":730,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"`--features wasm` and requires the component toolchain (FIDIUS-T-0094).\nand drives it through `WasmComponentExecutor` directly. Runs only with\nreference greeter component (tests/wasm-fixtures/greeter) via cargo-component\nRound-trip tests for the WASM component backend (FIDIUS-T-0102). Builds the","visibility":"private"},{"name":"polyglot_c_guest_behaves_identically","kind":"function","file_path":"crates/fidius-host/tests/wasm_executor.rs","start_line":736,"end_line":782,"signature":"()","qualified_name":null,"doc_comment":"A C guest (wit-bindgen + wasi-sdk clang) implementing the SAME `greeter` WIT\nloads and is called through the identical host path as the Rust, Python,\nJavaScript, and Go guests — a fifth language, same host, same descriptor.","visibility":"private"}],"crates/fidius-host/tests/wasm_streaming_e2e.rs":[{"name":"IFACE","kind":"variable","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":33,"end_line":33,"signature":": &str","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"HASH","kind":"variable","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":35,"end_line":35,"signature":": u64","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"ticker_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":37,"end_line":51,"signature":"() -> &'static [u8]","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"BYTES","kind":"variable","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":38,"end_line":38,"signature":": OnceLock<Vec<u8>>","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"handle","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":53,"end_line":77,"signature":"() -> PluginHandle","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"wasm_stream_yields_all_items","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":80,"end_line":91,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"wasm_huge_stream_is_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":94,"end_line":107,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"wasm_empty_stream","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":110,"end_line":117,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"wasm_composition_pump_into_sink","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":124,"end_line":140,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"ticker_js_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":148,"end_line":152,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"js_handle","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":154,"end_line":178,"signature":"(bytes: &[u8]) -> PluginHandle","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_js_guest_streams","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":181,"end_line":200,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_js_guest_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":203,"end_line":219,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"ticker_py_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":226,"end_line":230,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"py_wasm_handle","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":232,"end_line":256,"signature":"(bytes: &[u8]) -> PluginHandle","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_py_wasm_guest_streams","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":259,"end_line":277,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_py_wasm_guest_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":280,"end_line":296,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"ticker_c_component","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":303,"end_line":307,"signature":"() -> Option<Vec<u8>>","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"c_wasm_handle","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":309,"end_line":333,"signature":"(bytes: &[u8]) -> PluginHandle","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_c_wasm_guest_streams","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":336,"end_line":354,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"},{"name":"polyglot_c_wasm_guest_bounded_and_cancellable","kind":"function","file_path":"crates/fidius-host/tests/wasm_streaming_e2e.rs","start_line":357,"end_line":373,"signature":"()","qualified_name":null,"doc_comment":"under the sandbox. Requires the component toolchain (cargo-component).\nbounded memory + backpressure + cancel (huge stream, pull-few-then-drop), all\nit through `PluginHandle::call_streaming` → `ChunkStream`. Proves items,\n(`tests/wasm-fixtures/ticker`) exports a streaming `resource`; the host drives\nWASM server-streaming E2E (FIDIUS-I-0026 Phase 2). A real Rust component","visibility":"private"}],"crates/fidius-macro/src/impl_macro.rs":[{"name":"MethodInfo","kind":"struct","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":31,"end_line":51,"signature":"{ name: &'a Ident, is_async: bool, returns_result: bool, arg_types: Vec<&'a Type>, arg_names: Vec<Ident>, wire_raw: bool...","qualified_name":null,"doc_comment":"Info about an impl method, extracted from the impl block.","visibility":"private"},{"name":"impl_method_is_raw","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":56,"end_line":73,"signature":"(attrs: &[syn::Attribute]) -> syn::Result<bool>","qualified_name":null,"doc_comment":"Detect a `#[wire(raw)]` attribute on an impl-side method. Mirrors the\ntrait-side parser in `ir.rs` — an explicit `raw` keyword is the only\nsupported form.","visibility":"private"},{"name":"kebab_to_pascal","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":78,"end_line":88,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"kebab-case → PascalCase, for deriving the wit-bindgen resource type name from\na method name: `tick` → resource `tick-stream` → `TickStream` (trait\n`GuestTickStream`). (FIDIUS-I-0026.)","visibility":"private"},{"name":"is_result_type","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":91,"end_line":102,"signature":"(ty: &Type) -> bool","qualified_name":null,"doc_comment":"Check if a return type looks like `Result<T, ...>`.","visibility":"private"},{"name":"PluginImplAttrs","kind":"struct","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":106,"end_line":120,"signature":"{ trait_name: Ident, crate_path: Path, buffer_strategy: BufferStrategyAttr, config: Option<Path> }","qualified_name":null,"doc_comment":"Arguments to `#[plugin_impl(TraitName)]`, `#[plugin_impl(TraitName, crate = \"...\")]`,\nor `#[plugin_impl(TraitName, buffer = Arena)]`.","visibility":"public"},{"name":"PluginImplAttrs","kind":"type","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":122,"end_line":177,"signature":"impl Parse for PluginImplAttrs","qualified_name":null,"doc_comment":"dylibs, the FIDIUS_PLUGIN_REGISTRY.\na populated vtable static, a PluginDescriptor static, and for single-plugin\nGenerates: the original impl, extern \"C\" FFI shims, a static instance,\n\nCode generation for `#[plugin_impl(TraitName)]`.","visibility":"private"},{"name":"parse","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":123,"end_line":176,"signature":"(input: ParseStream) -> syn::Result<Self>","qualified_name":null,"doc_comment":"dylibs, the FIDIUS_PLUGIN_REGISTRY.\na populated vtable static, a PluginDescriptor static, and for single-plugin\nGenerates: the original impl, extern \"C\" FFI shims, a static instance,\n\nCode generation for `#[plugin_impl(TraitName)]`.","visibility":"private"},{"name":"generate_plugin_impl","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":180,"end_line":336,"signature":"(attrs: &PluginImplAttrs, item: &ItemImpl) -> syn::Result<TokenStream>","qualified_name":null,"doc_comment":"Generate all code for a `#[plugin_impl(TraitName)]` invocation.","visibility":"public"},{"name":"generate_wasm_adapter","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":346,"end_line":640,"signature":"(\n    trait_name: &Ident,\n    instance_name: &Ident,\n    methods: &[MethodInfo],\n    config: Option<&Path>,\n    crate_path: &Path,\n    impl_type: &Type,\n) -> TokenStream","qualified_name":null,"doc_comment":"Generate the WASM component auto-export adapter for `#[plugin_impl]`.\n\nEmits, under `#[cfg(target_family = \"wasm\")]`, a `wit_bindgen::generate!`\ninvocation (inline WIT generated from the method signatures) plus a `Guest`\nimpl that forwards to the plugin's static instance, and `export!`. If any\nmethod has a reference argument or a type outside the supported set, it\ninstead emits a wasm-gated `compile_error!` (a clear failure on wasm builds;\na no-op on cdylib/Python builds where the whole adapter is cfg'd out).","visibility":"private"},{"name":"collect_user_idents","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":644,"end_line":689,"signature":"(ty: &Type, out: &mut std::collections::BTreeSet<String>)","qualified_name":null,"doc_comment":"Collect candidate user-type idents (non-primitive path leaves) from a type,\ndescending through `Vec`/`Option`/`Box` and `Result`'s ok type.","visibility":"private"},{"name":"gen_type","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":694,"end_line":720,"signature":"(ty: &Type, known: &std::collections::BTreeSet<String>, pkg_seg: &Ident) -> TokenStream","qualified_name":null,"doc_comment":"The wit-bindgen-generated type for an author type: identity for types holding\nno user type (wit-bindgen uses the same Rust type), else the generated path\n`exports::fidius::<iface>::<iface>::<T>`, recursing through `Vec`/`Option`.","visibility":"private"},{"name":"wasm_first_generic","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":722,"end_line":731,"signature":"(seg: &syn::PathSegment) -> Option<&Type>","qualified_name":null,"doc_comment":"dylibs, the FIDIUS_PLUGIN_REGISTRY.\na populated vtable static, a PluginDescriptor static, and for single-plugin\nGenerates: the original impl, extern \"C\" FFI shims, a static instance,\n\nCode generation for `#[plugin_impl(TraitName)]`.","visibility":"private"},{"name":"wasm_unsupported","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":737,"end_line":747,"signature":"(method: &Ident, reason: &str) -> TokenStream","qualified_name":null,"doc_comment":"Emit a `#[cfg(target_family = \"wasm\")]`-gated `compile_error!` for a method\nthe WASM auto-export can't handle. On native (cdylib/Python) builds the cfg\nis false, so this is a no-op there; on a wasm build it fails the compile with\na clear message instead of silently producing a component that exports nothing.","visibility":"private"},{"name":"generate_shims","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":751,"end_line":1064,"signature":"(\n    impl_ident: &Ident,\n    methods: &[MethodInfo],\n    crate_path: &Path,\n    buffer_strategy: BufferStrategyAttr,\n) -> TokenStream","qualified_name":null,"doc_comment":"Generate extern \"C\" shim functions for each method. Shim signatures and\nbodies vary by buffer strategy — see the two emit paths below.","visibility":"private"},{"name":"generate_vtable_static","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":1070,"end_line":1092,"signature":"(\n    trait_name: &Ident,\n    impl_ident: &Ident,\n    methods: &[&Ident],\n) -> TokenStream","qualified_name":null,"doc_comment":"Generate the static vtable with function pointers.\n\nUses the `new_{trait}` constructor generated by `#[plugin_interface]`,\nwhich knows which fields are optional (Option<fn>) vs required (fn).","visibility":"private"},{"name":"generate_descriptor","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":1095,"end_line":1221,"signature":"(\n    trait_name: &Ident,\n    impl_ident: &Ident,\n    methods: &[&Ident],\n    crate_path: &Path,\n    buffer_strategy: BufferStrategyAttr,\n    config: Option<&Path>,\n) -> TokenStream","qualified_name":null,"doc_comment":"Generate the PluginDescriptor static.","visibility":"private"},{"name":"generate_inventory_registration","kind":"function","file_path":"crates/fidius-macro/src/impl_macro.rs","start_line":1224,"end_line":1235,"signature":"(impl_ident: &Ident, crate_path: &Path) -> TokenStream","qualified_name":null,"doc_comment":"Register the descriptor via inventory for multi-plugin support.","visibility":"private"}],"crates/fidius-macro/src/interface.rs":[{"name":"strip_optional_attrs","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":29,"end_line":45,"signature":"(item: &ItemTrait) -> ItemTrait","qualified_name":null,"doc_comment":"Strip fidius-specific helper attributes (`#[optional]`, `#[method_meta]`,\n`#[trait_meta]`) from the trait and its methods so the emitted trait\ncompiles as a plain Rust trait definition.","visibility":"private"},{"name":"is_fidius_helper","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":30,"end_line":35,"signature":"(attr: &syn::Attribute) -> bool","qualified_name":null,"doc_comment":"capability bit constants, version/strategy constants, and a descriptor builder function.\nGenerates: the original trait, a `#[repr(C)]` vtable struct, interface hash constant,\n\nCode generation for `#[plugin_interface]`.","visibility":"private"},{"name":"generate_interface","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":48,"end_line":81,"signature":"(ir: &InterfaceIR) -> syn::Result<TokenStream>","qualified_name":null,"doc_comment":"Generate all code for a `#[plugin_interface]` invocation.","visibility":"public"},{"name":"generate_metadata","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":92,"end_line":190,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Emit the static metadata arrays for `#[method_meta]` and `#[trait_meta]`\nattributes into the companion module. Emits:\n\n- `__FIDIUS_METHOD_META_<NAME>: [MetaKv; N]` for each method with metadata\n- `__FIDIUS_METHOD_META_TABLE: [MethodMetaEntry; method_count]` if any method has metadata\n- `__FIDIUS_TRAIT_META: [MetaKv; N]` if any trait-level metadata exists\n\nIf no metadata is declared anywhere, emits nothing — the descriptor\nbuilder falls back to null pointers.","visibility":"private"},{"name":"generate_vtable","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":193,"end_line":272,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Generate the `#[repr(C)]` vtable struct.","visibility":"private"},{"name":"generate_constants","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":275,"end_line":408,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Generate interface hash, capability bit constants, version, and buffer strategy constants.","visibility":"private"},{"name":"generate_descriptor_builder","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":411,"end_line":485,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Generate the descriptor builder function used by `#[plugin_impl]`.","visibility":"private"},{"name":"generate_method_indices","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":488,"end_line":504,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Generate method index constants.","visibility":"private"},{"name":"generate_client","kind":"function","file_path":"crates/fidius-macro/src/interface.rs","start_line":518,"end_line":663,"signature":"(ir: &InterfaceIR) -> TokenStream","qualified_name":null,"doc_comment":"Generate a typed `{Trait}Client` struct that wraps a `PluginHandle` and\nexposes named methods matching the trait signature. Eliminates raw\n`call_method(index, ...)` boilerplate at host call sites.\n\nEmission is gated with `#[cfg(feature = \"host\")]` so plugin cdylibs that\ndon't enable the `host` feature pay zero cost (no `fidius-host`, no\n`libloading` in the dep tree). Host applications enable the feature on\nthe interface crate to receive the generated Client type.\n\nUniform tuple encoding matches the plugin-side shim (see\n`impl_macro.rs::generate_shims`): args are always serialized as a tuple\n`(arg1, arg2, ...)` — for zero args this is `()`; for one arg `(arg,)`.","visibility":"private"}],"crates/fidius-macro/src/ir.rs":[{"name":"InterfaceAttrs","kind":"struct","file_path":"crates/fidius-macro/src/ir.rs","start_line":30,"end_line":36,"signature":"{ version: u32, buffer_strategy: BufferStrategyAttr, crate_path: Path }","qualified_name":null,"doc_comment":"Parsed attributes from `#[plugin_interface(version = N, buffer = Strategy)]`.","visibility":"public"},{"name":"BufferStrategyAttr","kind":"enum","file_path":"crates/fidius-macro/src/ir.rs","start_line":43,"end_line":46,"signature":"PluginAllocated | Arena","qualified_name":null,"doc_comment":"Discriminants match `fidius_core::descriptor::BufferStrategyKind` — values\n`1` (PluginAllocated) and `2` (Arena). `0` is reserved for the removed\n`CallerAllocated` strategy.","visibility":"public"},{"name":"InterfaceAttrs","kind":"type","file_path":"crates/fidius-macro/src/ir.rs","start_line":48,"end_line":120,"signature":"impl Parse for InterfaceAttrs","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"parse","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":49,"end_line":119,"signature":"(input: ParseStream) -> syn::Result<Self>","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"MetaKvAttr","kind":"struct","file_path":"crates/fidius-macro/src/ir.rs","start_line":125,"end_line":128,"signature":"{ key: String, value: String }","qualified_name":null,"doc_comment":"A static metadata key/value pair parsed from a `#[method_meta(...)]`\nor `#[trait_meta(...)]` attribute. Both values are string literals.","visibility":"public"},{"name":"InterfaceIR","kind":"struct","file_path":"crates/fidius-macro/src/ir.rs","start_line":132,"end_line":140,"signature":"{ trait_name: Ident, attrs: InterfaceAttrs, methods: Vec<MethodIR>, trait_metas: Vec<MetaKvAttr>, original_trait: ItemTr...","qualified_name":null,"doc_comment":"Full IR for a parsed interface trait.","visibility":"public"},{"name":"MethodIR","kind":"struct","file_path":"crates/fidius-macro/src/ir.rs","start_line":145,"end_line":180,"signature":"{ name: Ident, arg_types: Vec<Type>, arg_names: Vec<Ident>, return_type: Option<Type>, is_async: bool, optional_since: O...","qualified_name":null,"doc_comment":"IR for a single trait method.","visibility":"public"},{"name":"MethodIR","kind":"type","file_path":"crates/fidius-macro/src/ir.rs","start_line":182,"end_line":187,"signature":"= MethodIR","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"is_required","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":184,"end_line":186,"signature":"(&self) -> bool","qualified_name":null,"doc_comment":"Whether this is a required (non-optional) method.","visibility":"public"},{"name":"parse_meta_attrs","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":193,"end_line":237,"signature":"(attrs: &[Attribute], ident: &str) -> syn::Result<Vec<MetaKvAttr>>","qualified_name":null,"doc_comment":"Parse all `#[method_meta(\"k\", \"v\")]` or `#[trait_meta(\"k\", \"v\")]`\nattributes with the given name from an attribute list into a `Vec<MetaKvAttr>`.\nValidates string-literal only, non-empty keys, no duplicate keys, and\nrejects keys in the reserved `fidius.*` namespace.","visibility":"private"},{"name":"parse_optional_attr","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":240,"end_line":258,"signature":"(attrs: &[Attribute]) -> syn::Result<Option<u32>>","qualified_name":null,"doc_comment":"Parse an `#[optional(since = N)]` attribute, if present.","visibility":"private"},{"name":"parse_wire_attr","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":263,"end_line":280,"signature":"(attrs: &[Attribute]) -> syn::Result<bool>","qualified_name":null,"doc_comment":"Parse a `#[wire(raw)]` attribute, if present. Returns `true` when raw wire\nmode is opted in, `false` otherwise. Any other `wire(...)` form is a\ncompile-time error.","visibility":"private"},{"name":"is_vec_u8","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":283,"end_line":310,"signature":"(ty: &Type) -> bool","qualified_name":null,"doc_comment":"Return `true` if the given type is `Vec<u8>`.","visibility":"private"},{"name":"result_ok_type","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":313,"end_line":329,"signature":"(ty: &Type) -> Option<&Type>","qualified_name":null,"doc_comment":"Extract the first type parameter of `Result<_, _>`, if `ty` is a Result.","visibility":"private"},{"name":"validate_raw_method_signature","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":334,"end_line":371,"signature":"(\n    method: &TraitItemFn,\n    arg_types: &[Type],\n    return_type: Option<&Type>,\n) -> syn::Result<()>","qualified_name":null,"doc_comment":"Validate that a method flagged `#[wire(raw)]` has a supported signature:\nexactly one `Vec<u8>` argument, and returns either `Vec<u8>` or\n`Result<Vec<u8>, _>`. Returns a helpful error otherwise.","visibility":"private"},{"name":"stream_item_type","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":378,"end_line":396,"signature":"(ty: &Type) -> Option<Type>","qualified_name":null,"doc_comment":"Return the per-item type `T` if `ty` is a `Stream<T>` (i.e. its final path\nsegment is `Stream` with exactly one angle-bracketed type argument). Matches\n`fidius::Stream<T>`, `crate::fidius::Stream<T>`, or a bare `Stream<T>` — the\ndetection keys on the segment name, since the marker is written explicitly\n(FIDIUS-I-0026, D4). Returns `None` for any other type.","visibility":"private"},{"name":"build_signature_string","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":408,"end_line":435,"signature":"(\n    method: &TraitItemFn,\n    wire_raw: bool,\n    stream_item: Option<&Type>,\n) -> String","qualified_name":null,"doc_comment":"Build the canonical signature string for a method.\n\nDelegates the format to `fidius_core::hash::signature_string` so the\nproc macro and any other tooling (e.g. `fidius python-stub`) share one\nsource of truth. The `!raw` (raw-wire) and `!stream` (server-streaming)\nmarkers are part of that shared format.\n\nFor a streaming method (`-> fidius::Stream<T>`) the canonical return type is\nthe per-item type `T` (passed in `stream_item`), plus the `!stream` marker —\nso `read:->Row!stream` hashes distinctly from a unary `read:->Row`.","visibility":"private"},{"name":"extract_arg_names","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":438,"end_line":455,"signature":"(method: &TraitItemFn) -> Vec<Ident>","qualified_name":null,"doc_comment":"Extract argument names from a method signature (excluding `self`).","visibility":"private"},{"name":"extract_arg_types","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":458,"end_line":468,"signature":"(method: &TraitItemFn) -> Vec<Type>","qualified_name":null,"doc_comment":"Extract argument types from a method signature (excluding `self`).","visibility":"private"},{"name":"extract_return_type","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":471,"end_line":476,"signature":"(method: &TraitItemFn) -> Option<Type>","qualified_name":null,"doc_comment":"Extract the return type (unwrapped from `-> Type`).","visibility":"private"},{"name":"parse_interface","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":479,"end_line":572,"signature":"(attrs: InterfaceAttrs, item: &ItemTrait) -> syn::Result<InterfaceIR>","qualified_name":null,"doc_comment":"Parse an `ItemTrait` into an `InterfaceIR`.","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-macro/src/ir.rs","start_line":575,"end_line":763,"signature":null,"qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"parse_test_trait","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":579,"end_line":587,"signature":"(tokens: proc_macro2::TokenStream) -> InterfaceIR","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"basic_trait_parsing","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":590,"end_line":607,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"optional_method_parsing","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":610,"end_line":623,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"async_method_detection","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":626,"end_line":636,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"rejects_mut_self","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":639,"end_line":655,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"signature_string_format","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":658,"end_line":668,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"interface_attrs_parsing","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":671,"end_line":677,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"interface_attrs_with_crate_path","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":680,"end_line":693,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"detects_server_streaming_return","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":696,"end_line":715,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"streaming_and_unary_hash_differently","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":718,"end_line":732,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"bare_stream_marker_is_detected","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":735,"end_line":742,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"},{"name":"rejects_stream_in_argument_position","kind":"function","file_path":"crates/fidius-macro/src/ir.rs","start_line":745,"end_line":762,"signature":"()","qualified_name":null,"doc_comment":"Both `#[plugin_interface]` and `#[plugin_impl]` consume this IR.\n\nIntermediate representation for parsed plugin interface traits.","visibility":"private"}],"crates/fidius-macro/src/lib.rs":[{"name":"impl_macro","kind":"module","file_path":"crates/fidius-macro/src/lib.rs","start_line":15,"end_line":15,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"interface","kind":"module","file_path":"crates/fidius-macro/src/lib.rs","start_line":16,"end_line":16,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"ir","kind":"module","file_path":"crates/fidius-macro/src/lib.rs","start_line":17,"end_line":17,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"wit","kind":"module","file_path":"crates/fidius-macro/src/lib.rs","start_line":18,"end_line":18,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"plugin_interface","kind":"function","file_path":"crates/fidius-macro/src/lib.rs","start_line":43,"end_line":54,"signature":"(attr: TokenStream, item: TokenStream) -> TokenStream","qualified_name":null,"doc_comment":"Define a plugin interface from a trait.\n\nGenerates a `#[repr(C)]` vtable struct, interface hash constant,\ncapability bit constants, and a descriptor builder function.\n\n# Example\n\n```ignore\n#[plugin_interface(version = 1, buffer = PluginAllocated)]\npub trait Greeter: Send + Sync {\nfn greet(&self, name: String) -> String;\n\n#[optional(since = 2)]\nfn greet_fancy(&self, name: String) -> String;\n}\n```","visibility":"public"},{"name":"plugin_impl","kind":"function","file_path":"crates/fidius-macro/src/lib.rs","start_line":74,"end_line":82,"signature":"(attr: TokenStream, item: TokenStream) -> TokenStream","qualified_name":null,"doc_comment":"Implement a plugin interface for a concrete type.\n\nGenerates extern \"C\" FFI shims, a static vtable, a plugin descriptor,\nand a plugin registry.\n\n# Example\n\n```ignore\npub struct MyGreeter;\n\n#[plugin_impl(Greeter)]\nimpl Greeter for MyGreeter {\nfn greet(&self, name: String) -> String {\nformat!(\"Hello, {name}!\")\n}\n}\n```","visibility":"public"},{"name":"derive_wit_type","kind":"function","file_path":"crates/fidius-macro/src/lib.rs","start_line":98,"end_line":102,"signature":"(_item: TokenStream) -> TokenStream","qualified_name":null,"doc_comment":"Mark a `struct`/`enum` as usable in a WASM plugin interface (FIDIUS-I-0023).\n\nThis is a **marker** derive: it emits no code. The `fidius wit` generator\n(run from `build.rs`) keys on the `#[derive(WitType)]` attribute when it\nparses the crate source, mapping the struct to a WIT `record` (named fields)\nor the enum to a WIT `variant` (unit / single-field cases) and emitting the\ngenerated↔author conversions the wasm adapter uses. The same type continues\nto cross the cdylib/Python boundary via serde, unchanged.\n\n```ignore\n#[derive(WitType, serde::Serialize, serde::Deserialize, Clone)]\npub struct Point { pub x: i32, pub y: i32 }\n```","visibility":"public"}],"crates/fidius-macro/src/wit.rs":[{"name":"to_kebab_case","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":32,"end_line":47,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"Convert a Rust identifier (CamelCase or snake_case) to kebab-case, the WIT\nnaming convention. `BytePipe` → `byte-pipe`, `echo_bytes` → `echo-bytes`.","visibility":"crate"},{"name":"result_ok_type","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":50,"end_line":59,"signature":"(ty: &Type) -> Option<&Type>","qualified_name":null,"doc_comment":"Extract the `T` from `Result<T, _>`, if `ty` is a `Result`.","visibility":"crate"},{"name":"WitMethod","kind":"struct","file_path":"crates/fidius-macro/src/wit.rs","start_line":62,"end_line":69,"signature":"{ name: String, params: Vec<(String, String)>, ret: Option<String> }","qualified_name":null,"doc_comment":"One method projected to WIT (already-mapped strings).","visibility":"crate"},{"name":"render_wit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":75,"end_line":102,"signature":"(iface_kebab: &str, methods: &[WitMethod]) -> String","qualified_name":null,"doc_comment":"Render a complete `.wit` document for an interface and its methods. Mirrors\nthe hand-authored reference (`tests/wasm-fixtures/greeter/wit/world.wit`):\na `plugin-error` record, the interface's funcs, the `fidius-interface-hash`\ncarrier, and a `<iface>-plugin` world exporting the interface.","visibility":"crate"},{"name":"rust_type_to_wit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":106,"end_line":169,"signature":"(ty: &Type) -> Result<String, String>","qualified_name":null,"doc_comment":"Map a Rust argument/return type to its WIT spelling. Returns `Err(msg)` for\ntypes outside the supported set.","visibility":"crate"},{"name":"return_to_wit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":174,"end_line":193,"signature":"(ret: Option<&Type>) -> Result<Option<String>, String>","qualified_name":null,"doc_comment":"Map a method's return type to an optional WIT return. `Result<T, PluginError>`\n→ `result<T, plugin-error>` (or `result<_, plugin-error>` for `T = ()`);\n`()`/none → no return.","visibility":"crate"},{"name":"is_unit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":195,"end_line":197,"signature":"(ty: &Type) -> bool","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"path_is","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":199,"end_line":205,"signature":"(p: &syn::TypePath, name: &str) -> bool","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"single_generic","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":207,"end_line":209,"signature":"(seg: &'a syn::PathSegment, what: &str) -> Result<&'a Type, String>","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"first_generic","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":211,"end_line":220,"signature":"(seg: &syn::PathSegment) -> Option<&Type>","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-macro/src/wit.rs","start_line":223,"end_line":296,"signature":null,"qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"wit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":226,"end_line":228,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"ret","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":229,"end_line":231,"signature":"(s: &str) -> Option<String>","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"primitives_and_strings","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":234,"end_line":242,"signature":"()","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"containers","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":245,"end_line":251,"signature":"()","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"returns","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":254,"end_line":266,"signature":"()","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"unsupported_is_error","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":269,"end_line":271,"signature":"()","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"},{"name":"renders_greeter_like_wit","kind":"function","file_path":"crates/fidius-macro/src/wit.rs","start_line":274,"end_line":295,"signature":"()","qualified_name":null,"doc_comment":"a clear compile error rather than silently-wrong WIT.\nrecords/variants need a future `#[derive(WitType)]`; an unsupported type is\n`Vec<T>`→`list<T>`, `Option<T>`, and `Result<T, PluginError>`. User\nis the WIT-expressible primitives plus `String`, `Vec<u8>`→`list<u8>`,\ndefinitions (it only sees the method signatures), so the supported type set\n**Scope (v1):** a proc-macro can't introspect external `struct`/`enum`\n\nthe `.wit` text that `#[plugin_impl]` feeds to `wit_bindgen::generate!`.\nWIT, per the mapping in `docs/explanation/wasm-component-abi.md`, and renders\nMaps the Rust types in a `#[plugin_interface]`/`#[plugin_impl]` method to\n\nWIT generation for the WASM component target (FIDIUS-I-0021 Phase 3).","visibility":"private"}],"crates/fidius-macro/tests/arena_basic.rs":[{"name":"EchoArena","kind":"interface","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":26,"end_line":28,"signature":"{ fn echo() }","qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"public"},{"name":"MyEcho","kind":"struct","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":30,"end_line":30,"signature":null,"qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"public"},{"name":"MyEcho","kind":"type","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":33,"end_line":37,"signature":"impl EchoArena for MyEcho","qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"private"},{"name":"echo","kind":"function","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":34,"end_line":36,"signature":"(&self, input: String) -> String","qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"private"},{"name":"arena_shim_round_trip_with_sufficient_buffer","kind":"function","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":42,"end_line":85,"signature":"()","qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"private"},{"name":"arena_shim_returns_buffer_too_small","kind":"function","file_path":"crates/fidius-macro/tests/arena_basic.rs","start_line":88,"end_line":119,"signature":"()","qualified_name":null,"doc_comment":"buffer as the arena.\ncall the vtable fn pointer directly with a heap-allocated scratch\nExercises the Arena codegen path without going through the host — we\n\nArena buffer strategy: compile test + raw vtable invocation.","visibility":"private"}],"crates/fidius-macro/tests/async_plugin.rs":[{"name":"AsyncProcessor","kind":"interface","file_path":"crates/fidius-macro/tests/async_plugin.rs","start_line":21,"end_line":23,"signature":"{ fn process() }","qualified_name":null,"doc_comment":"Test that async methods work with the fidius macros.","visibility":"public"},{"name":"MyProcessor","kind":"struct","file_path":"crates/fidius-macro/tests/async_plugin.rs","start_line":25,"end_line":25,"signature":null,"qualified_name":null,"doc_comment":"Test that async methods work with the fidius macros.","visibility":"public"},{"name":"MyProcessor","kind":"type","file_path":"crates/fidius-macro/tests/async_plugin.rs","start_line":28,"end_line":33,"signature":"impl AsyncProcessor for MyProcessor","qualified_name":null,"doc_comment":"Test that async methods work with the fidius macros.","visibility":"private"},{"name":"process","kind":"function","file_path":"crates/fidius-macro/tests/async_plugin.rs","start_line":29,"end_line":32,"signature":"(&self, input: String) -> String","qualified_name":null,"doc_comment":"Test that async methods work with the fidius macros.","visibility":"private"},{"name":"can_call_async_method_via_vtable","kind":"function","file_path":"crates/fidius-macro/tests/async_plugin.rs","start_line":38,"end_line":71,"signature":"()","qualified_name":null,"doc_comment":"Test that async methods work with the fidius macros.","visibility":"private"}],"crates/fidius-macro/tests/compile_fail/caller_allocated_removed.rs":[{"name":"BadPlugin","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/caller_allocated_removed.rs","start_line":11,"end_line":13,"signature":"{ fn do_thing() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/caller_allocated_removed.rs","start_line":15,"end_line":15,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/compile_fail/duplicate_method_meta_key.rs":[{"name":"BadPlugin","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/duplicate_method_meta_key.rs","start_line":7,"end_line":11,"signature":"{ fn do_thing() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/duplicate_method_meta_key.rs","start_line":13,"end_line":13,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/compile_fail/missing_version.rs":[{"name":"BadPlugin","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/missing_version.rs","start_line":18,"end_line":20,"signature":"{ fn do_thing() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/missing_version.rs","start_line":22,"end_line":22,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/compile_fail/mut_self.rs":[{"name":"BadPlugin","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/mut_self.rs","start_line":18,"end_line":20,"signature":"{ fn mutate() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/mut_self.rs","start_line":22,"end_line":22,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/compile_fail/reserved_fidius_namespace.rs":[{"name":"BadPlugin","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/reserved_fidius_namespace.rs","start_line":7,"end_line":10,"signature":"{ fn do_thing() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/reserved_fidius_namespace.rs","start_line":12,"end_line":12,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/compile_fail/stream_in_arg_position.rs":[{"name":"BadStream","kind":"interface","file_path":"crates/fidius-macro/tests/compile_fail/stream_in_arg_position.rs","start_line":22,"end_line":24,"signature":"{ fn sink() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"main","kind":"function","file_path":"crates/fidius-macro/tests/compile_fail/stream_in_arg_position.rs","start_line":26,"end_line":26,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-macro/tests/crate_path.rs":[{"name":"Calculator","kind":"interface","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":23,"end_line":25,"signature":"{ fn add() }","qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"public"},{"name":"MyCalculator","kind":"struct","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":27,"end_line":27,"signature":null,"qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"public"},{"name":"MyCalculator","kind":"type","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":30,"end_line":34,"signature":"impl Calculator for MyCalculator","qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"private"},{"name":"add","kind":"function","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":31,"end_line":33,"signature":"(&self, input: String) -> String","qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"private"},{"name":"custom_crate_path_compiles_and_works","kind":"function","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":39,"end_line":43,"signature":"()","qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"private"},{"name":"custom_crate_path_shim_callable","kind":"function","file_path":"crates/fidius-macro/tests/crate_path.rs","start_line":46,"end_line":78,"signature":"()","qualified_name":null,"doc_comment":"to verify custom crate path resolution.\nUses `fidius_core` directly (the real crate name) instead of the `fidius` alias\n\nTest that `crate = \"...\"` attribute works on both plugin_interface and plugin_impl.","visibility":"private"}],"crates/fidius-macro/tests/impl_basic.rs":[{"name":"Greeter","kind":"interface","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":21,"end_line":23,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"public"},{"name":"HelloGreeter","kind":"struct","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":25,"end_line":25,"signature":null,"qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"public"},{"name":"HelloGreeter","kind":"type","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":28,"end_line":32,"signature":"impl Greeter for HelloGreeter","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"},{"name":"greet","kind":"function","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":29,"end_line":31,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"},{"name":"get_registry","kind":"function","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":37,"end_line":39,"signature":"() -> &'static fidius_core::descriptor::PluginRegistry","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"},{"name":"registry_exists_and_is_valid","kind":"function","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":42,"end_line":47,"signature":"()","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"},{"name":"descriptor_fields_are_correct","kind":"function","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":50,"end_line":61,"signature":"()","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"},{"name":"can_call_shim_via_vtable","kind":"function","file_path":"crates/fidius-macro/tests/impl_basic.rs","start_line":64,"end_line":100,"signature":"()","qualified_name":null,"doc_comment":"Test that #[plugin_impl] compiles and generates expected items.","visibility":"private"}],"crates/fidius-macro/tests/interface_basic.rs":[{"name":"Greeter","kind":"interface","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":21,"end_line":26,"signature":"{ fn greet(), fn greet_fancy() }","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"public"},{"name":"vtable_struct_exists","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":29,"end_line":34,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"},{"name":"interface_hash_is_nonzero","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":37,"end_line":39,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"},{"name":"interface_version_matches","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":42,"end_line":44,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"},{"name":"buffer_strategy_matches","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":47,"end_line":49,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"},{"name":"capability_constant_exists","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":52,"end_line":55,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"},{"name":"method_index_constants_exist","kind":"function","file_path":"crates/fidius-macro/tests/interface_basic.rs","start_line":58,"end_line":61,"signature":"()","qualified_name":null,"doc_comment":"Basic test that #[plugin_interface] compiles and generates expected items.","visibility":"private"}],"crates/fidius-macro/tests/metadata.rs":[{"name":"Tagged","kind":"interface","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":27,"end_line":37,"signature":"{ fn create(), fn list(), fn version() }","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"public"},{"name":"MyTagged","kind":"struct","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":39,"end_line":39,"signature":null,"qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"public"},{"name":"MyTagged","kind":"type","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":42,"end_line":52,"signature":"impl Tagged for MyTagged","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"create","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":43,"end_line":45,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"list","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":46,"end_line":48,"signature":"(&self) -> String","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"version","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":49,"end_line":51,"signature":"(&self) -> String","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"read_cstr","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":56,"end_line":60,"signature":"(ptr: *const std::ffi::c_char) -> &'static str","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"trait_metadata_is_populated","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":63,"end_line":76,"signature":"()","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"method_metadata_is_populated_per_method","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":79,"end_line":106,"signature":"()","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"},{"name":"interface_hash_unaffected_by_metadata","kind":"function","file_path":"crates/fidius-macro/tests/metadata.rs","start_line":109,"end_line":116,"signature":"()","qualified_name":null,"doc_comment":"into the PluginDescriptor at the plugin-link level (not dylib).\nof static MetaKv arrays and MethodMetaEntry table, and correct wiring\nExercises `#[method_meta]` + `#[trait_meta]` attribute parsing, emission\n\nMetadata declaration and descriptor round-trip test.","visibility":"private"}],"crates/fidius-macro/tests/multi_arg.rs":[{"name":"MultiArg","kind":"interface","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":23,"end_line":35,"signature":"{ fn status(), fn echo(), fn concat(), fn add_three() }","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"public"},{"name":"MyMultiArg","kind":"struct","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":37,"end_line":37,"signature":null,"qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"public"},{"name":"MyMultiArg","kind":"type","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":40,"end_line":56,"signature":"impl MultiArg for MyMultiArg","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"status","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":41,"end_line":43,"signature":"(&self) -> String","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"echo","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":45,"end_line":47,"signature":"(&self, msg: String) -> String","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"concat","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":49,"end_line":51,"signature":"(&self, a: String, b: String) -> String","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"add_three","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":53,"end_line":55,"signature":"(&self, x: i64, y: i64, z: i64) -> i64","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"get_registry","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":60,"end_line":62,"signature":"() -> &'static fidius_core::descriptor::PluginRegistry","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"call_vtable","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":65,"end_line":107,"signature":"(\n    vtable: &__fidius_MultiArg::MultiArg_VTable,\n    desc: &fidius_core::descriptor::PluginDescriptor,\n    index: usize,\n    input_bytes: &[u8],\n    free_buffer: Option<unsafe extern \"C\" fn(*mut u8, usize)>,\n) -> (i32, Vec<u8>)","qualified_name":null,"doc_comment":"Helper: call a vtable method by index with given input bytes.","visibility":"private"},{"name":"zero_args_status","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":110,"end_line":122,"signature":"()","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"one_arg_echo","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":125,"end_line":137,"signature":"()","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"two_args_concat","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":140,"end_line":153,"signature":"()","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"three_args_add","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":156,"end_line":168,"signature":"()","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"},{"name":"method_indices_correct","kind":"function","file_path":"crates/fidius-macro/tests/multi_arg.rs","start_line":171,"end_line":176,"signature":"()","qualified_name":null,"doc_comment":"with uniform tuple encoding.\nTest that multi-arg, single-arg, and zero-arg methods all work","visibility":"private"}],"crates/fidius-macro/tests/multi_plugin.rs":[{"name":"Greeter","kind":"interface","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":21,"end_line":23,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"public"},{"name":"HelloGreeter","kind":"struct","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":26,"end_line":26,"signature":null,"qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"public"},{"name":"HelloGreeter","kind":"type","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":29,"end_line":33,"signature":"impl Greeter for HelloGreeter","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"greet","kind":"function","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":30,"end_line":32,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"GoodbyeGreeter","kind":"struct","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":36,"end_line":36,"signature":null,"qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"public"},{"name":"GoodbyeGreeter","kind":"type","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":39,"end_line":43,"signature":"impl Greeter for GoodbyeGreeter","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"greet","kind":"function","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":40,"end_line":42,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"registry_has_two_plugins","kind":"function","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":49,"end_line":54,"signature":"()","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"both_descriptors_are_valid","kind":"function","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":57,"end_line":80,"signature":"()","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"},{"name":"can_call_both_plugins","kind":"function","file_path":"crates/fidius-macro/tests/multi_plugin.rs","start_line":83,"end_line":123,"signature":"()","qualified_name":null,"doc_comment":"Test that multiple #[plugin_impl] in one binary produces a registry with multiple plugins.","visibility":"private"}],"crates/fidius-macro/tests/raw_wire.rs":[{"name":"TypedPipe","kind":"interface","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":32,"end_line":34,"signature":"{ fn process() }","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"public"},{"name":"RawPipe","kind":"interface","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":37,"end_line":40,"signature":"{ fn process() }","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"public"},{"name":"raw_marker_changes_interface_hash","kind":"function","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":43,"end_line":50,"signature":"()","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"private"},{"name":"Mixed","kind":"interface","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":56,"end_line":65,"signature":"{ fn bulk(), fn ping(), fn bulk_v2() }","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"public"},{"name":"mixed_interface_companion_module_compiles","kind":"function","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":68,"end_line":77,"signature":"()","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"private"},{"name":"FallibleBytePipe","kind":"interface","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":84,"end_line":87,"signature":"{ fn maybe() }","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"public"},{"name":"raw_method_with_result_return_compiles","kind":"function","file_path":"crates/fidius-macro/tests/raw_wire.rs","start_line":90,"end_line":93,"signature":"()","qualified_name":null,"doc_comment":"without needing to load a dylib.\nprobe the generated companion module's interface-hash constants directly,\nThese tests live in fidius-macro (rather than fidius-host) so they can\n\nMacro-side tests for `#[wire(raw)]` byte-passthrough mode.","visibility":"private"}],"crates/fidius-macro/tests/smoke_cdylib.rs":[{"name":"load_cdylib_and_call_plugin","kind":"function","file_path":"crates/fidius-macro/tests/smoke_cdylib.rs","start_line":23,"end_line":139,"signature":"()","qualified_name":null,"doc_comment":"loads it via dlopen/dlsym and verifies the registry and vtable work.\nThis test builds the test-plugin-smoke crate as a cdylib, then\n\nSmoke test: build a cdylib plugin and load it with libloading.","visibility":"private"},{"name":"AddInput","kind":"struct","file_path":"crates/fidius-macro/tests/smoke_cdylib.rs","start_line":96,"end_line":99,"signature":"{ a: i64, b: i64 }","qualified_name":null,"doc_comment":"loads it via dlopen/dlsym and verifies the registry and vtable work.\nThis test builds the test-plugin-smoke crate as a cdylib, then\n\nSmoke test: build a cdylib plugin and load it with libloading.","visibility":"private"},{"name":"AddOutput","kind":"struct","file_path":"crates/fidius-macro/tests/smoke_cdylib.rs","start_line":101,"end_line":103,"signature":"{ result: i64 }","qualified_name":null,"doc_comment":"loads it via dlopen/dlsym and verifies the registry and vtable work.\nThis test builds the test-plugin-smoke crate as a cdylib, then\n\nSmoke test: build a cdylib plugin and load it with libloading.","visibility":"private"},{"name":"AddFn","kind":"type","file_path":"crates/fidius-macro/tests/smoke_cdylib.rs","start_line":110,"end_line":111,"signature":"= unsafe extern \"C\" fn(*mut std::ffi::c_void, *const u8, u32, *mut *mut u8, *mut u32) -> i32","qualified_name":null,"doc_comment":"loads it via dlopen/dlsym and verifies the registry and vtable work.\nThis test builds the test-plugin-smoke crate as a cdylib, then\n\nSmoke test: build a cdylib plugin and load it with libloading.","visibility":"private"}],"crates/fidius-macro/tests/trybuild.rs":[{"name":"compile_fail_tests","kind":"function","file_path":"crates/fidius-macro/tests/trybuild.rs","start_line":16,"end_line":19,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"crates/fidius-python/build.rs":[{"name":"main","kind":"function","file_path":"crates/fidius-python/build.rs","start_line":26,"end_line":47,"signature":"()","qualified_name":null,"doc_comment":"Build script: configure PyO3 cfg flags and emit a runtime rpath so the\nembedded Python shared library is found at process launch.\n\nWithout this, macOS framework Python builds crash on first call with:\n\n```text\ndyld: Library not loaded: @rpath/Python3.framework/Versions/3.x/Python3\n```\n\nMirrors the cloacina-build pattern (which exists for the same reason).","visibility":"private"}],"crates/fidius-python/src/error.rs":[{"name":"pyerr_to_plugin_error","kind":"function","file_path":"crates/fidius-python/src/error.rs","start_line":40,"end_line":69,"signature":"(err: PyErr) -> PluginError","qualified_name":null,"doc_comment":"Convert a `PyErr` into a `PluginError`, preserving class name, message,\nand a formatted traceback in `details`.\n\nAcquires the GIL internally, so callers can pass a `PyErr` they captured\noutside `Python::with_gil` (the typical case for `?` propagation).","visibility":"public"},{"name":"format_traceback","kind":"function","file_path":"crates/fidius-python/src/error.rs","start_line":74,"end_line":79,"signature":"(py: Python<'_>, tb: Bound<'_, PyTraceback>) -> PyResult<String>","qualified_name":null,"doc_comment":"Format a Python traceback into a plain string by calling\n`traceback.format_tb(tb)` and joining the result. Best-effort: returns an\nempty string on internal failure rather than recursively raising.","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-python/src/error.rs","start_line":82,"end_line":105,"signature":null,"qualified_name":null,"doc_comment":"plugin code can raise typed errors without their fields being flattened.\nFIDIUS-T-0089) extend it with `fidius.PluginError`-aware unwrapping so\nThis file deliberately stays minimal: later tasks (FIDIUS-T-0086,\n\nraise carried).\n(and, in later tasks, any structured fields a `fidius.PluginError`\n- `details` ← a JSON-encoded object containing the formatted traceback\n- `message` ← `str(exc)` — the user-facing message Python produced.\n- `code` ← the exception class name (e.g. `\"ValueError\"`, `\"KeyError\"`).\n\nway back to the host. The mapping rules are:\nEvery Python exception raised by plugin code crosses this helper on its\n\nBridge Python exceptions into fidius's `PluginError`.","visibility":"private"},{"name":"maps_value_error_to_plugin_error","kind":"function","file_path":"crates/fidius-python/src/error.rs","start_line":86,"end_line":104,"signature":"()","qualified_name":null,"doc_comment":"plugin code can raise typed errors without their fields being flattened.\nFIDIUS-T-0089) extend it with `fidius.PluginError`-aware unwrapping so\nThis file deliberately stays minimal: later tasks (FIDIUS-T-0086,\n\nraise carried).\n(and, in later tasks, any structured fields a `fidius.PluginError`\n- `details` ← a JSON-encoded object containing the formatted traceback\n- `message` ← `str(exc)` — the user-facing message Python produced.\n- `code` ← the exception class name (e.g. `\"ValueError\"`, `\"KeyError\"`).\n\nway back to the host. The mapping rules are:\nEvery Python exception raised by plugin code crosses this helper on its\n\nBridge Python exceptions into fidius's `PluginError`.","visibility":"private"}],"crates/fidius-python/src/handle.rs":[{"name":"PythonCallError","kind":"enum","file_path":"crates/fidius-python/src/handle.rs","start_line":44,"end_line":70,"signature":"InvalidMethodIndex | WireModeMismatch | InputDecode | OutputEncode | Plugin","qualified_name":null,"doc_comment":"Errors a typed call can produce on the Python side.","visibility":"public"},{"name":"PythonPluginHandle","kind":"struct","file_path":"crates/fidius-python/src/handle.rs","start_line":74,"end_line":82,"signature":"{ descriptor: &'static PythonInterfaceDescriptor, _module: Py<PyAny>, method_callables: Vec<Py<PyAny>> }","qualified_name":null,"doc_comment":"Loaded-and-validated handle to one Python plugin.","visibility":"public"},{"name":"PythonPluginHandle","kind":"type","file_path":"crates/fidius-python/src/handle.rs","start_line":84,"end_line":237,"signature":"= PythonPluginHandle","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":85,"end_line":95,"signature":"(\n        descriptor: &'static PythonInterfaceDescriptor,\n        module: Py<PyAny>,\n        method_callables: Vec<Py<PyAny>>,\n    ) -> Self","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"crate"},{"name":"descriptor","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":97,"end_line":99,"signature":"(&self) -> &'static PythonInterfaceDescriptor","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"public"},{"name":"method_count","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":101,"end_line":103,"signature":"(&self) -> usize","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"public"},{"name":"call_typed","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":112,"end_line":134,"signature":"(\n        &self,\n        method_index: usize,\n        input_bincode: &[u8],\n    ) -> Result<Vec<u8>, PythonCallError>","qualified_name":null,"doc_comment":"Typed dispatch.\n\n`input_bincode` is the bincode-encoded args tuple — the same byte\npayload the cdylib path would receive. We use bincode here only\nbecause every other fidius caller does; on the way into Python we\npivot through `serde_json::Value` (so the host's `I: Serialize` works\nfor any type the macro accepts).","visibility":"public"},{"name":"call_typed_json","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":139,"end_line":159,"signature":"(\n        &self,\n        method_index: usize,\n        input_json: &[u8],\n    ) -> Result<Vec<u8>, PythonCallError>","qualified_name":null,"doc_comment":"Typed dispatch where the input is already JSON-serialised (the\nhost's `serde_json::to_vec(&input)`). Returns JSON bytes the caller\n`serde_json::from_slice::<O>` decodes.","visibility":"public"},{"name":"call_streaming_start","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":165,"end_line":190,"signature":"(\n        &self,\n        method_index: usize,\n        input_json: &[u8],\n    ) -> Result<crate::stream::PythonStream, PythonCallError>","qualified_name":null,"doc_comment":"Start a server-streaming call (FIDIUS-I-0026). Calls the method to obtain\nits iterator/generator and wraps it in a [`crate::stream::PythonStream`]\nthe host then pumps. Input is JSON like [`Self::call_typed_json`];\nstreaming methods use the typed (non-raw) path.","visibility":"public"},{"name":"call_raw","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":193,"end_line":212,"signature":"(&self, method_index: usize, input: &[u8]) -> Result<Vec<u8>, PythonCallError>","qualified_name":null,"doc_comment":"Raw dispatch — pass bytes in, get bytes out, no encoding.","visibility":"public"},{"name":"lookup_method","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":214,"end_line":236,"signature":"(\n        &self,\n        index: usize,\n        attempting_raw: bool,\n    ) -> Result<MethodLookup<'_>, PythonCallError>","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"private"},{"name":"MethodLookup","kind":"struct","file_path":"crates/fidius-python/src/handle.rs","start_line":239,"end_line":241,"signature":"{ callable: &'a Py<PyAny> }","qualified_name":null,"doc_comment":"`code = <ExceptionClassName>` otherwise.\n`fidius.PluginError` raises (round-trips `code`/`message`/`details`) and\n`pyerr_to_plugin_error` helper, with `code = \"PluginError\"` for typed\nAll Python exceptions become `fidius_core::PluginError` via the\n\ninto `#[wire(raw)]` (T-0082).\nPython, expects `bytes` back. No encoding hops — used by methods opted\n- **Raw**: `call_raw` takes `&[u8]`, passes a `bytes` arg directly to\n\nreturn back to a `Value`, and re-encodes as bincode for the host.\nto convert to Python primitives, calls the callable, converts the\ncdylib `call_method` would receive), pivots through `serde_json::Value`\n- **Typed**: `call_typed` takes raw bincode bytes (the same payload the\n\ninterface descriptor's method order. Dispatch happens via two paths:\nHolds the imported module + a vector of callables aligned with the\n\nHandle to a loaded Python plugin.","visibility":"private"},{"name":"build_call_args","kind":"function","file_path":"crates/fidius-python/src/handle.rs","start_line":250,"end_line":269,"signature":"(\n    py: Python<'py>,\n    input: &serde_json::Value,\n) -> PyResult<Bound<'py, PyTuple>>","qualified_name":null,"doc_comment":"Build positional args for `callable.call(...)` from a JSON value.\n\nThe host's typed encoding is a tuple `(arg1, arg2, ...)` — this surfaces\nas a JSON array. We unpack each element as a positional Python arg so\n`def greet(name)` works rather than `def greet((name,))`. Non-array\nvalues (degenerate case for zero-arg methods that produce JSON `null`)\ndispatch as zero-arg calls.","visibility":"private"}],"crates/fidius-python/src/interpreter.rs":[{"name":"INIT","kind":"variable","file_path":"crates/fidius-python/src/interpreter.rs","start_line":30,"end_line":30,"signature":": Once","qualified_name":null,"doc_comment":"separate `Mutex<PyInterpreter>` to manage.\ncorrect concurrency primitive for embedded interpreters: there is no\nAll Python work is gated by PyO3's `Python::with_gil`, which is the\n\ntearing it down or reinitialising it. This matches cloacina's pattern.\nremainder of the host process — fidius does not currently support\nThe interpreter is brought up lazily on first use and lives for the\nFidius-python links libpython through PyO3's `auto-initialize` feature.\n\nEmbedded interpreter lifecycle.","visibility":"private"},{"name":"ensure_initialized","kind":"function","file_path":"crates/fidius-python/src/interpreter.rs","start_line":38,"end_line":46,"signature":"()","qualified_name":null,"doc_comment":"Idempotent: ensure the embedded Python interpreter is initialised.\n\n`auto-initialize` already brings it up on the first `Python::with_gil`\ncall, so this function is mostly a documented entry point and a tracing\nhook for diagnosing \"did Python actually start up\" issues. Calling it\nrepeatedly is cheap.","visibility":"public"}],"crates/fidius-python/src/lib.rs":[{"name":"error","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":27,"end_line":27,"signature":null,"qualified_name":null,"doc_comment":"Python plugin runtime for Fidius.\n\nThis crate embeds CPython into the host process via PyO3 and (in later\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nPython module. Hosts opt into Python plugin support by depending on this\ncrate (typically through `fidius-host`'s `python` feature flag).\n\nAt this stage the crate provides only the foundation: shared interpreter\ninitialisation and Python-exception-to-`PluginError` conversion. The\nloader, dispatcher, and packaging integration land in subsequent tasks\nunder FIDIUS-I-0020.","visibility":"public"},{"name":"handle","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":28,"end_line":28,"signature":null,"qualified_name":null,"doc_comment":"under FIDIUS-I-0020.\nloader, dispatcher, and packaging integration land in subsequent tasks\ninitialisation and Python-exception-to-`PluginError` conversion. The\nAt this stage the crate provides only the foundation: shared interpreter\n\ncrate (typically through `fidius-host`'s `python` feature flag).\nPython module. Hosts opt into Python plugin support by depending on this\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nThis crate embeds CPython into the host process via PyO3 and (in later\n\nPython plugin runtime for Fidius.","visibility":"public"},{"name":"interpreter","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":29,"end_line":29,"signature":null,"qualified_name":null,"doc_comment":"under FIDIUS-I-0020.\nloader, dispatcher, and packaging integration land in subsequent tasks\ninitialisation and Python-exception-to-`PluginError` conversion. The\nAt this stage the crate provides only the foundation: shared interpreter\n\ncrate (typically through `fidius-host`'s `python` feature flag).\nPython module. Hosts opt into Python plugin support by depending on this\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nThis crate embeds CPython into the host process via PyO3 and (in later\n\nPython plugin runtime for Fidius.","visibility":"public"},{"name":"loader","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":30,"end_line":30,"signature":null,"qualified_name":null,"doc_comment":"under FIDIUS-I-0020.\nloader, dispatcher, and packaging integration land in subsequent tasks\ninitialisation and Python-exception-to-`PluginError` conversion. The\nAt this stage the crate provides only the foundation: shared interpreter\n\ncrate (typically through `fidius-host`'s `python` feature flag).\nPython module. Hosts opt into Python plugin support by depending on this\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nThis crate embeds CPython into the host process via PyO3 and (in later\n\nPython plugin runtime for Fidius.","visibility":"public"},{"name":"stream","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":31,"end_line":31,"signature":null,"qualified_name":null,"doc_comment":"under FIDIUS-I-0020.\nloader, dispatcher, and packaging integration land in subsequent tasks\ninitialisation and Python-exception-to-`PluginError` conversion. The\nAt this stage the crate provides only the foundation: shared interpreter\n\ncrate (typically through `fidius-host`'s `python` feature flag).\nPython module. Hosts opt into Python plugin support by depending on this\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nThis crate embeds CPython into the host process via PyO3 and (in later\n\nPython plugin runtime for Fidius.","visibility":"public"},{"name":"value_bridge","kind":"module","file_path":"crates/fidius-python/src/lib.rs","start_line":32,"end_line":32,"signature":null,"qualified_name":null,"doc_comment":"under FIDIUS-I-0020.\nloader, dispatcher, and packaging integration land in subsequent tasks\ninitialisation and Python-exception-to-`PluginError` conversion. The\nAt this stage the crate provides only the foundation: shared interpreter\n\ncrate (typically through `fidius-host`'s `python` feature flag).\nPython module. Hosts opt into Python plugin support by depending on this\ntasks) exposes a `PluginHandle` whose dispatcher calls into a loaded\nThis crate embeds CPython into the host process via PyO3 and (in later\n\nPython plugin runtime for Fidius.","visibility":"public"}],"crates/fidius-python/src/loader.rs":[{"name":"PythonLoadError","kind":"enum","file_path":"crates/fidius-python/src/loader.rs","start_line":47,"end_line":82,"signature":"Manifest | NotPythonRuntime | MissingPythonSection | ImportFailed | InterfaceHashMismatch | MissingAttr | MethodNotRegis...","qualified_name":null,"doc_comment":"Errors that can happen during Python plugin load.","visibility":"public"},{"name":"load_python_plugin","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":89,"end_line":143,"signature":"(\n    package_dir: &Path,\n    descriptor: &'static PythonInterfaceDescriptor,\n) -> Result<PythonPluginHandle, PythonLoadError>","qualified_name":null,"doc_comment":"Load a Python plugin package against a static interface descriptor.\n\n`package_dir` must point at an unpacked Python plugin directory (the\nthing `unpack_package` returns or that lives next to a `package.toml`\nduring local dev).","visibility":"public"},{"name":"load_python_plugin_configured","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":150,"end_line":208,"signature":"(\n    package_dir: &Path,\n    descriptor: &'static PythonInterfaceDescriptor,\n    config: &serde_json::Value,\n) -> Result<PythonPluginHandle, PythonLoadError>","qualified_name":null,"doc_comment":"Load a **configured** Python plugin instance (FIDIUS-A-0006 / CI.4): import the\nmodule, call its module-level `__fidius_configure__(config) -> instance` with\nthe deserialized config, and bind methods on the returned instance — so the\nconfig is bound once and N differently-configured instances coexist (each is a\ndistinct object). The module must export `__fidius_configure__`.","visibility":"public"},{"name":"prepend_sys_path","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":213,"end_line":242,"signature":"(py: Python<'_>, dir: &Path) -> Result<(), PythonLoadError>","qualified_name":null,"doc_comment":"Prepend `<dir>/vendor` and `<dir>` to `sys.path` if not already present.\nBoth are pushed at index 0 so they shadow anything else with the same\nmodule name — important for the vendored-deps story.","visibility":"private"},{"name":"validate_interface_hash","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":244,"end_line":266,"signature":"(\n    module: &Bound<'_, PyModule>,\n    descriptor: &'static PythonInterfaceDescriptor,\n) -> Result<(), PythonLoadError>","qualified_name":null,"doc_comment":"All Python work happens in the host's embedded interpreter (T-0085).\nWhat we don't do here: subprocess spawning, venv creation, or cancellation.\n\nindices resolve to Python callables at call time.\n5. Look up each method by name (in the descriptor's order) so vtable\ndescriptor passed by the host. Mismatch = clean load error.\n4. Validate the module's `__interface_hash__` constant against the\n3. Import the entry module declared in `[python].entry_module`.\nloads of the same package don't insert twice).\n2. Prepend `<dir>/vendor` and `<dir>` to `sys.path` (idempotent — repeated\n1. Read the package's `package.toml` and assert `runtime = \"python\"`.\n\nLoading steps:\n\npath.\nmethod-index map lets the host dispatch by index just like the cdylib\nLoad a Python plugin package and produce a `PythonPluginHandle` whose","visibility":"private"},{"name":"resolve_methods","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":268,"end_line":293,"signature":"(\n    obj: &Bound<'_, PyAny>,\n    descriptor: &'static PythonInterfaceDescriptor,\n    ctx_name: &str,\n) -> Result<Vec<Py<PyAny>>, PythonLoadError>","qualified_name":null,"doc_comment":"All Python work happens in the host's embedded interpreter (T-0085).\nWhat we don't do here: subprocess spawning, venv creation, or cancellation.\n\nindices resolve to Python callables at call time.\n5. Look up each method by name (in the descriptor's order) so vtable\ndescriptor passed by the host. Mismatch = clean load error.\n4. Validate the module's `__interface_hash__` constant against the\n3. Import the entry module declared in `[python].entry_module`.\nloads of the same package don't insert twice).\n2. Prepend `<dir>/vendor` and `<dir>` to `sys.path` (idempotent — repeated\n1. Read the package's `package.toml` and assert `runtime = \"python\"`.\n\nLoading steps:\n\npath.\nmethod-index map lets the host dispatch by index just like the cdylib\nLoad a Python plugin package and produce a `PythonPluginHandle` whose","visibility":"private"},{"name":"import_failure","kind":"function","file_path":"crates/fidius-python/src/loader.rs","start_line":295,"end_line":301,"signature":"(what: &str, err: PyErr) -> PythonLoadError","qualified_name":null,"doc_comment":"All Python work happens in the host's embedded interpreter (T-0085).\nWhat we don't do here: subprocess spawning, venv creation, or cancellation.\n\nindices resolve to Python callables at call time.\n5. Look up each method by name (in the descriptor's order) so vtable\ndescriptor passed by the host. Mismatch = clean load error.\n4. Validate the module's `__interface_hash__` constant against the\n3. Import the entry module declared in `[python].entry_module`.\nloads of the same package don't insert twice).\n2. Prepend `<dir>/vendor` and `<dir>` to `sys.path` (idempotent — repeated\n1. Read the package's `package.toml` and assert `runtime = \"python\"`.\n\nLoading steps:\n\npath.\nmethod-index map lets the host dispatch by index just like the cdylib\nLoad a Python plugin package and produce a `PythonPluginHandle` whose","visibility":"private"}],"crates/fidius-python/src/stream.rs":[{"name":"PyStreamStep","kind":"enum","file_path":"crates/fidius-python/src/stream.rs","start_line":39,"end_line":46,"signature":"Item | End | Error","qualified_name":null,"doc_comment":"One step of advancing a Python plugin's server-streaming iterator.","visibility":"public"},{"name":"PythonStream","kind":"struct","file_path":"crates/fidius-python/src/stream.rs","start_line":53,"end_line":55,"signature":"{ iter: Py<PyAny> }","qualified_name":null,"doc_comment":"A handle to an in-flight Python server-stream — the iterator obtained by\ncalling a streaming plugin method.\n\n`Send` (a `Py<PyAny>` is `Send + Sync`) so the host can drive it from a\ndedicated pump thread, acquiring the GIL only for each `next`/`cancel`.","visibility":"public"},{"name":"PythonStream","kind":"type","file_path":"crates/fidius-python/src/stream.rs","start_line":57,"end_line":92,"signature":"= PythonStream","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":58,"end_line":60,"signature":"(iter: Py<PyAny>) -> Self","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"crate"},{"name":"next","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":64,"end_line":78,"signature":"(&self) -> PyStreamStep","qualified_name":null,"doc_comment":"Advance one item. Holds the GIL only for the duration of this call, so a\nslow downstream consumer never pins the interpreter.","visibility":"public"},{"name":"cancel","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":84,"end_line":91,"signature":"(&self)","qualified_name":null,"doc_comment":"Cancel the stream: run the generator's cleanup by calling `close()`,\nwhich raises `GeneratorExit` inside the generator so its `finally`/\ncontext-manager `__exit__` runs (closing DB cursors, releasing handles).\nA no-op for iterables without `close` (e.g. a plain `list_iterator`).","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-python/src/stream.rs","start_line":95,"end_line":201,"signature":null,"qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"stream_from","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":100,"end_line":109,"signature":"(code: &str) -> PythonStream","qualified_name":null,"doc_comment":"Build a `PythonStream` from a snippet that evaluates to an iterator.","visibility":"private"},{"name":"item_i64","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":111,"end_line":116,"signature":"(step: PyStreamStep) -> i64","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"step_name","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":118,"end_line":124,"signature":"(s: &PyStreamStep) -> &'static str","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"yields_items_then_end","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":127,"end_line":135,"signature":"()","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"generator_exception_becomes_error","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":138,"end_line":150,"signature":"()","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"},{"name":"gen_from_def","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":153,"end_line":162,"signature":"(code: &str) -> PythonStream","qualified_name":null,"doc_comment":"Run a snippet that binds `it` to an iterator/generator in fresh globals.","visibility":"private"},{"name":"cancel_runs_generator_finally","kind":"function","file_path":"crates/fidius-python/src/stream.rs","start_line":165,"end_line":200,"signature":"()","qualified_name":null,"doc_comment":"split (`fidius-python` has no async runtime).\nmachinery. Keeping PyO3 here and tokio there respects the existing crate\nbackpressure — lives in `fidius-host`'s `Pyo3Executor`, which owns the tokio\n*async* half — pumping it onto a `ChunkStream` over a bounded channel with\n([`PythonStream::cancel`] → `gen.close()` → `GeneratorExit`/`finally`). The\nthat step) and runs the generator's cleanup on cancellation\niterator one item at a time ([`PythonStream::next`], holding the GIL only for\n[`PythonStream`] is the *sync, GIL-aware* half of the bridge: it advances the\n\nto obtain its iterator and wraps it in a [`PythonStream`].\n[`super::handle::PythonPluginHandle::call_streaming_start`] calls the method\nA streaming plugin method returns a Python **generator** (or any iterable).\n\nServer-streaming dispatch for Python plugins (FIDIUS-I-0026).","visibility":"private"}],"crates/fidius-python/src/value_bridge.rs":[{"name":"value_to_pyobject","kind":"function","file_path":"crates/fidius-python/src/value_bridge.rs","start_line":33,"end_line":65,"signature":"(py: Python<'py>, value: &Value) -> PyResult<Bound<'py, PyAny>>","qualified_name":null,"doc_comment":"Convert a `serde_json::Value` into a Python object owned by `py`.","visibility":"public"},{"name":"pyobject_to_value","kind":"function","file_path":"crates/fidius-python/src/value_bridge.rs","start_line":72,"end_line":141,"signature":"(obj: &Bound<'_, PyAny>) -> PyResult<Value>","qualified_name":null,"doc_comment":"Convert a Python object back into a `serde_json::Value`.\n\n`bytes` are encoded as a JSON object `{ \"__bytes__\": [...] }` so the\nhost can recover them via a custom serde adapter (today only used by\ninternal error paths — typed bytes go through `#[wire(raw)]` instead).","visibility":"public"},{"name":"tests","kind":"module","file_path":"crates/fidius-python/src/value_bridge.rs","start_line":144,"end_line":167,"signature":null,"qualified_name":null,"doc_comment":"which bypasses this layer entirely.\nPython-side. The hot path for bulk data uses `#[wire(raw)]` (T-0082)\nself-describing-serde-on-the-Rust-side and natural-Python-types-on-the-\nJSON is the wire format because it's the common ground between\n\ncan `serde_json::from_value::<O>` into the expected return type.\nPython return value and turn it back into a `serde_json::Value` the host\nthe JSON tree into Python primitives. The reverse is symmetric: take a\n`serde_json::to_value` (any `Serialize` type works) and we then convert\nFor typed plugin calls, the host serialises arguments through\n\nBridge between `serde_json::Value` and Python objects.","visibility":"private"},{"name":"roundtrip_primitives","kind":"function","file_path":"crates/fidius-python/src/value_bridge.rs","start_line":149,"end_line":166,"signature":"()","qualified_name":null,"doc_comment":"which bypasses this layer entirely.\nPython-side. The hot path for bulk data uses `#[wire(raw)]` (T-0082)\nself-describing-serde-on-the-Rust-side and natural-Python-types-on-the-\nJSON is the wire format because it's the common ground between\n\ncan `serde_json::from_value::<O>` into the expected return type.\nPython return value and turn it back into a `serde_json::Value` the host\nthe JSON tree into Python primitives. The reverse is symmetric: take a\n`serde_json::to_value` (any `Serialize` type works) and we then convert\nFor typed plugin calls, the host serialises arguments through\n\nBridge between `serde_json::Value` and Python objects.","visibility":"private"}],"crates/fidius-python/tests/loader_e2e.rs":[{"name":"HASH","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":29,"end_line":29,"signature":": u64","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"GREETER_METHODS","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":30,"end_line":43,"signature":": [PythonMethodDesc; 3]","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"ERROR_METHODS","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":45,"end_line":48,"signature":": [PythonMethodDesc; 1]","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"fresh_descriptor","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":55,"end_line":68,"signature":"(\n    methods: &'static [PythonMethodDesc],\n) -> (&'static PythonInterfaceDescriptor, String)","qualified_name":null,"doc_comment":"Make a `'static` interface descriptor with a unique name so each test\ngets its own slot in Python's `sys.modules` cache. Tests share the\nembedded interpreter, so a fixed module name across tests would mean\nthe second-and-later loads silently reuse the first test's module\n(the shared-`sys.modules` constraint we ship as a documented feature).","visibility":"private"},{"name":"COUNTER","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":58,"end_line":58,"signature":": AtomicUsize","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"make_plugin","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":75,"end_line":127,"signature":"(\n    tmp: &tempfile::TempDir,\n    entry_module: &str,\n    declared_hash: u64,\n    methods_source: &str,\n) -> PathBuf","qualified_name":null,"doc_comment":"Stand up a minimal Python plugin package on disk:\n- manifest declaring runtime=python with the supplied entry_module\n- <entry_module>.py implementing the three test methods (or `boom` for\nthe error scenario)\n- vendored fidius SDK so plugin code can `from fidius import method, PluginError`","visibility":"private"},{"name":"GREETER_METHODS_SRC","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":129,"end_line":141,"signature":": &str","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"ERROR_METHODS_SRC","kind":"variable","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":143,"end_line":147,"signature":": &str","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"repo_root","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":149,"end_line":156,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"copy_dir","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":158,"end_line":170,"signature":"(src: &std::path::Path, dst: &std::path::Path)","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"load_greeter","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":172,"end_line":178,"signature":"() -> (tempfile::TempDir, fidius_python::PythonPluginHandle)","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"typed_call_round_trip_string","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":181,"end_line":187,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"typed_call_with_struct_args","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":190,"end_line":218,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"DoubleIn","kind":"struct","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":194,"end_line":197,"signature":"{ name: String, count: i64 }","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"DoubleOut","kind":"struct","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":199,"end_line":202,"signature":"{ name: String, twice: i64 }","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"raw_call_round_trip_2mb","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":221,"end_line":231,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"plugin_error_round_trips_with_code_and_details","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":234,"end_line":260,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"interface_hash_mismatch_is_rejected","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":263,"end_line":273,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"wire_mode_mismatch_typed_called_as_raw_errors","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":276,"end_line":281,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"},{"name":"out_of_range_method_index_errors","kind":"function","file_path":"crates/fidius-python/tests/loader_e2e.rs","start_line":284,"end_line":288,"signature":"()","qualified_name":null,"doc_comment":"independently.\nthe loader API directly so we can verify the dispatcher's behaviour\nNo PluginHost integration here — that lands in T-0090. This test pokes\n\n`load_python_plugin`, and exercise typed + raw + error paths.\nprepend the in-tree `python/fidius` SDK to its vendor/, load it via\nEnd-to-end loader test: build a small Python plugin in a temp dir,","visibility":"private"}],"crates/fidius-python/tests/smoke.rs":[{"name":"interpreter_evaluates_simple_expression","kind":"function","file_path":"crates/fidius-python/tests/smoke.rs","start_line":25,"end_line":35,"signature":"()","qualified_name":null,"doc_comment":"Python exception. Subsequent tasks layer the loader and dispatcher on top.\nstarts up and that the error helper produces a sensible PluginError for a\nFoundation smoke test for fidius-python: prove the embedded interpreter","visibility":"private"},{"name":"pyerr_to_plugin_error_preserves_class_message_and_traceback","kind":"function","file_path":"crates/fidius-python/tests/smoke.rs","start_line":38,"end_line":50,"signature":"()","qualified_name":null,"doc_comment":"Python exception. Subsequent tasks layer the loader and dispatcher on top.\nstarts up and that the error helper produces a sensible PluginError for a\nFoundation smoke test for fidius-python: prove the embedded interpreter","visibility":"private"}],"crates/fidius-test/src/dylib.rs":[{"name":"dylib_fixture","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":52,"end_line":58,"signature":"(plugin_dir: impl Into<PathBuf>) -> DylibFixtureBuilder","qualified_name":null,"doc_comment":"Start building a dylib fixture for the plugin crate at `plugin_dir`.\n\n`plugin_dir` must contain a `Cargo.toml` with `crate-type = [\"cdylib\"]`.\nThe resulting fixture caches the build across the current test binary\nprocess; subsequent calls with the same `plugin_dir` return the cached\nfixture without re-running cargo.","visibility":"public"},{"name":"DylibFixtureBuilder","kind":"struct","file_path":"crates/fidius-test/src/dylib.rs","start_line":61,"end_line":65,"signature":"{ plugin_dir: PathBuf, release: bool, signing_key: Option<SigningKey> }","qualified_name":null,"doc_comment":"Builder for [`DylibFixture`]. See [`dylib_fixture`].","visibility":"public"},{"name":"DylibFixtureBuilder","kind":"type","file_path":"crates/fidius-test/src/dylib.rs","start_line":67,"end_line":117,"signature":"= DylibFixtureBuilder","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"with_release","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":70,"end_line":73,"signature":"(mut self, release: bool) -> Self","qualified_name":null,"doc_comment":"Build in release mode. Defaults to the test binary's own profile\n(release if tests are built with `--release`, otherwise debug).","visibility":"public"},{"name":"signed_with","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":81,"end_line":84,"signature":"(mut self, key: &SigningKey) -> Self","qualified_name":null,"doc_comment":"Sign the produced dylib with `key`, writing a `.sig` file alongside it.\n\nOnly takes effect on the first (un-cached) build — subsequent cached\nfixtures are returned unchanged. For tests that need re-signing,\nre-sign via [`crate::signing::sign_dylib`] on the returned\n[`DylibFixture::dylib_path`].","visibility":"public"},{"name":"build","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":90,"end_line":116,"signature":"(self) -> DylibFixture","qualified_name":null,"doc_comment":"Execute the build (or return cached result) and produce the fixture.\n\nPanics on build failure — tests should not attempt recovery from a\nplugin that won't compile.","visibility":"public"},{"name":"DylibFixture","kind":"struct","file_path":"crates/fidius-test/src/dylib.rs","start_line":121,"end_line":127,"signature":"{ plugin_output_dir: PathBuf, dylib_path: PathBuf }","qualified_name":null,"doc_comment":"A built plugin ready to be loaded by a `PluginHost`.","visibility":"public"},{"name":"DylibFixture","kind":"type","file_path":"crates/fidius-test/src/dylib.rs","start_line":129,"end_line":140,"signature":"= DylibFixture","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"dir","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":131,"end_line":133,"signature":"(&self) -> &Path","qualified_name":null,"doc_comment":"Directory containing the built dylib — `search_path` for `PluginHost`.","visibility":"public"},{"name":"dylib_path","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":137,"end_line":139,"signature":"(&self) -> &Path","qualified_name":null,"doc_comment":"Full path to the dylib file itself. Use this to sign, inspect, or load\nthe dylib directly (e.g., `fidius_host::loader::load_library`).","visibility":"public"},{"name":"CacheKey","kind":"struct","file_path":"crates/fidius-test/src/dylib.rs","start_line":145,"end_line":148,"signature":"{ plugin_dir: PathBuf, release: bool }","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"cache","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":150,"end_line":153,"signature":"() -> &'static Mutex<HashMap<CacheKey, DylibFixture>>","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"CACHE","kind":"variable","file_path":"crates/fidius-test/src/dylib.rs","start_line":151,"end_line":151,"signature":": OnceLock<Mutex<HashMap<CacheKey, DylibFixture>>>","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"dylib_extension","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":155,"end_line":163,"signature":"() -> &'static str","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"},{"name":"build_uncached","kind":"function","file_path":"crates/fidius-test/src/dylib.rs","start_line":165,"end_line":205,"signature":"(plugin_dir: &Path, release: bool) -> DylibFixture","qualified_name":null,"doc_comment":"```\n.build()?;\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\n```ignore\n\n# Example\n\ninvocations still rebuild (on cache miss in cargo's own target dir).\nthe existing path without re-running cargo. Fresh `cargo test`\ncall builds the plugin; subsequent calls in the same test binary return\n[`dylib_fixture`] returns a process-wide cached build result: the first\n\nthough the source hasn't changed.\nevery test is noisy and slow — each test re-builds the plugin even\n`PluginHost` at its containing directory. Doing this from scratch in\ncrate, locate the produced `.dylib`/`.so`/`.dll`, and point a\nIntegration tests frequently need to invoke `cargo build` on a plugin\n\nBuild-and-cache helpers for plugin cdylib fixtures.","visibility":"private"}],"crates/fidius-test/src/lib.rs":[{"name":"dylib","kind":"module","file_path":"crates/fidius-test/src/lib.rs","start_line":45,"end_line":45,"signature":null,"qualified_name":null,"doc_comment":"Testing helpers for Fidius plugin authors and hosts.\n\nThis crate provides the infrastructure the Fidius codebase uses internally\nfor its own tests, now exposed so downstream users don't have to reinvent\nthe wheel. Add it under `[dev-dependencies]` and you get:\n\n- [`dylib_fixture`] — build a plugin crate's cdylib via `cargo build`,\ncached across tests in the same process. Optional signing via\n[`DylibFixtureBuilder::signed_with`].\n- [`signing::fixture_keypair`] — deterministic Ed25519 keypair for tests.\n- [`signing::sign_dylib`] — produce a `.sig` file next to a dylib.\n\n# Example\n\n```ignore\nuse fidius_test::dylib_fixture;\nuse fidius_host::PluginHost;\n\n#[test]\nfn loads_plugin() {\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\nlet host = PluginHost::builder()\n.search_path(fixture.dir())\n.build()\n.unwrap();\nlet plugins = host.discover().unwrap();\nassert!(!plugins.is_empty());\n}\n```","visibility":"public"},{"name":"signing","kind":"module","file_path":"crates/fidius-test/src/lib.rs","start_line":46,"end_line":46,"signature":null,"qualified_name":null,"doc_comment":"```\n}\nassert!(!plugins.is_empty());\nlet plugins = host.discover().unwrap();\n.unwrap();\n.build()\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\nfn loads_plugin() {\n#[test]\n\nuse fidius_host::PluginHost;\nuse fidius_test::dylib_fixture;\n```ignore\n\n# Example\n\n- [`signing::sign_dylib`] — produce a `.sig` file next to a dylib.\n- [`signing::fixture_keypair`] — deterministic Ed25519 keypair for tests.\n[`DylibFixtureBuilder::signed_with`].\ncached across tests in the same process. Optional signing via\n- [`dylib_fixture`] — build a plugin crate's cdylib via `cargo build`,\n\nthe wheel. Add it under `[dev-dependencies]` and you get:\nfor its own tests, now exposed so downstream users don't have to reinvent\nThis crate provides the infrastructure the Fidius codebase uses internally\n\nTesting helpers for Fidius plugin authors and hosts.","visibility":"public"},{"name":"stream","kind":"module","file_path":"crates/fidius-test/src/lib.rs","start_line":48,"end_line":48,"signature":null,"qualified_name":null,"doc_comment":"```\n}\nassert!(!plugins.is_empty());\nlet plugins = host.discover().unwrap();\n.unwrap();\n.build()\n.search_path(fixture.dir())\nlet host = PluginHost::builder()\nlet fixture = dylib_fixture(\"./path/to/my-plugin\").build();\nfn loads_plugin() {\n#[test]\n\nuse fidius_host::PluginHost;\nuse fidius_test::dylib_fixture;\n```ignore\n\n# Example\n\n- [`signing::sign_dylib`] — produce a `.sig` file next to a dylib.\n- [`signing::fixture_keypair`] — deterministic Ed25519 keypair for tests.\n[`DylibFixtureBuilder::signed_with`].\ncached across tests in the same process. Optional signing via\n- [`dylib_fixture`] — build a plugin crate's cdylib via `cargo build`,\n\nthe wheel. Add it under `[dev-dependencies]` and you get:\nfor its own tests, now exposed so downstream users don't have to reinvent\nThis crate provides the infrastructure the Fidius codebase uses internally\n\nTesting helpers for Fidius plugin authors and hosts.","visibility":"public"}],"crates/fidius-test/src/signing.rs":[{"name":"fixture_keypair_with_seed","kind":"function","file_path":"crates/fidius-test/src/signing.rs","start_line":30,"end_line":34,"signature":"(seed: u8) -> (SigningKey, VerifyingKey)","qualified_name":null,"doc_comment":"Deterministic Ed25519 keypair derived from `seed` repeated 32 times.\n\nUse different seeds across tests that need distinct keys (e.g., to verify\nthat a wrong-key signature is rejected).","visibility":"public"},{"name":"fixture_keypair","kind":"function","file_path":"crates/fidius-test/src/signing.rs","start_line":37,"end_line":39,"signature":"() -> (SigningKey, VerifyingKey)","qualified_name":null,"doc_comment":"Convenience: [`fixture_keypair_with_seed(1)`](fixture_keypair_with_seed).","visibility":"public"},{"name":"sign_dylib","kind":"function","file_path":"crates/fidius-test/src/signing.rs","start_line":45,"end_line":54,"signature":"(dylib: &Path, key: &SigningKey) -> std::io::Result<()>","qualified_name":null,"doc_comment":"Sign a plugin dylib in place by writing a detached `.sig` file alongside it.\n\nThe signature file uses the same naming convention as `fidius sign` —\nappends `.sig` to the full filename (e.g., `foo.dylib` → `foo.dylib.sig`).","visibility":"public"}],"crates/fidius-test/src/stream.rs":[{"name":"StreamSink","kind":"interface","file_path":"crates/fidius-test/src/stream.rs","start_line":42,"end_line":45,"signature":"{ fn accept() }","qualified_name":null,"doc_comment":"The destination side of a pipe: a consumer `pump` hands each item to, in\npull order. Deliberately tiny — a real destination plugin presents whatever\nricher surface it likes; this is only what composition tests need.","visibility":"public"},{"name":"stream_of","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":52,"end_line":56,"signature":"(items: Vec<Value>) -> ChunkStream","qualified_name":null,"doc_comment":"An in-memory source over a fixed item sequence. **Test-only by nature** —\nyou would never construct a stream from a `Vec` in production; that it is\nuseful only in tests is exactly why the whole module is test-tier.\n\nYields each value as `Ok`, then a clean end of stream.","visibility":"public"},{"name":"collect","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":60,"end_line":66,"signature":"(mut s: ChunkStream) -> Result<Vec<Value>, CallError>","qualified_name":null,"doc_comment":"Drain a stream to a `Vec`, stopping at — and returning — the first error.\nThe single-plugin unit-test idiom: `collect(plugin.process(stream_of(rows)))`.","visibility":"public"},{"name":"pump","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":76,"end_line":84,"signature":"(mut out: ChunkStream, into: &S) -> Result<(), CallError>","qualified_name":null,"doc_comment":"The reference pull-loop wiring a producer stream to a [`StreamSink`].\n\nPull-paced: exactly one item is awaited at a time, so a slow sink naturally\nbackpressures the producer (the next item is not pulled until the sink has\naccepted the current one). Stops at the first error from either side —\nproducer error or sink rejection — and returns it; on a clean end of stream\nreturns `Ok(())`. This is the ~10 lines you would copy into production and\nthen grow your own retries/observability around.","visibility":"public"},{"name":"CollectSink","kind":"struct","file_path":"crates/fidius-test/src/stream.rs","start_line":89,"end_line":91,"signature":"{ items: Mutex<Vec<Value>> }","qualified_name":null,"doc_comment":"A [`StreamSink`] that records everything it accepts — for asserting on the\nfar end of a `pump`.","visibility":"public"},{"name":"CollectSink","kind":"type","file_path":"crates/fidius-test/src/stream.rs","start_line":93,"end_line":103,"signature":"= CollectSink","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"new","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":95,"end_line":97,"signature":"() -> Self","qualified_name":null,"doc_comment":"A fresh, empty sink.","visibility":"public"},{"name":"take","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":100,"end_line":102,"signature":"(&self) -> Vec<Value>","qualified_name":null,"doc_comment":"Snapshot of everything accepted so far.","visibility":"public"},{"name":"CollectSink","kind":"type","file_path":"crates/fidius-test/src/stream.rs","start_line":106,"end_line":111,"signature":"impl StreamSink for CollectSink","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"accept","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":107,"end_line":110,"signature":"(&self, item: Value) -> Result<(), CallError>","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-test/src/stream.rs","start_line":114,"end_line":175,"signature":null,"qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"vals","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":118,"end_line":120,"signature":"(xs: &[i64]) -> Vec<Value>","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"ints","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":122,"end_line":124,"signature":"(vs: Vec<Value>) -> Vec<i64>","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"stream_of_then_collect_round_trips","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":127,"end_line":130,"signature":"()","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"collect_surfaces_first_error","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":133,"end_line":141,"signature":"()","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"pump_delivers_all_items_to_sink","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":144,"end_line":148,"signature":"()","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"pump_stops_on_producer_error","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":151,"end_line":161,"signature":"()","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"},{"name":"compose_single_plugin_idiom","kind":"function","file_path":"crates/fidius-test/src/stream.rs","start_line":164,"end_line":174,"signature":"()","qualified_name":null,"doc_comment":"See ADR FIDIUS-A-0004 (\"Streaming as Mechanism, Not Protocol\").\norchestrator; orchestration (scheduling, retries, observability) is yours.\nloop** — fidius ships the typed pipe (the [`ChunkStream`] primitive), not an\nbut its API may change at any time. **In production, write your own pump\nis *correct* (pull-paced backpressure, stop-and-surface on the first error)\nso the canonical composition pattern is readable in one place. Its behaviour\nIt exists so tests can compose streams without re-deriving the same loop, and\n**This module is deliberately NOT part of fidius's semver-stable surface.**\n\n# Stability\n\nfor \"pipes of plugins.\"\nin isolation or to wire a producer to a consumer. They are the reference `|`\n`stream_of` / `collect` / `pump` make it trivial to test a streaming plugin\n\nComposition harness for streaming plugins — **for tests, not production**.","visibility":"private"}],"crates/fidius-test/tests/smoke.rs":[{"name":"plugin_source_dir","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":25,"end_line":27,"signature":"() -> PathBuf","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"fixture_keypair_is_deterministic","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":30,"end_line":35,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"fixture_keypair_with_seed_differs_by_seed","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":38,"end_line":42,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"sign_dylib_produces_verifiable_signature","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":45,"end_line":67,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"dylib_fixture_builds_plugin_and_host_can_discover","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":70,"end_line":94,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"dylib_fixture_is_cached_across_builds","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":97,"end_line":106,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"client_in_process_calls_plugin_without_dylib_load","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":109,"end_line":120,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"},{"name":"client_in_process_returns_not_found_for_missing_plugin","kind":"function","file_path":"crates/fidius-test/tests/smoke.rs","start_line":123,"end_line":130,"signature":"()","qualified_name":null,"doc_comment":"fixture.\nSelf-tests exercising the public API against the real test-plugin-smoke","visibility":"private"}],"crates/fidius-wit/src/generate.rs":[{"name":"Generated","kind":"struct","file_path":"crates/fidius-wit/src/generate.rs","start_line":33,"end_line":45,"signature":"{ interface_name: String, iface_kebab: String, user_types: Vec<String>, wit: String, conversions: String }","qualified_name":null,"doc_comment":"The product of generating from a plugin crate's source.","visibility":"public"},{"name":"generate","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":50,"end_line":55,"signature":"(src: &str) -> Result<Generated, String>","qualified_name":null,"doc_comment":"Generate WIT + conversions from a crate's source string (`lib.rs`). Inline\nmodules (`mod m { .. }`) are walked; external `mod m;` files cannot be read\nfrom a bare string — use [`generate_from_path`] (the `build.rs` helper does).","visibility":"public"},{"name":"generate_from_path","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":60,"end_line":68,"signature":"(lib_rs: &std::path::Path) -> Result<Generated, String>","qualified_name":null,"doc_comment":"Like [`generate`], but reads `lib_rs` and follows external `mod m;` files\n(resolving `m.rs` / `m/mod.rs`), so `#[derive(WitType)]` types and the\n`#[plugin_interface]` trait may live in submodules.","visibility":"public"},{"name":"Collected","kind":"struct","file_path":"crates/fidius-wit/src/generate.rs","start_line":73,"end_line":77,"signature":"{ structs: Vec<(Vec<String>, syn::ItemStruct)>, enums: Vec<(Vec<String>, syn::ItemEnum)>, the_trait: Option<syn::ItemTra...","qualified_name":null,"doc_comment":"`#[derive(WitType)]` types (tagged with their Rust module path) + the\n`#[plugin_interface]` trait, gathered across the module tree.","visibility":"private"},{"name":"collect","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":81,"end_line":127,"signature":"(\n    items: &[Item],\n    mod_path: &[String],\n    dir: Option<&std::path::Path>,\n    acc: &mut Collected,\n) -> Result<(), String>","qualified_name":null,"doc_comment":"Recursively gather items, descending into inline `mod m { .. }` and (when\n`dir` is `Some`) external `mod m;` files (`m.rs` / `m/mod.rs`).","visibility":"private"},{"name":"assemble","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":130,"end_line":207,"signature":"(acc: Collected) -> Result<Generated, String>","qualified_name":null,"doc_comment":"Build the `.wit` + conversions from the collected items.","visibility":"private"},{"name":"author_path","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":210,"end_line":216,"signature":"(mod_path: &[String], name: &str) -> String","qualified_name":null,"doc_comment":"`crate::<mod::path>::<Name>` — the author-side path for a type at `mod_path`.","visibility":"private"},{"name":"render_conversions","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":222,"end_line":344,"signature":"(\n    iface_kebab: &str,\n    structs: &[(Vec<String>, syn::ItemStruct)],\n    enums: &[(Vec<String>, syn::ItemEnum)],\n    known: &BTreeSet<String>,\n) -> String","qualified_name":null,"doc_comment":"Render `From` impls (both directions) between each user type and its\nwit-bindgen-generated mirror. Emitted into the adapter module, where the\ngenerated types live (flat) at `exports::fidius::<iface>::<iface>::<Type>`\nand the author types at `crate::<mod::path>::<Type>`.","visibility":"private"},{"name":"conv_expr","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":352,"end_line":379,"signature":"(access: &str, ty: &Type, known: &BTreeSet<String>) -> String","qualified_name":null,"doc_comment":"Conversion expression for a field/payload `access` of type `ty`. Identity\n(move) when the type holds no user type (generated and author types are then\nidentical); otherwise `.into()` (user type), or a `map`/`into_iter().map()`\nrecursing through `Option`/`Vec`. Symmetric — works generated→author and\nauthor→generated, since `From` is generated both ways. Public so the macro's\nadapter reuses the exact same boundary conversions.","visibility":"public"},{"name":"contains_user_type","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":383,"end_line":398,"signature":"(ty: &Type, known: &BTreeSet<String>) -> bool","qualified_name":null,"doc_comment":"Whether `ty` is, or contains (through `Vec`/`Option`/`Box`), a user type in\n`known`. Public so the macro can classify args/returns.","visibility":"public"},{"name":"single_generic","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":400,"end_line":409,"signature":"(seg: &syn::PathSegment) -> Option<&Type>","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"has_attr","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":412,"end_line":420,"signature":"(attrs: &[syn::Attribute], name: &str) -> bool","qualified_name":null,"doc_comment":"Does `attrs` contain `#[<name>(...)]` / `#[<path>::<name>]` (last segment match)?","visibility":"private"},{"name":"has_derive","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":423,"end_line":445,"signature":"(attrs: &[syn::Attribute], name: &str) -> bool","qualified_name":null,"doc_comment":"Does `attrs` contain a `#[derive(... <name> ...)]`?","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-wit/src/generate.rs","start_line":448,"end_line":521,"signature":null,"qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"SRC","kind":"variable","file_path":"crates/fidius-wit/src/generate.rs","start_line":451,"end_line":464,"signature":": &str","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"generates_wit_with_records_variants_and_funcs","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":467,"end_line":483,"signature":"()","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"generates_conversions_both_ways","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":486,"end_line":499,"signature":"()","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"primitive_only_interface_has_no_conversions","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":502,"end_line":511,"signature":"()","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"},{"name":"unsupported_type_errors","kind":"function","file_path":"crates/fidius-wit/src/generate.rs","start_line":514,"end_line":520,"signature":"()","qualified_name":null,"doc_comment":"the `fidius wit` CLI, which read the source files.\ncan't see external type definitions, so this runs from a `build.rs` helper and\ngenerated↔author `From` conversions the wasm adapter includes. A proc-macro\ndocument (records/variants + funcs) and (b) the Rust source for\nevery `#[derive(WitType)]` `struct`/`enum`, and produces (a) a complete `.wit`\nParses a plugin crate's Rust source, finds the `#[plugin_interface]` trait and\n\nSource-parsing WIT generator (FIDIUS-I-0023).","visibility":"private"}],"crates/fidius-wit/src/lib.rs":[{"name":"generate","kind":"module","file_path":"crates/fidius-wit/src/lib.rs","start_line":30,"end_line":30,"signature":null,"qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"to_kebab_case","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":35,"end_line":50,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"Convert a Rust identifier (CamelCase or snake_case) to kebab-case, the WIT\nnaming convention. `BytePipe` → `byte-pipe`, `echo_bytes` → `echo-bytes`.","visibility":"public"},{"name":"result_ok_type","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":53,"end_line":62,"signature":"(ty: &Type) -> Option<&Type>","qualified_name":null,"doc_comment":"Extract the `T` from `Result<T, _>`, if `ty` is a `Result`.","visibility":"public"},{"name":"WitMethod","kind":"struct","file_path":"crates/fidius-wit/src/lib.rs","start_line":65,"end_line":78,"signature":"{ name: String, params: Vec<(String, String)>, ret: Option<String>, stream_item: Option<String> }","qualified_name":null,"doc_comment":"One method projected to WIT (already-mapped strings).","visibility":"public"},{"name":"stream_item_type","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":83,"end_line":92,"signature":"(ty: &Type) -> Option<&Type>","qualified_name":null,"doc_comment":"If `ty` is `fidius::Stream<T>` (final path segment `Stream`, exactly one type\nargument), return `T`. The server-streaming marker (FIDIUS-I-0026, D4). Public\nso the macro's WASM adapter shares the same detection.","visibility":"public"},{"name":"wit_type_with","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":97,"end_line":155,"signature":"(ty: &Type, known: &BTreeSet<String>) -> Result<String, String>","qualified_name":null,"doc_comment":"Map a Rust argument/return type to its WIT spelling, where `known` holds the\nRust identifiers of `#[derive(WitType)]` user types (mapped to their\nkebab-case record/variant name). Returns `Err(msg)` for unsupported types.","visibility":"public"},{"name":"rust_type_to_wit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":159,"end_line":161,"signature":"(ty: &Type) -> Result<String, String>","qualified_name":null,"doc_comment":"Primitive/std-only mapping (no user types) — the form `fidius-macro` uses for\nthe descriptor/method tables.","visibility":"public"},{"name":"return_to_wit_with","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":166,"end_line":188,"signature":"(\n    ret: Option<&Type>,\n    known: &BTreeSet<String>,\n) -> Result<Option<String>, String>","qualified_name":null,"doc_comment":"Map a method's return type to an optional WIT return, with user types in\n`known`. `Result<T, PluginError>` → `result<T, plugin-error>` (or\n`result<_, plugin-error>` for `T = ()`); `()`/none → no return.","visibility":"public"},{"name":"return_to_wit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":191,"end_line":193,"signature":"(ret: Option<&Type>) -> Result<Option<String>, String>","qualified_name":null,"doc_comment":"Primitive/std-only return mapping (no user types).","visibility":"public"},{"name":"struct_to_record","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":198,"end_line":215,"signature":"(item: &ItemStruct, known: &BTreeSet<String>) -> Result<String, String>","qualified_name":null,"doc_comment":"Render a `record <name> { ... }` WIT block from a Rust struct (named fields\nonly). Field types are mapped with `known` so they may reference other user\ntypes.","visibility":"public"},{"name":"enum_to_wit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":225,"end_line":268,"signature":"(\n    item: &ItemEnum,\n    known: &BTreeSet<String>,\n) -> Result<(Vec<String>, String), String>","qualified_name":null,"doc_comment":"Render a Rust enum to WIT: a `variant <name> { ... }` plus any **synthetic\nrecords** for struct-shaped cases. Returns `(synthetic_records, variant)`.\n\nCase shapes: unit → `case`; single tuple field → `case(type)`; named fields\n(`Case { .. }`) → a synthesized `record <enum>-<case>` and `case(<enum>-<case>)`.\nA multi-field *tuple* case is rejected: WIT cases take one payload, and a\nmulti-field tuple serializes as a sequence (not a record) via serde, so it\ncan't round-trip — use a struct variant `Case { .. }` instead.","visibility":"public"},{"name":"render_wit_full","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":274,"end_line":328,"signature":"(iface_kebab: &str, type_defs: &[String], methods: &[WitMethod]) -> String","qualified_name":null,"doc_comment":"Render a complete `.wit` document: package + interface (the `plugin-error`\nrecord, any user `type_defs` (records/variants), the funcs, and the\n`fidius-interface-hash` carrier) + the `<iface>-plugin` world. `type_defs`\nare pre-rendered (see [`struct_to_record`] / [`enum_to_variant`]).","visibility":"public"},{"name":"render_wit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":332,"end_line":334,"signature":"(iface_kebab: &str, methods: &[WitMethod]) -> String","qualified_name":null,"doc_comment":"Convenience: render a WIT document with no user type defs (the primitives-only\nform `fidius-macro` emits inline today).","visibility":"public"},{"name":"is_unit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":338,"end_line":340,"signature":"(ty: &Type) -> bool","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"path_is","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":342,"end_line":348,"signature":"(p: &syn::TypePath, name: &str) -> bool","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"single_generic","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":350,"end_line":352,"signature":"(seg: &'a syn::PathSegment, what: &str) -> Result<&'a Type, String>","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"first_generic","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":354,"end_line":363,"signature":"(seg: &syn::PathSegment) -> Option<&Type>","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"tests","kind":"module","file_path":"crates/fidius-wit/src/lib.rs","start_line":366,"end_line":518,"signature":null,"qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"known","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":369,"end_line":371,"signature":"(names: &[&str]) -> BTreeSet<String>","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"wit","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":372,"end_line":374,"signature":"(s: &str) -> String","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"primitives_strings_containers","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":377,"end_line":385,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"returns","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":388,"end_line":399,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"user_types_need_the_known_set","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":402,"end_line":415,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"struct_renders_to_record","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":418,"end_line":424,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"struct_with_nested_user_type","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":427,"end_line":432,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"enum_renders_to_variant","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":435,"end_line":444,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"struct_variant_synthesizes_a_record","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":447,"end_line":456,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"multifield_tuple_variant_is_rejected","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":459,"end_line":462,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"full_document_places_type_defs_before_funcs","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":465,"end_line":488,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"streaming_method_renders_a_resource","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":491,"end_line":506,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"},{"name":"stream_item_type_detects_marker","kind":"function","file_path":"crates/fidius-wit/src/lib.rs","start_line":509,"end_line":517,"signature":"()","qualified_name":null,"doc_comment":"helper, and the `fidius wit` CLI can all share one implementation.\nThis is a plain library (not a proc-macro), so `fidius-macro`, the `build.rs`\n\n(enums), referenced by their kebab-case name.\ncarry `#[derive(WitType)]` map to WIT `record`s (structs) and `variant`s\n`Option<T>`, and `Result<T, PluginError>` map directly; user types that\n`docs/explanation/wasm-component-abi.md`. Primitives, `String`, `Vec<T>`,\nMaps the Rust types in a `#[plugin_interface]` to WIT, per\n\nWIT generation for the Fidius WASM backend (FIDIUS-I-0023).","visibility":"private"}],"examples/examples/01_load_and_call.rs":[{"name":"Greeter","kind":"interface","file_path":"examples/examples/01_load_and_call.rs","start_line":10,"end_line":12,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 01_load_and_call`\n\nHost composition: load an in-process cdylib plugin and call it.","visibility":"public"},{"name":"Hello","kind":"struct","file_path":"examples/examples/01_load_and_call.rs","start_line":14,"end_line":14,"signature":null,"qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 01_load_and_call`\n\nHost composition: load an in-process cdylib plugin and call it.","visibility":"public"},{"name":"Hello","kind":"type","file_path":"examples/examples/01_load_and_call.rs","start_line":17,"end_line":21,"signature":"impl Greeter for Hello","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 01_load_and_call`\n\nHost composition: load an in-process cdylib plugin and call it.","visibility":"private"},{"name":"greet","kind":"function","file_path":"examples/examples/01_load_and_call.rs","start_line":18,"end_line":20,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 01_load_and_call`\n\nHost composition: load an in-process cdylib plugin and call it.","visibility":"private"},{"name":"main","kind":"function","file_path":"examples/examples/01_load_and_call.rs","start_line":25,"end_line":33,"signature":"()","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 01_load_and_call`\n\nHost composition: load an in-process cdylib plugin and call it.","visibility":"private"}],"examples/examples/02_configure.rs":[{"name":"Config","kind":"struct","file_path":"examples/examples/02_configure.rs","start_line":12,"end_line":14,"signature":"{ greeting: String }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"public"},{"name":"Greeter","kind":"interface","file_path":"examples/examples/02_configure.rs","start_line":17,"end_line":19,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"public"},{"name":"ConfGreeter","kind":"struct","file_path":"examples/examples/02_configure.rs","start_line":21,"end_line":23,"signature":"{ cfg: Config }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"public"},{"name":"ConfGreeter","kind":"type","file_path":"examples/examples/02_configure.rs","start_line":26,"end_line":31,"signature":"impl Greeter for ConfGreeter","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"private"},{"name":"greet","kind":"function","file_path":"examples/examples/02_configure.rs","start_line":27,"end_line":30,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"private"},{"name":"ConfGreeter","kind":"type","file_path":"examples/examples/02_configure.rs","start_line":33,"end_line":37,"signature":"= ConfGreeter","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"private"},{"name":"configure","kind":"function","file_path":"examples/examples/02_configure.rs","start_line":34,"end_line":36,"signature":"(cfg: Config) -> Self","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"private"},{"name":"main","kind":"function","file_path":"examples/examples/02_configure.rs","start_line":41,"end_line":65,"signature":"()","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 02_configure`\n\ninstances of one plugin coexist (partial application).\nHost composition: bind config once, then call — N differently-configured","visibility":"private"}],"examples/examples/03_streaming.rs":[{"name":"Source","kind":"interface","file_path":"examples/examples/03_streaming.rs","start_line":12,"end_line":14,"signature":"{ fn read() }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 03_streaming`\n\n(pull-based, backpressured, drop-to-cancel).\nHost composition: consume a server-streaming plugin via `call_streaming`","visibility":"public"},{"name":"Counter","kind":"struct","file_path":"examples/examples/03_streaming.rs","start_line":16,"end_line":16,"signature":null,"qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 03_streaming`\n\n(pull-based, backpressured, drop-to-cancel).\nHost composition: consume a server-streaming plugin via `call_streaming`","visibility":"public"},{"name":"Counter","kind":"type","file_path":"examples/examples/03_streaming.rs","start_line":19,"end_line":23,"signature":"impl Source for Counter","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 03_streaming`\n\n(pull-based, backpressured, drop-to-cancel).\nHost composition: consume a server-streaming plugin via `call_streaming`","visibility":"private"},{"name":"read","kind":"function","file_path":"examples/examples/03_streaming.rs","start_line":20,"end_line":22,"signature":"(&self, count: u32) -> Stream<u64>","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 03_streaming`\n\n(pull-based, backpressured, drop-to-cancel).\nHost composition: consume a server-streaming plugin via `call_streaming`","visibility":"private"},{"name":"main","kind":"function","file_path":"examples/examples/03_streaming.rs","start_line":28,"end_line":43,"signature":"()","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 03_streaming`\n\n(pull-based, backpressured, drop-to-cancel).\nHost composition: consume a server-streaming plugin via `call_streaming`","visibility":"private"}],"examples/examples/04_pipeline.rs":[{"name":"Reader","kind":"interface","file_path":"examples/examples/04_pipeline.rs","start_line":15,"end_line":17,"signature":"{ fn read() }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"public"},{"name":"Range","kind":"struct","file_path":"examples/examples/04_pipeline.rs","start_line":19,"end_line":19,"signature":null,"qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"public"},{"name":"Range","kind":"type","file_path":"examples/examples/04_pipeline.rs","start_line":22,"end_line":26,"signature":"impl Reader for Range","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"private"},{"name":"read","kind":"function","file_path":"examples/examples/04_pipeline.rs","start_line":23,"end_line":25,"signature":"(&self, count: u32) -> Stream<u64>","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"private"},{"name":"Transformer","kind":"interface","file_path":"examples/examples/04_pipeline.rs","start_line":30,"end_line":32,"signature":"{ fn transform() }","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"public"},{"name":"Labeler","kind":"struct","file_path":"examples/examples/04_pipeline.rs","start_line":34,"end_line":34,"signature":null,"qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"public"},{"name":"Labeler","kind":"type","file_path":"examples/examples/04_pipeline.rs","start_line":37,"end_line":41,"signature":"impl Transformer for Labeler","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"private"},{"name":"transform","kind":"function","file_path":"examples/examples/04_pipeline.rs","start_line":38,"end_line":40,"signature":"(&self, value: u64) -> String","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"private"},{"name":"main","kind":"function","file_path":"examples/examples/04_pipeline.rs","start_line":46,"end_line":73,"signature":"()","qualified_name":null,"doc_comment":"Run: `cargo run -p fidius-examples --example 04_pipeline`\n\nB's input\", driven entirely by the host.\ninto the transformer, one item at a time. This is \"plugin A's output → plugin\nplugins — a streaming *reader* and a *transformer* — wiring the reader's stream\nHost composition: a real **multi-plugin pipeline**. The host orchestrates two","visibility":"private"}],"python/fidius/_errors.py":[{"name":"PluginError","kind":"class","file_path":"python/fidius/_errors.py","start_line":32,"end_line":57,"signature":"(Exception) { __init__ }","qualified_name":null,"doc_comment":"Structured plugin error that round-trips to the host with its fields intact.\n\n    Parameters\n    ----------\n    code:\n        Short machine-readable error code (e.g. ``\"BAD_INPUT\"``).\n    message:\n        Human-readable explanation.\n    details:\n        Optional dict of structured context. Must be JSON-serialisable.","visibility":"public"},{"name":"__init__","kind":"method","file_path":"python/fidius/_errors.py","start_line":45,"end_line":54,"signature":"def __init__(\n        self,\n        code: str,\n        message: str,\n        details: Optional[dict] = None,\n    ) -> None","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"__repr__","kind":"method","file_path":"python/fidius/_errors.py","start_line":56,"end_line":57,"signature":"def __repr__(self) -> str","qualified_name":null,"doc_comment":null,"visibility":"public"}],"python/fidius/_registry.py":[{"name":"method","kind":"function","file_path":"python/fidius/_registry.py","start_line":33,"end_line":49,"signature":"def method(func: Callable) -> Callable","qualified_name":null,"doc_comment":"Register *func* under its ``__name__`` as a fidius plugin method.\n\n    The registry is scoped per-module: two plugins can both expose\n    ``@method def process()`` without clashing. Raises :class:`ValueError`\n    if the *same* module registers two functions with the same name.","visibility":"public"},{"name":"get_method","kind":"function","file_path":"python/fidius/_registry.py","start_line":52,"end_line":70,"signature":"def get_method(name: str, module: str | None = None) -> Callable","qualified_name":null,"doc_comment":"Look up a previously-registered method.\n\n    If *module* is given, looks for the entry registered against that\n    module specifically. If *module* is ``None``, searches across all\n    modules and raises if the name is ambiguous (registered in more than\n    one module). Raises :class:`KeyError` if unknown.","visibility":"public"},{"name":"list_methods","kind":"function","file_path":"python/fidius/_registry.py","start_line":73,"end_line":81,"signature":"def list_methods(module: str | None = None) -> list[str]","qualified_name":null,"doc_comment":"Return the sorted list of registered method names.\n\n    If *module* is given, restricts to that module's methods. Otherwise\n    returns the union across all modules.","visibility":"public"},{"name":"reset_registry","kind":"function","file_path":"python/fidius/_registry.py","start_line":84,"end_line":86,"signature":"def reset_registry() -> None","qualified_name":null,"doc_comment":"Clear the registry. Intended for use in tests; not part of the public API.","visibility":"public"}],"python/tests/test_sdk.py":[{"name":"test_method_registers_under_function_name","kind":"function","file_path":"python/tests/test_sdk.py","start_line":40,"end_line":46,"signature":"def test_method_registers_under_function_name()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_decorator_returns_function_unchanged","kind":"function","file_path":"python/tests/test_sdk.py","start_line":49,"end_line":55,"signature":"def test_decorator_returns_function_unchanged()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_multiple_methods_in_one_module","kind":"function","file_path":"python/tests/test_sdk.py","start_line":58,"end_line":71,"signature":"def test_multiple_methods_in_one_module()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_duplicate_registration_raises","kind":"function","file_path":"python/tests/test_sdk.py","start_line":74,"end_line":83,"signature":"def test_duplicate_registration_raises()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_get_method_unknown_raises_keyerror","kind":"function","file_path":"python/tests/test_sdk.py","start_line":86,"end_line":88,"signature":"def test_get_method_unknown_raises_keyerror()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_plugin_error_carries_code_message_details","kind":"function","file_path":"python/tests/test_sdk.py","start_line":91,"end_line":97,"signature":"def test_plugin_error_carries_code_message_details()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_plugin_error_details_optional","kind":"function","file_path":"python/tests/test_sdk.py","start_line":100,"end_line":102,"signature":"def test_plugin_error_details_optional()","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"test_module_importable_from_vendor_layout","kind":"function","file_path":"python/tests/test_sdk.py","start_line":105,"end_line":134,"signature":"def test_module_importable_from_vendor_layout(tmp_path)","qualified_name":null,"doc_comment":"Simulate the vendored-load pattern: copy fidius/ into a temp dir,\n    add it to sys.path, and verify import + decoration still work.","visibility":"public"}],"tests/test-plugin-py-configured-stream/configured_ticker.py":[{"name":"ConfiguredTicker","kind":"class","file_path":"tests/test-plugin-py-configured-stream/configured_ticker.py","start_line":8,"end_line":14,"signature":"{ __init__, tick }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"__init__","kind":"method","file_path":"tests/test-plugin-py-configured-stream/configured_ticker.py","start_line":9,"end_line":10,"signature":"def __init__(self, config)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"tick","kind":"method","file_path":"tests/test-plugin-py-configured-stream/configured_ticker.py","start_line":12,"end_line":14,"signature":"def tick(self, count)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"__fidius_configure__","kind":"function","file_path":"tests/test-plugin-py-configured-stream/configured_ticker.py","start_line":17,"end_line":18,"signature":"def __fidius_configure__(config)","qualified_name":null,"doc_comment":null,"visibility":"public"}],"tests/test-plugin-py-configured/configured_pipe.py":[{"name":"ConfiguredPipe","kind":"class","file_path":"tests/test-plugin-py-configured/configured_pipe.py","start_line":14,"end_line":22,"signature":"{ __init__, reverse, name }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"__init__","kind":"method","file_path":"tests/test-plugin-py-configured/configured_pipe.py","start_line":15,"end_line":16,"signature":"def __init__(self, config)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"reverse","kind":"method","file_path":"tests/test-plugin-py-configured/configured_pipe.py","start_line":18,"end_line":19,"signature":"def reverse(self, data)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"name","kind":"method","file_path":"tests/test-plugin-py-configured/configured_pipe.py","start_line":21,"end_line":22,"signature":"def name(self)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"__fidius_configure__","kind":"function","file_path":"tests/test-plugin-py-configured/configured_pipe.py","start_line":25,"end_line":27,"signature":"def __fidius_configure__(config)","qualified_name":null,"doc_comment":"Bind the config once and return the configured instance.","visibility":"public"}],"tests/test-plugin-smoke/src/lib.rs":[{"name":"Calculator","kind":"interface","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":21,"end_line":34,"signature":"{ fn add(), fn add_direct(), fn version(), fn multiply() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"AddInput","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":37,"end_line":40,"signature":"{ a: i64, b: i64 }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"AddOutput","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":43,"end_line":45,"signature":"{ result: i64 }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MulInput","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":48,"end_line":51,"signature":"{ a: i64, b: i64 }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MulOutput","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":54,"end_line":56,"signature":"{ result: i64 }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"BasicCalculator","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":58,"end_line":58,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"BasicCalculator","kind":"type","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":61,"end_line":81,"signature":"impl Calculator for BasicCalculator","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"add","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":62,"end_line":66,"signature":"(&self, input: AddInput) -> AddOutput","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"add_direct","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":68,"end_line":70,"signature":"(&self, a: i64, b: i64) -> i64","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"version","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":72,"end_line":74,"signature":"(&self) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"multiply","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":76,"end_line":80,"signature":"(&self, input: MulInput) -> MulOutput","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"ArenaEcho","kind":"interface","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":86,"end_line":88,"signature":"{ fn echo() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ArenaEchoer","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":90,"end_line":90,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ArenaEchoer","kind":"type","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":93,"end_line":97,"signature":"impl ArenaEcho for ArenaEchoer","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"echo","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":94,"end_line":96,"signature":"(&self, input: String) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"BytePipe","kind":"interface","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":103,"end_line":110,"signature":"{ fn reverse(), fn name() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ReverseBytes","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":112,"end_line":112,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ReverseBytes","kind":"type","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":115,"end_line":125,"signature":"impl BytePipe for ReverseBytes","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"reverse","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":117,"end_line":120,"signature":"(&self, mut data: Vec<u8>) -> Vec<u8>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"name","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":122,"end_line":124,"signature":"(&self) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Ticker","kind":"interface","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":134,"end_line":137,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"TickerImpl","kind":"struct","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":142,"end_line":142,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"TickerImpl","kind":"type","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":145,"end_line":149,"signature":"impl Ticker for TickerImpl","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"tick","kind":"function","file_path":"tests/test-plugin-smoke/src/lib.rs","start_line":146,"end_line":148,"signature":"(&self, count: u32) -> fidius::Stream<u64>","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/fetcher/src/lib.rs":[{"name":"Component","kind":"struct","file_path":"tests/wasm-fixtures/fetcher/src/lib.rs","start_line":19,"end_line":19,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Component","kind":"type","file_path":"tests/wasm-fixtures/fetcher/src/lib.rs","start_line":21,"end_line":37,"signature":"impl Guest for Component","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"fetch","kind":"function","file_path":"tests/wasm-fixtures/fetcher/src/lib.rs","start_line":25,"end_line":30,"signature":"(url: String) -> String","qualified_name":null,"doc_comment":"Plain-string return so the host test never has to round-trip a WIT\n`result<>`: success = the body, failure (incl. a denied/blocked egress) =\n`\"ERROR: …\"`.","visibility":"private"},{"name":"fidius_interface_hash","kind":"function","file_path":"tests/wasm-fixtures/fetcher/src/lib.rs","start_line":34,"end_line":36,"signature":"() -> u64","qualified_name":null,"doc_comment":"Interface-hash carrier; the host's `load_wasm` checks it against the\ndescriptor. Arbitrary fixed value for this hand-built fixture.","visibility":"private"},{"name":"do_fetch","kind":"function","file_path":"tests/wasm-fixtures/fetcher/src/lib.rs","start_line":39,"end_line":90,"signature":"(url: String) -> Result<String, String>","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/greeter-go/main.go":[{"name":"init","kind":"function","file_path":"tests/wasm-fixtures/greeter-go/main.go","start_line":17,"end_line":34,"signature":"func init()","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"main","kind":"function","file_path":"tests/wasm-fixtures/greeter-go/main.go","start_line":36,"end_line":36,"signature":"func main()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/greeter-py/app.py":[{"name":"Greeter","kind":"class","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":14,"end_line":33,"signature":"{ greet, add, echo_bytes, probe_env, fidius_interface_hash }","qualified_name":null,"doc_comment":"Implements the exported `greeter` interface.","visibility":"public"},{"name":"greet","kind":"method","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":17,"end_line":18,"signature":"def greet(self, name: str) -> str","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"add","kind":"method","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":20,"end_line":22,"signature":"def add(self, a: int, b: int) -> int","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"echo_bytes","kind":"method","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":24,"end_line":25,"signature":"def echo_bytes(self, data: bytes) -> bytes","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"probe_env","kind":"method","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":27,"end_line":29,"signature":"def probe_env(self) -> bool","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"fidius_interface_hash","kind":"method","file_path":"tests/wasm-fixtures/greeter-py/app.py","start_line":31,"end_line":33,"signature":"def fidius_interface_hash(self) -> int","qualified_name":null,"doc_comment":null,"visibility":"public"}],"tests/wasm-fixtures/greeter/src/lib.rs":[{"name":"bindings","kind":"module","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":9,"end_line":9,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"INTERFACE_HASH","kind":"variable","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":15,"end_line":15,"signature":": u64","qualified_name":null,"doc_comment":"Must match what the host expects for this interface. The host calls\n`fidius-interface-hash` at load and rejects a mismatch.","visibility":"private"},{"name":"Component","kind":"struct","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":17,"end_line":17,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Component","kind":"type","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":19,"end_line":47,"signature":"impl Guest for Component","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"greet","kind":"function","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":20,"end_line":22,"signature":"(name: String) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"add","kind":"function","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":24,"end_line":30,"signature":"(a: i64, b: i64) -> Result<i64, PluginError>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"echo_bytes","kind":"function","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":32,"end_line":37,"signature":"(data: Vec<u8>) -> Vec<u8>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"fidius_interface_hash","kind":"function","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":39,"end_line":41,"signature":"() -> u64","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"probe_env","kind":"function","file_path":"tests/wasm-fixtures/greeter/src/lib.rs","start_line":43,"end_line":46,"signature":"() -> bool","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/macro-configured-stream/src/lib.rs":[{"name":"Cfg","kind":"struct","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":10,"end_line":12,"signature":"{ base: u64 }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"Ticker","kind":"interface","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":15,"end_line":17,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ConfTicker","kind":"struct","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":19,"end_line":21,"signature":"{ cfg: Cfg }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ConfTicker","kind":"type","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":24,"end_line":29,"signature":"impl Ticker for ConfTicker","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"tick","kind":"function","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":25,"end_line":28,"signature":"(&self, count: u32) -> fidius_guest::Stream<u64>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"ConfTicker","kind":"type","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":31,"end_line":35,"signature":"= ConfTicker","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"configure","kind":"function","file_path":"tests/wasm-fixtures/macro-configured-stream/src/lib.rs","start_line":32,"end_line":34,"signature":"(cfg: Cfg) -> Self","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/macro-configured/src/lib.rs":[{"name":"Cfg","kind":"struct","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":12,"end_line":14,"signature":"{ greeting: String }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"Greeter","kind":"interface","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":17,"end_line":19,"signature":"{ fn greet() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ConfGreeter","kind":"struct","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":21,"end_line":23,"signature":"{ cfg: Cfg }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"ConfGreeter","kind":"type","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":26,"end_line":30,"signature":"impl Greeter for ConfGreeter","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"greet","kind":"function","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":27,"end_line":29,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"ConfGreeter","kind":"type","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":32,"end_line":36,"signature":"= ConfGreeter","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"configure","kind":"function","file_path":"tests/wasm-fixtures/macro-configured/src/lib.rs","start_line":33,"end_line":35,"signature":"(cfg: Cfg) -> Self","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/macro-fetcher/src/lib.rs":[{"name":"Fetcher","kind":"interface","file_path":"tests/wasm-fixtures/macro-fetcher/src/lib.rs","start_line":13,"end_line":16,"signature":"{ fn fetch() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyFetcher","kind":"struct","file_path":"tests/wasm-fixtures/macro-fetcher/src/lib.rs","start_line":18,"end_line":18,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyFetcher","kind":"type","file_path":"tests/wasm-fixtures/macro-fetcher/src/lib.rs","start_line":21,"end_line":28,"signature":"impl Fetcher for MyFetcher","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"fetch","kind":"function","file_path":"tests/wasm-fixtures/macro-fetcher/src/lib.rs","start_line":22,"end_line":27,"signature":"(&self, url: String) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/macro-greeter/src/lib.rs":[{"name":"Greeter","kind":"interface","file_path":"tests/wasm-fixtures/macro-greeter/src/lib.rs","start_line":11,"end_line":16,"signature":"{ fn greet(), fn echo() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyGreeter","kind":"struct","file_path":"tests/wasm-fixtures/macro-greeter/src/lib.rs","start_line":18,"end_line":18,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyGreeter","kind":"type","file_path":"tests/wasm-fixtures/macro-greeter/src/lib.rs","start_line":21,"end_line":32,"signature":"impl Greeter for MyGreeter","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"greet","kind":"function","file_path":"tests/wasm-fixtures/macro-greeter/src/lib.rs","start_line":22,"end_line":24,"signature":"(&self, name: String) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"echo","kind":"function","file_path":"tests/wasm-fixtures/macro-greeter/src/lib.rs","start_line":27,"end_line":31,"signature":"(&self, data: Vec<u8>) -> Vec<u8>","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/macro-ticker/src/lib.rs":[{"name":"Ticker","kind":"interface","file_path":"tests/wasm-fixtures/macro-ticker/src/lib.rs","start_line":13,"end_line":16,"signature":"{ fn tick() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyTicker","kind":"struct","file_path":"tests/wasm-fixtures/macro-ticker/src/lib.rs","start_line":18,"end_line":18,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyTicker","kind":"type","file_path":"tests/wasm-fixtures/macro-ticker/src/lib.rs","start_line":21,"end_line":25,"signature":"impl Ticker for MyTicker","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"tick","kind":"function","file_path":"tests/wasm-fixtures/macro-ticker/src/lib.rs","start_line":22,"end_line":24,"signature":"(&self, count: u32) -> fidius_guest::Stream<u64>","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/records-greeter/build.rs":[{"name":"main","kind":"function","file_path":"tests/wasm-fixtures/records-greeter/build.rs","start_line":7,"end_line":9,"signature":"()","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/records-greeter/src/geom.rs":[{"name":"Point","kind":"struct","file_path":"tests/wasm-fixtures/records-greeter/src/geom.rs","start_line":7,"end_line":10,"signature":"{ x: i32, y: i32 }","qualified_name":null,"doc_comment":null,"visibility":"public"}],"tests/wasm-fixtures/records-greeter/src/lib.rs":[{"name":"geom","kind":"module","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":11,"end_line":11,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"Shape","kind":"enum","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":15,"end_line":20,"signature":"Circle | Rect | Triangle | Dot","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"Geo","kind":"interface","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":23,"end_line":26,"signature":"{ fn midpoint(), fn describe() }","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyGeo","kind":"struct","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":28,"end_line":28,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"MyGeo","kind":"type","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":31,"end_line":47,"signature":"impl Geo for MyGeo","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"midpoint","kind":"function","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":32,"end_line":37,"signature":"(&self, a: Point, b: Point) -> Point","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"describe","kind":"function","file_path":"tests/wasm-fixtures/records-greeter/src/lib.rs","start_line":39,"end_line":46,"signature":"(&self, s: Shape) -> String","qualified_name":null,"doc_comment":null,"visibility":"private"}],"tests/wasm-fixtures/ticker-js/ticker.js":[{"name":"TickStream","kind":"class","file_path":"tests/wasm-fixtures/ticker-js/ticker.js","start_line":13,"end_line":26,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"constructor","kind":"method","file_path":"tests/wasm-fixtures/ticker-js/ticker.js","start_line":14,"end_line":17,"signature":"constructor(count)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"next","kind":"method","file_path":"tests/wasm-fixtures/ticker-js/ticker.js","start_line":18,"end_line":25,"signature":"next()","qualified_name":null,"doc_comment":null,"visibility":"public"}],"tests/wasm-fixtures/ticker-py/app.py":[{"name":"TickStream","kind":"class","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":19,"end_line":31,"signature":"(ticker_exports.TickStream) { __init__, next }","qualified_name":null,"doc_comment":"The server-stream resource handle. `next()` is the poll method.","visibility":"public"},{"name":"__init__","kind":"method","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":22,"end_line":24,"signature":"def __init__(self, count: int)","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"next","kind":"method","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":26,"end_line":31,"signature":"def next(self) -> Optional[int]","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"Ticker","kind":"class","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":34,"end_line":42,"signature":"{ tick, fidius_interface_hash }","qualified_name":null,"doc_comment":"Implements the exported `ticker` interface (the free functions).","visibility":"public"},{"name":"tick","kind":"method","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":37,"end_line":38,"signature":"def tick(self, count: int) -> TickStream","qualified_name":null,"doc_comment":null,"visibility":"public"},{"name":"fidius_interface_hash","kind":"method","file_path":"tests/wasm-fixtures/ticker-py/app.py","start_line":40,"end_line":42,"signature":"def fidius_interface_hash(self) -> int","qualified_name":null,"doc_comment":null,"visibility":"public"}],"tests/wasm-fixtures/ticker/src/lib.rs":[{"name":"bindings","kind":"module","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":8,"end_line":8,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"INTERFACE_HASH","kind":"variable","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":17,"end_line":17,"signature":": u64","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Component","kind":"struct","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":19,"end_line":19,"signature":null,"qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Ticks","kind":"struct","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":23,"end_line":26,"signature":"{ current: Cell<u64>, count: u64 }","qualified_name":null,"doc_comment":"Guest-side stream state. `next` takes `&self` (WIT resource methods are\n`&self`), so the cursor lives behind a `Cell`.","visibility":"private"},{"name":"Ticks","kind":"type","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":28,"end_line":38,"signature":"impl GuestTickStream for Ticks","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"next","kind":"function","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":29,"end_line":37,"signature":"(&self) -> Result<Option<u64>, PluginError>","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"Component","kind":"type","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":40,"end_line":53,"signature":"impl Guest for Component","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"TickStream","kind":"type","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":41,"end_line":41,"signature":"= Ticks","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"tick","kind":"function","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":43,"end_line":48,"signature":"(count: u32) -> TickStream","qualified_name":null,"doc_comment":null,"visibility":"private"},{"name":"fidius_interface_hash","kind":"function","file_path":"tests/wasm-fixtures/ticker/src/lib.rs","start_line":50,"end_line":52,"signature":"() -> u64","qualified_name":null,"doc_comment":null,"visibility":"private"}]}}