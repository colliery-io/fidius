[package]
name = "fides-core"
version = "0.1.0"
edition = "2021"
description = "Shared types for the Fides plugin framework"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
bincode = { workspace = true }
thiserror = { workspace = true }
