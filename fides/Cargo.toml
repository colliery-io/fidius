[package]
name = "fides"
version = "0.1.0"
edition = "2021"
description = "Fides — a Rust plugin framework for trait-to-dylib plugin systems"
license = "MIT OR Apache-2.0"

[dependencies]
fides-core = { path = "../fides-core" }
fides-macro = { path = "../fides-macro" }
